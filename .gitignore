# Secrets — real env file never committed (only .env.example / .env.test are tracked)
.env

# Dependencies
node_modules

# Build output
dist

# Bun
.bun

# Generated tooling output (regenerable — never committed)
graphify-out/
.planning/graphs/
.rtk/

# Local scratch + working captures
scratchpad/
chat.png
rich-menu.png
