// STUB — filled by 00-03 (POST /files/presign, GET /files/:key/url via R2 presign).
// Wave-2 replaces the body of THIS file only; it must NOT edit index.ts.
import { Elysia } from "elysia";

export const filesRoutes = new Elysia();
