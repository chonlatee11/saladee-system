// STUB — filled by 00-03 (Bun native S3Client -> Cloudflare R2, presign helpers).
// Wave-2 replaces the body of THIS file only; it must NOT edit index.ts.
import { Elysia } from "elysia";

export const storagePlugin = new Elysia({ name: "storage" });
