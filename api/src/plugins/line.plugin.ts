// STUB — filled by 00-04 (@line/bot-sdk MessagingApiClient from env).
// Wave-2 replaces the body of THIS file only; it must NOT edit index.ts.
import { Elysia } from "elysia";

export const linePlugin = new Elysia({ name: "line" });
