---
phase: 02-line-storefront-payments-delivery
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - web/src/views/CatalogView.vue
  - web/src/views/VarietyDetailView.vue
  - web/src/views/CareView.vue
  - web/src/stores/cart.ts
  - web/src/components/VarietyCard.vue
  - web/src/components/QtyStepper.vue
  - web/src/components/EmptyState.vue
  - web/tests/catalog-view.test.ts
autonomous: true
requirements: [LINE-02]

must_haves:
  truths:
    - "From the Rich Menu 'สั่งผักรอบนี้', the LIFF catalog view lists the open round's varieties/packs with prices, renders a 'หมดรอบนี้' sold-out badge for depleted items, and shows a 'รอบขายปิดชั่วคราว' empty state when no round is open (LINE-02 / UI-SPEC states)."
    - "The variety detail view shows name/description/photo plus storage_tips/washing_tips care fields, hiding a care field when null (D-24 / UI-SPEC)."
    - "The care view (Rich Menu 'ความรู้เรื่องผัก') lists varieties with their care tips."
    - "Adding a pack updates a client cart store; the cart persists across the wizard (consumed by 02-08); all money displayed as ฿ from server-resolved values (never client-computed)."
    - "Every screen implements its full state set from the UI-SPEC inventory (loading · has-items · sold-out · empty · error)."
  artifacts:
    - "web/src/views/{CatalogView, VarietyDetailView, CareView}.vue — the browse surface."
    - "web/src/stores/cart.ts — the cart state consumed by the checkout wizard."
    - "web/src/components/{VarietyCard, QtyStepper, EmptyState}.vue — reusable SFCs."
  key_links:
    - "CatalogView reads GET /catalog via the Eden client (web/src/api.ts from 02-02); care fields come from the 02-03 catalog extension."
    - "cart.ts is the hand-off to the 02-08 checkout wizard."
---

<objective>
Build the LIFF browse surface — the round catalog, the variety detail with care content, and the care list — with a cart store, so a customer can browse the open round and add packs. First customer-facing web slice: after it, a real user opens the Rich Menu and browses/adds salad in LINE.

Purpose: Cover the browse half of LINE-02 and surface D-24 care content in the UI; produce the cart the checkout wizard consumes.
Output: `CatalogView`, `VarietyDetailView`, `CareView`, `cart.ts`, and reusable components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md
@.planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md
@web/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog + variety detail views with full state sets</name>
  <files>web/src/views/CatalogView.vue, web/src/views/VarietyDetailView.vue, web/src/components/VarietyCard.vue, web/src/components/QtyStepper.vue, web/src/components/EmptyState.vue, web/tests/catalog-view.test.ts</files>
  <read_first>
    - web/src/api.ts + web/src/router.ts (from 02-02 — Eden client + route paths / (CatalogView), /variety/:id (VarietyDetailView))
    - .planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md (Key Screen → State Inventory: Catalog + Variety detail rows; Color/Typography/Spacing tokens; Thai copy for sold-out + empty states)
    - api/src/routes/catalog.ts (the GET /catalog response shape incl. care fields from 02-03, sold-out signalling)
  </read_first>
  <action>
    Create CatalogView.vue (route /): fetch the open round's catalog via the Eden client; render VarietyCard per item with server-resolved ฿ price and a QtyStepper (44x44 touch targets, UI-SPEC). Implement all states: loading skeleton, has-items, per-item sold-out badge "หมดรอบนี้" (destructive color), no-open-round empty state "รอบขายปิดชั่วคราว" (EmptyState), and error "เกิดข้อผิดพลาด โปรดลองใหม่อีกครั้ง". Adding to cart calls the cart store (Task 2). Use only the UI-SPEC tokens (accent #2E7D32 reserved for the primary CTA only; steppers use neutral ink).
    Create VarietyDetailView.vue (route /variety/:id): show name/description/photo + storage_tips/washing_tips (hide a care block when null, D-24); add-to-cart CTA; loading + error states.
    Create the reusable components VarietyCard, QtyStepper, EmptyState per the UI-SPEC visual contract (Sarabun, 4/2 type scale, spacing scale). Fill web/tests/catalog-view.test.ts: mount CatalogView with a mocked Eden response and assert the has-items render, the sold-out badge, and the empty state.
  </action>
  <verify>
    <automated>cd web && bun run build && bun test test/catalog-view.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Catalog and variety-detail views render all UI-SPEC states, surface care content, and add packs to the cart; the app builds and the view test passes.</done>
  <acceptance_criteria>
    - `cd web && bun run build` exits 0.
    - `grep -c 'หมดรอบนี้' web/src/views/CatalogView.vue` >= 1 (sold-out copy) and `grep -c 'รอบขายปิดชั่วคราว' web/src/views/CatalogView.vue` >= 1 (empty state).
    - `grep -c 'storageTips\|washingTips' web/src/views/VarietyDetailView.vue` >= 1.
    - `bun test test/catalog-view.test.ts` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Cart store + care list view</name>
  <files>web/src/stores/cart.ts, web/src/views/CareView.vue</files>
  <read_first>
    - web/src/router.ts (routes /care → CareView; the cart is app-global)
    - .planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md (Cart empty state copy "ยังไม่ได้เลือกผัก"; care content surface)
    - api/src/routes/catalog.ts (care fields for the care list)
  </read_first>
  <action>
    Create web/src/stores/cart.ts (a lightweight reactive store or Pinia) holding cart lines { roundId, varietyId, saleUnitId, qty } and box lines { boxId, roundId, qty } — the exact shapes POST /orders expects (never a price/plants field; the server resolves money). Expose add/remove/updateQty/clear and a derived line count; persist to sessionStorage so the wizard survives navigation. Empty state copy handled by consuming views.
    Create web/src/views/CareView.vue (route /care): list varieties with their storage_tips/washing_tips from the catalog read (D-24 content list linked by the Rich Menu content button). Loading + empty + error states.
  </action>
  <verify>
    <automated>cd web && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>The cart store holds server-shaped lines and persists; the care view lists varieties with tips.</done>
  <acceptance_criteria>
    - `cd web && bun run build` exits 0.
    - `grep -c 'saleUnitId\|boxId' web/src/stores/cart.ts` >= 1 (server-shaped lines, no client price field).
    - CareView.vue exists and imports the Eden client.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Views:** `web/src/views/CatalogView.vue`, `VarietyDetailView.vue`, `CareView.vue`.
- **Store:** `web/src/stores/cart.ts` (server-shaped cart lines; sessionStorage-persisted).
- **Components:** `web/src/components/VarietyCard.vue`, `QtyStepper.vue`, `EmptyState.vue`.
- **Test:** `web/tests/catalog-view.test.ts`.
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LIFF client cart → API | The cart holds ids + qty only; the server resolves all money at POST /orders. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17 | Tampering | client cart carrying a price/plants field | high | mitigate | cart.ts stores ids + qty only; POST /orders re-resolves price/plants server-side (Phase-1 rule). |
| T-02-18 | Information Disclosure | XSS via variety description/care fields in the LINE WebView | medium | mitigate | Render care/description via Vue text interpolation (auto-escaped); no v-html on server-supplied text. |
</threat_model>

<verification>
- `cd web && bun run build` succeeds.
- `bun test test/catalog-view.test.ts` green.
- All UI-SPEC catalog/variety states present.
</verification>

<success_criteria>
A customer opens the Rich Menu, browses the open round in LIFF, views a variety with its care tips, and adds packs to a server-shaped cart. Browse half of LINE-02 + D-24 UI covered.
</success_criteria>

<output>
Create `.planning/phases/02-line-storefront-payments-delivery/02-05-SUMMARY.md` when done.
</output>
