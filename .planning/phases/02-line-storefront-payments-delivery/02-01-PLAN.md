---
phase: 02-line-storefront-payments-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/db/schema.ts
  - api/src/env.ts
  - api/src/routes/orders.ts
  - api/src/services/order-transition.ts
  - api/src/services/order-status.ts
  - api/src/jobs/boss.ts
  - api/src/index.ts
  - api/drizzle/0003_payments_delivery_consent.sql
  - api/drizzle/0003_payments_delivery_consent.down.sql
  - api/drizzle/meta/_journal.json
  - api/.env.example
  - api/test/order-transition.test.ts
  - api/test/hold-expiry.test.ts
  - api/test/hold-idempotent.test.ts
  - api/test/promptpay.test.ts
  - api/test/slip-verify.test.ts
  - api/test/slip-dedup.test.ts
  - api/test/delivery.test.ts
  - api/test/notify.test.ts
  - api/test/auth-line.test.ts
  - api/test/reorder.test.ts
  - api/test/consent.test.ts
autonomous: false
requirements: [PAY-03]
user_setup: []

must_haves:
  truths:
    - "The Phase-1 staff PATCH /orders/:id/status still passes all its existing tests after its guarded transition logic is moved into the shared applyTransition() (behavior-preserving extraction) — D-09."
    - "applyTransition(tx, orderId, next, opts) row-locks the order (SELECT FOR UPDATE), re-reads authoritative status, releases reserved stock ONLY on entering cancelled, and — with opts.onlyIfHold — refuses to cancel any order whose locked status is not in {created, awaiting_payment} (D-09, RESEARCH Pitfall 1)."
    - "The full api test suite (existing 118 Phase-1 tests) stays green after migration + extraction."
    - "pg-boss boots against DATABASE_URL_DIRECT and creates the hold-expiry queue without spinning a worker during `bun test` (import.meta.main guard) — D-08, RESEARCH Pitfall 6."
    - "Migration 0003 applies AND rolls back cleanly (up→down→up leaves no residue), creating the payments (trans_ref UNIQUE — D-06), consent_logs, delivery/order snapshot columns, varieties.delivery_class + care columns, and orders hold/qr columns."
  artifacts:
    - "api/src/services/order-transition.ts — exported applyTransition() + OrderError, with an onlyIfHold option and a post-commit notify hook seam."
    - "api/src/jobs/boss.ts — pg-boss singleton + startJobs() (start + createQueue('hold-expiry')) under import.meta.main."
    - "api/drizzle/0003_payments_delivery_consent.sql (+ .down.sql) registered in meta/_journal.json."
    - "New EnvSchema keys: SLIP_VERIFY_PROVIDER, SLIPOK_BRANCH_ID, SLIPOK_API_KEY, PROMPTPAY_PAYEE_ID, HOLD_WINDOW_SECONDS."
    - "10 failing/placeholder test files under api/test/ scaffolding the RESEARCH Validation Architecture (Wave-0 gaps)."
  key_links:
    - "orders.ts PATCH must call applyTransition() (single source of truth) so slip-verify (02-06) and hold-expiry (02-07) reuse the exact same guarded path."
    - "orders.holdExpiresAt is the authoritative hold deadline the 02-07 safety-net sweep reads (RESEARCH Pitfall 2)."
---

<objective>
Establish the shared data + control-flow backbone every Phase-2 vertical slice builds on, and register the phase's test surface (Wave-0). This is the one cross-cutting foundation plan mandated by (a) the schema-gate BLOCKING migration requirement and (b) the single shared files `schema.ts` / `env.ts` / `orders.ts`. All subsequent plans (02-02..02-09) are true vertical slices that consume this backbone and never re-edit the schema.

Purpose: Give every later slice one migrated database, one guarded `applyTransition()` (so three future callers cannot each re-break the Phase-1 concurrent-cancel oversell fix), a booted pg-boss queue, validated env, and a failing test scaffold to make green.
Output: Migration 0003 (applied + reversible), `order-transition.ts`, `boss.ts`, extended `env.ts`/`schema.ts`, and 10 test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md
@.planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md
@api/src/routes/orders.ts
@api/src/services/order-status.ts
@api/src/db/schema.ts
@api/src/env.ts
@api/drizzle/0001_commerce.down.sql
@api/drizzle/0002_prices_default_uniq.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema, migration 0003, and env keys for the whole phase</name>
  <files>api/src/db/schema.ts, api/src/env.ts, api/src/db/schema.ts, api/drizzle/0003_payments_delivery_consent.sql, api/drizzle/0003_payments_delivery_consent.down.sql, api/drizzle/meta/_journal.json, api/.env.example</files>
  <read_first>
    - api/src/db/schema.ts (table conventions: uuid defaultRandom pk, timestamptz defaultNow notNull, pgEnum declared before use, integer satang money, uniqueIndex idiom at lines 39/110/146/197)
    - api/src/env.ts (EnvSchema TypeBox shape, t.String minLength, fail-fast loadEnv)
    - api/drizzle/0002_prices_default_uniq.sql (hand-focused up + CREATE UNIQUE INDEX idiom)
    - api/drizzle/0001_commerce.down.sql (down ordering: children before parents, pgEnum types LAST, IF EXISTS everywhere)
    - api/drizzle/meta/_journal.json (journal entry shape to register 0003)
  </read_first>
  <action>
    In schema.ts add, following the existing conventions:
    (a) pgEnum `deliveryClassEnum = pgEnum("delivery_class", ["very_fresh","normal"])` declared before `varieties`; add columns to `varieties`: `deliveryClass` (deliveryClassEnum, notNull, default "normal"), `storageTips` (text, nullable), `washingTips` (text, nullable) — per D-13/D-24.
    (b) Add columns to `orders`: `holdExpiresAt` (timestamptz, nullable), `qrPayload` (text, nullable), `deliveryMethod` (text, nullable), `deliveryZone` (text, nullable), `deliveryFeeSatang` (integer, nullable) — delivery snapshot alongside subtotalSatang (D-10/D-12/D-14/D-15).
    (c) New table `payments`: id uuid pk, orderId uuid FK->orders notNull, status text notNull (values used by code: "verifying" | "awaiting_review" | "clean" | "rejected"), transRef text nullable, amountSatang integer nullable, rejectReason text nullable, slipKey text nullable, rawJson jsonb nullable, createdAt timestamptz defaultNow notNull. Add `uniqueIndex("payments_trans_ref_idx").on(t.transRef)` (D-06 system-wide dedup; partial WHERE trans_ref IS NOT NULL so multiple NULL-transRef rows for awaiting-review coexist — mirror the partial-index idiom in 0002).
    (d) New table `consent_logs`: id uuid pk, customerId uuid FK->customers nullable, orderId uuid FK->orders nullable, consentType text notNull ("usage" | "marketing"), granted boolean notNull, policyVersion text notNull, source text nullable, createdAt timestamptz defaultNow notNull (D-25/26).
    In env.ts add to EnvSchema (same t.String style, fail-fast): SLIP_VERIFY_PROVIDER (t.String default "slipok"), SLIPOK_BRANCH_ID, SLIPOK_API_KEY, PROMPTPAY_PAYEE_ID (t.String minLength 1), HOLD_WINDOW_SECONDS (t.String default "1800"). NEVER log these values (Phase-0 D-15). Add the same keys with placeholder values to api/.env.example.
    Generate the migration: run `cd api && bun run db:generate` to emit `0003_payments_delivery_consent.sql` from the schema diff (auto-registers in meta/_journal.json — rename the emitted tag to `0003_payments_delivery_consent` if needed to match). Then hand-write `0003_payments_delivery_consent.down.sql` mirroring 0001_commerce.down.sql: drop indexes and the two new tables (consent_logs, payments) and the ALTER TABLE DROP COLUMN statements, then DROP TYPE "delivery_class" LAST, every statement IF EXISTS, `--> statement-breakpoint` separators.
    Do NOT apply the migration in this task — Task 3 is the BLOCKING apply gate.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit && test -f drizzle/0003_payments_delivery_consent.sql && test -f drizzle/0003_payments_delivery_consent.down.sql && grep -q "0003_payments_delivery_consent" drizzle/meta/_journal.json && grep -q "payments_trans_ref_idx" src/db/schema.ts</automated>
  </verify>
  <done>schema.ts + env.ts typecheck; migration 0003 up + down files exist and 0003 is registered in the journal; payments has a UNIQUE trans_ref index; new env keys present in EnvSchema and .env.example.</done>
  <acceptance_criteria>
    - `grep -c 'delivery_class\|payments\|consent_logs\|hold_expires_at\|qr_payload\|delivery_fee_satang' api/drizzle/0003_payments_delivery_consent.sql` is > 0 for each token.
    - `bunx tsc --noEmit` passes in api/.
    - `payments_trans_ref_idx` appears in both schema.ts and the up migration.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract the shared, guarded applyTransition() and refactor the Phase-1 PATCH to use it</name>
  <files>api/src/services/order-transition.ts, api/src/routes/orders.ts, api/src/services/order-status.ts</files>
  <read_first>
    - api/src/routes/orders.ts (PATCH handler lines 518-602: the SELECT FOR UPDATE + canTransition + on-cancel release() block, and OrderError lines 42-49)
    - api/src/services/order-status.ts (TRANSITIONS table + canTransition — keep the enum intact)
    - api/src/services/reservation.ts (release() signature — unchanged)
    - .planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md (order-transition.ts section — the exact block to lift verbatim)
  </read_first>
  <action>
    Create api/src/services/order-transition.ts exporting `OrderError(code, httpStatus)` (moved here from orders.ts) and `async function applyTransition(tx, orderId, next, opts?)`. Lift the row-lock + re-read + `canTransition` + on-cancel release block VERBATIM from the orders.ts PATCH handler (lines 523-583) so the Phase-1 concurrent-cancel fix is preserved exactly. Add option `onlyIfHold?: boolean`: when true, throw a no-op sentinel (or return a `{ skipped: true }` result) unless the locked current status is in {created, awaiting_payment} — this is the guard that stops a late hold-expiry timer from cancelling a just-paid order and releasing sold stock (RESEARCH Pitfall 1 / D-09). Add a post-commit seam: after the tx commits, call an injected `onCommit?(orderId, next)` callback (02-07 wires the Flex notify here) — keep it optional so this plan and 02-06 need no notify dependency. Do NOT put a fenced literal of the milestone list in a head comment.
    Refactor orders.ts PATCH /orders/:id/status to import and call `applyTransition(tx, params.id, next)` instead of the inlined block; import OrderError from order-transition.ts (re-export from orders.ts if other code imports it). Behavior must be identical — the existing Phase-1 status tests are the regression gate.
  </action>
  <verify>
    <automated>cd api && bun test test/orders*.test.ts test/reservation*.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>applyTransition() is the only implementation of the guarded transition; orders.ts PATCH delegates to it; all pre-existing order/reservation tests pass unchanged; onlyIfHold refuses non-hold states.</done>
  <acceptance_criteria>
    - `grep -c 'applyTransition' api/src/routes/orders.ts` >= 1 (PATCH delegates).
    - Existing Phase-1 order + reservation test files pass (`bun test test/orders*.test.ts test/reservation*.test.ts` green).
    - `grep -c 'onlyIfHold' api/src/services/order-transition.ts` >= 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply migration 0003 + boot pg-boss + scaffold Wave-0 tests</name>
  <files>api/src/jobs/boss.ts, api/src/index.ts, api/test/order-transition.test.ts, api/test/hold-expiry.test.ts, api/test/hold-idempotent.test.ts, api/test/promptpay.test.ts, api/test/slip-verify.test.ts, api/test/slip-dedup.test.ts, api/test/delivery.test.ts, api/test/notify.test.ts, api/test/auth-line.test.ts, api/test/reorder.test.ts, api/test/consent.test.ts</files>
  <read_first>
    - api/src/index.ts (import.meta.main guard lines 57-62 — start the worker under the SAME guard)
    - api/src/env.ts (DATABASE_URL_DIRECT — pg-boss must use the unpooled URL, Pitfall 6)
    - .planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md (pg-boss init/lifecycle lines 257-292; Validation Architecture test map lines 660-687)
    - web/tests/eden-types.test.ts (bun:test describe/it/expect shape for scaffolds)
  </read_first>
  <action>
    Create api/src/jobs/boss.ts: `export const boss = new PgBoss(env.DATABASE_URL_DIRECT)` and `export async function startJobs()` that awaits `boss.start()` then `boss.createQueue("hold-expiry")`. Do NOT register the work handler here — 02-07 adds `boss.work("hold-expiry", ...)` + the safety-net sweep. Export a graceful `stopJobs()` calling `boss.stop()`. In index.ts, inside the existing `if (import.meta.main)` block only, call `startJobs()` after `app.listen` (and register `stopJobs()` on shutdown) so `bun test` importing `app` never spins a worker.
    Scaffold the 10 test files listed in RESEARCH Validation Architecture (each a `bun:test` file with a describe + at least one `it(...)` that currently `expect(...).toBe(...)` fails or is `it.todo`, naming the behavior it will assert). These are the Wave-0 gaps other plans make green: promptpay CRC golden vector; slip-verify adapter mapping; slip-dedup transRef unique; hold-expiry release + no-op-on-paid; hold-idempotent singletonKey; delivery fee/freshness matrix; notify members-only; auth-line forged/valid idToken; reorder reprice+flags; consent two-row logging; order-transition onlyIfHold.
    Then APPLY the migration (BLOCKING gate — must run after Task 1 schema mods are complete): `cd api && bun run db:migrate`. This is mandatory; type checks pass without it (types come from schema.ts, not the live DB), so verification would be a false positive without applying. Because migrate targets the live DATABASE_URL_DIRECT and the operator must confirm the target DB, this plan is autonomous:false — pause at this step for the operator to confirm the DB target, then run migrate.
  </action>
  <verify>
    <automated>cd api && bun run db:migrate && bun run db:down && bun run db:migrate && bunx tsc --noEmit && bun test 2>&1 | tail -8</automated>
  </verify>
  <done>Migration 0003 applies, rolls back, and re-applies cleanly (reversible proven); pg-boss creates its queue under import.meta.main; all 10 scaffold files are collected by the runner; the pre-existing 118 Phase-1 tests remain green (new scaffolds may be failing/todo and are made green by later plans).</done>
  <acceptance_criteria>
    - `bun run db:migrate && bun run db:down && bun run db:migrate` all exit 0 (up/down/up reversible).
    - `ls api/test/*.test.ts | wc -l` shows the 10 new scaffolds present.
    - `grep -c 'DATABASE_URL_DIRECT' api/src/jobs/boss.ts` >= 1 (Pitfall 6).
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Tables:** `payments` (with UNIQUE `payments_trans_ref_idx` — D-06), `consent_logs`.
- **Columns:** `varieties.delivery_class` (enum), `varieties.storage_tips`, `varieties.washing_tips`; `orders.hold_expires_at`, `orders.qr_payload`, `orders.delivery_method`, `orders.delivery_zone`, `orders.delivery_fee_satang`.
- **Enum:** `delivery_class` (`very_fresh` | `normal`).
- **Migration:** `api/drizzle/0003_payments_delivery_consent.sql` + `.down.sql` (journal-registered).
- **Service:** `applyTransition(tx, orderId, next, opts)` + `OrderError` in `api/src/services/order-transition.ts` (shared by staff PATCH, slip-verify, hold-expiry).
- **Worker seam:** `boss`, `startJobs()`, `stopJobs()` in `api/src/jobs/boss.ts`; hold-expiry queue created.
- **Env keys:** `SLIP_VERIFY_PROVIDER`, `SLIPOK_BRANCH_ID`, `SLIPOK_API_KEY`, `PROMPTPAY_PAYEE_ID`, `HOLD_WINDOW_SECONDS`.
- **Tests:** 10 Wave-0 scaffold files under `api/test/`.
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pg-boss worker → DB | Background job mutates order state + releases stock; must reuse the guarded transition, never a raw UPDATE. |
| migration operator → live DB | Applying 0003 against production data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | hold-expiry job cancelling a paid order | high | mitigate | applyTransition `onlyIfHold` gates on locked status ∈ {created, awaiting_payment}; row lock + re-read (Pitfall 1). |
| T-02-02 | Denial of Service | pg-boss on pooled endpoint (advisory-lock errors) | medium | mitigate | Point pg-boss at DATABASE_URL_DIRECT, not the -pooler URL (Pitfall 6). |
| T-02-03 | Repudiation | duplicate slip paying two orders | high | mitigate | UNIQUE `payments_trans_ref_idx` schema-wide (D-06) — a unique violation IS the dedup, not vendor-only. |
| T-02-04 | Information Disclosure | secrets (SLIPOK_API_KEY) logged | high | mitigate | Env-only access via EnvSchema; never logged (Phase-0 D-15). |
| T-02-SC | Tampering | npm installs (promptpay-qr/qrcode/pg-boss/sharp) | high | mitigate | RESEARCH Package Legitimacy Audit clean (pg-boss pinned 12.23.0); no postinstall scripts. |
</threat_model>

<verification>
- `cd api && bun run db:migrate && bun run db:down && bun run db:migrate` reversible.
- `cd api && bun test` — pre-existing 118 tests green; new scaffolds collected.
- `bunx tsc --noEmit` clean.
</verification>

<success_criteria>
Migration 0003 applied + reversible; shared applyTransition() is the single guarded transition; pg-boss queue booted under import.meta.main; env validated; 10 test scaffolds registered. No later plan re-edits schema.ts or env.ts.
</success_criteria>

<output>
Create `.planning/phases/02-line-storefront-payments-delivery/02-01-SUMMARY.md` when done.
</output>
