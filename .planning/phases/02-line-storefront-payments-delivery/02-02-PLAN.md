---
phase: 02-line-storefront-payments-delivery
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - api/src/routes/auth.ts
  - api/test/auth-line.test.ts
  - api/scripts/provision-rich-menu.ts
  - web/src/liff.ts
  - web/src/api.ts
  - web/src/style.css
  - web/src/router.ts
  - web/src/App.vue
  - web/src/main.ts
  - web/index.html
autonomous: false
requirements: [LINE-01, LINE-02]
user_setup:
  - service: line
    why: "LIFF app id + Rich Menu + webhook URL are console-only artifacts that block the LINE surface"
    env_vars:
      - name: VITE_LIFF_ID
        source: "LINE Developers Console -> LINE Login channel -> LIFF -> Add (endpoint = deployed web origin)"
    dashboard_config:
      - task: "Create LIFF app under the SAME Login channel whose id is LINE_LOGIN_CHANNEL_ID (aud must match — Pitfall 4)"
        location: "LINE Developers Console -> LINE Login channel -> LIFF"
      - task: "Set the Messaging webhook URL to the prod domain"
        location: "LINE Developers Console -> Messaging channel -> Webhook settings"
      - task: "Run the Rich Menu provisioning script once (needs a 2500x1686 PNG)"
        location: "api/scripts/provision-rich-menu.ts"

must_haves:
  truths:
    - "Inside LIFF, a logged-in user's idToken is verified SERVER-SIDE (jose ES256, iss/aud) and upserts a customers row keyed on line_user_id with isMember=true (D-17); a forged/expired idToken is rejected 401 (RESEARCH Pitfall 4)."
    - "Guest checkout is allowed: the web app functions without an idToken, but history/reorder/push are gated off (D-17)."
    - "The Rich Menu provisioning script creates 5 buttons (สั่งผักรอบนี้, ราคาวันนี้, ติดตามออเดอร์, ติดต่อร้าน, ความรู้เรื่องผัก) deep-linking into LIFF routes and is idempotent (deletes menus by name before create) — LINE-01 / D-18."
    - "The web app is a Vue 3.5 + Vite 8 + Tailwind v4 SPA with Sarabun self-hosted, tokens from UI-SPEC in a style.css @theme block, and a router whose route paths match the Rich Menu deep links."
  artifacts:
    - "api/src/routes/auth.ts POST /auth/line — real customer upsert + session (replaces the placeholder)."
    - "api/scripts/provision-rich-menu.ts — one-time LINE-01 provisioning."
    - "web/src/{liff.ts, api.ts, style.css, router.ts, App.vue} — the LIFF SPA shell."
  key_links:
    - "The router registers every route path (/, /prices, /orders, /contact, /care, /checkout, /pay/:id, /variety/:id) so later view plans (02-05/02-08/02-09) only create the view SFCs, never edit router.ts."
    - "web api.ts treaty<App> imports the api App type — an API change is a compile error in the LIFF app."
---

<objective>
Stand up the LINE surface that every customer slice enters through: server-verified LINE Login (with guest fallback), the greenfield Vue LIFF SPA shell (router, Eden client, design tokens), and the one-time Rich Menu provisioning script. This is walking-slice-zero for the storefront — after it, a real user can open the Rich Menu, land in the LIFF app, and log in with LINE.

Purpose: Unblock LINE-01 (Rich Menu) and LINE-02 (LIFF ordering surface) and give later slices a router with every route path pre-registered so they add only view SFCs.
Output: `POST /auth/line`, `provision-rich-menu.ts`, and `web/src/{liff,api,style,router,App}`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md
@.planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md
@.planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md
@api/src/routes/auth.ts
@api/src/plugins/auth.plugin.ts
@api/src/plugins/line.plugin.ts
@web/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real POST /auth/line — server-verified LINE Login + customer upsert</name>
  <files>api/src/routes/auth.ts, api/test/auth-line.test.ts</files>
  <read_first>
    - api/src/routes/auth.ts (the POST /line placeholder lines 72-90 — it already calls auth.verifyLineIdToken)
    - api/src/plugins/auth.plugin.ts (verifyLineIdToken lines 65-72 enforcing iss/aud=LINE_LOGIN_CHANNEL_ID/ES256; issueSession)
    - api/src/db/schema.ts (customers table lines 153-159: isMember, lineUserId)
    - .planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md (auth.ts MODIFY section)
  </read_first>
  <action>
    Replace the placeholder body of POST /auth/line (auth.ts 74-85): verify idToken via `auth.verifyLineIdToken(body.idToken)` (already implemented — do NOT reimplement). Take `payload.sub` as lineUserId. Upsert a customers row by lineUserId (find-or-insert; set isMember=true, and name from payload.name if present) using the injected db from makeAuthRoutes(db). Issue a session via `auth.issueSession(customerId, role)` — model a customer session (do not mint a staff "packer" role). On a forged/expired token, keep the existing 401 `{ error: "invalid_id_token" }`. Return `{ token, customerId, lineUserId }`.
    Fill api/test/auth-line.test.ts (Wave-0 scaffold from 02-01): mock verifyLineIdToken (or stub JWKS) to assert a forged/expired token → 401, and a valid token upserts exactly one customer with isMember=true and returns a session. Use makeAuthRoutes(testDb).
  </action>
  <verify>
    <automated>cd api && bun test test/auth-line.test.ts</automated>
  </verify>
  <done>POST /auth/line verifies the idToken server-side, upserts a member customer by line_user_id, issues a session, and rejects forged/expired tokens 401.</done>
  <acceptance_criteria>
    - `bun test test/auth-line.test.ts` passes with both the forged-reject and valid-upsert cases.
    - `grep -c 'verifyLineIdToken' api/src/routes/auth.ts` >= 1 and no manual JWT decode is added.
    - `grep -c 'isMember' api/src/routes/auth.ts` >= 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: LIFF SPA shell — liff.ts, Eden client, Tailwind tokens, router, App</name>
  <files>web/src/liff.ts, web/src/api.ts, web/src/style.css, web/src/router.ts, web/src/App.vue, web/src/main.ts, web/index.html</files>
  <read_first>
    - web/src/main.ts (existing treaty client lines 10-23 and guarded initLiff lines 30-35, typeof document guard line 56)
    - .planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md (Design System, Spacing, Typography, Color tokens — put these in style.css @theme; Sarabun self-hosted 400/600)
    - .planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md (web/src/liff.ts + api.ts sections)
    - CLAUDE.md (Vue 3.5 + Vite 8 + Tailwind v4 CSS-first, @tailwindcss/vite; lucide-vue-next per UI-SPEC)
  </read_first>
  <action>
    Split main.ts seams into modules: web/src/api.ts exports `api = treaty<App>(VITE_API_URL)` importing `import type { App } from "../../api/src/index"`; web/src/liff.ts exports `initLiff()` (guarded on VITE_LIFF_ID) plus `getIdToken()` and `loginWithLine()` that calls POST /auth/line via the Eden client and stores the returned session token. Keep the guest path: if no VITE_LIFF_ID or not logged in, the app still runs (D-17).
    Create web/src/style.css with a Tailwind v4 `@theme { }` block declaring the UI-SPEC tokens as CSS custom properties (`--color-*` for #FFFFFF/#F1F5EC/#2E7D32/#D32F2F/#1B2A1B/#5B6B5B/#E2E8DD/#B26A00, `--font-*` Sarabun, `--spacing-*` 4-based); self-host Sarabun 400+600 via @font-face (no Google CDN). Import once in main.ts. Add `lucide-vue-next` (per UI-SPEC).
    Create web/src/router.ts (vue-router) registering ALL route paths with lazily-imported view components, so later plans only create the SFC files: `/` (CatalogView), `/prices` (PricesView), `/variety/:id` (VarietyDetailView), `/care` (CareView), `/checkout` (CheckoutWizard), `/pay/:id` (PayView), `/orders` (OrderHistoryView), `/orders/:id` (OrderDetailView), `/contact` (ContactView). Create web/src/App.vue as the router-view shell + a minimal loading/error boundary. Add placeholder stub SFCs ONLY where a later plan does not own them (PricesView, ContactView) so the router resolves; leave Catalog/Variety/Checkout/Pay/History/Care/OrderDetail as lazy imports the owning plans will create. main.ts bootstraps the router (keep the typeof document test guard). Ensure index.html has viewport-fit=cover (already set per UI-SPEC).
  </action>
  <verify>
    <automated>cd web && bun run build && bun test 2>&1 | tail -5</automated>
  </verify>
  <done>The web app builds; router registers every route path; Eden client + LIFF init are split into modules; design tokens + Sarabun load via style.css; guest path works without a LIFF id.</done>
  <acceptance_criteria>
    - `cd web && bun run build` exits 0.
    - `grep -c "path:" web/src/router.ts` >= 9 (all route paths registered).
    - `grep -c 'VITE_LIFF_ID' web/src/liff.ts` >= 1 and the init is guarded (no throw when absent).
    - `grep -c '@theme' web/src/style.css` >= 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rich Menu provisioning script + LINE console env-gap checkpoint</name>
  <files>api/scripts/provision-rich-menu.ts</files>
  <read_first>
    - api/scripts/r2-smoke.ts (standalone bun-run script shape)
    - api/src/plugins/line.plugin.ts (MessagingApiClient construction from env)
    - .planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md (Rich Menu section lines 300-326; Environment Availability lines 629-646)
    - .planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md (D-18 Thai button labels)
  </read_first>
  <action>
    Create api/scripts/provision-rich-menu.ts (run via `bun run api/scripts/provision-rich-menu.ts`): build `new messagingApi.MessagingApiClient({ channelAccessToken: env.LINE_CHANNEL_ACCESS_TOKEN })` + `messagingApi.MessagingApiBlobClient`. For idempotency, list existing rich menus and delete any named "saladee-main" first. Create a 2500x1686 menu, 5 areas (3-top + 2-bottom grid) with `uri` actions deep-linking to `https://liff.line.me/<VITE_LIFF_ID>` and its /prices, /orders, /contact, /care routes (D-18 order). Read the 2500x1686 PNG path from an arg/env (the image is a design artifact produced separately). Upload via `blob.setRichMenuImage`, then `client.setDefaultRichMenu`. Read the token from env only — never a literal. Log structured progress without secrets.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit scripts/provision-rich-menu.ts 2>&1 | tail -3 && grep -c 'setDefaultRichMenu' scripts/provision-rich-menu.ts</automated>
  </verify>
  <done>The script typechecks, is idempotent (delete-by-name before create), builds the 5-button D-18 menu, and reads the channel token from env.</done>
  <acceptance_criteria>
    - `grep -c 'saladee-main' api/scripts/provision-rich-menu.ts` >= 1 (idempotent delete-by-name).
    - `grep -c 'liff.line.me' api/scripts/provision-rich-menu.ts` >= 1 (deep links).
    - Script does not contain a literal channel token (reads `env.LINE_CHANNEL_ACCESS_TOKEN`).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Checkpoint — provision LIFF app id, webhook URL, and Rich Menu (console-only env gaps)</name>
  <action>Human-verify checkpoint: the operator provisions the three LINE-console artifacts that block auto-verification (LIFF app id → VITE_LIFF_ID, Messaging webhook URL, Rich Menu) per the steps below, then confirms the LIFF app loads and login returns a session.</action>
  <what-built>The LINE Login route, the LIFF SPA shell, and the Rich Menu provisioning script are code-complete, but three console/vendor artifacts (RESEARCH Environment Availability) cannot be auto-verified: the LIFF app id (VITE_LIFF_ID) does not exist yet, the Messaging webhook URL is unset, and the Rich Menu has not been provisioned.</what-built>
  <how-to-verify>
    1. In the LINE Developers Console, create a LIFF app UNDER the same Login channel whose id equals LINE_LOGIN_CHANNEL_ID (aud must match — Pitfall 4); set its endpoint to the deployed web origin (Cloudflare Pages URL). Copy the LIFF id into VITE_LIFF_ID (web build env + GitHub Actions var).
    2. Set the Messaging channel webhook URL to the prod API domain (https://146.190.100.171.sslip.io/webhook or the real domain).
    3. Provide a 2500x1686 Rich Menu PNG and run `bun run api/scripts/provision-rich-menu.ts`; confirm the 5 buttons appear in the LINE chat.
    4. Open the Rich Menu "สั่งผักรอบนี้" button and confirm the LIFF app loads and LINE Login returns a session (or guest path works).
  </how-to-verify>
  <resume-signal>Type "approved" once VITE_LIFF_ID is set, the webhook URL is configured, and the Rich Menu renders; or describe the blocker.</resume-signal>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Route:** `POST /auth/line` (real customer upsert + session) in `api/src/routes/auth.ts`.
- **Script:** `api/scripts/provision-rich-menu.ts` (LINE-01, idempotent, 5 buttons).
- **Web modules:** `web/src/liff.ts` (initLiff/getIdToken/loginWithLine), `web/src/api.ts` (Eden treaty client), `web/src/style.css` (Tailwind v4 @theme tokens + Sarabun), `web/src/router.ts` (all route paths), `web/src/App.vue` (shell).
- **Env key:** `VITE_LIFF_ID` (web build).
- **Placeholder view SFCs:** PricesView, ContactView (owned here); other views are lazy imports owned by later plans.
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LIFF client → API (/auth/line) | Untrusted idToken crosses; must be verified server-side. |
| Rich Menu script → LINE API | Uses the channel access token (secret). |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Spoofing | forged LINE idToken on the LIFF→backend hop | high | mitigate | jose ES256 + iss=access.line.me + aud=LINE_LOGIN_CHANNEL_ID via existing verifyLineIdToken; 401 on failure (Pitfall 4). |
| T-02-06 | Elevation of Privilege | idToken login minting a staff role | high | mitigate | /auth/line issues a customer session only (never owner/admin/packer). |
| T-02-07 | Information Disclosure | channel token in the Rich Menu script | high | mitigate | Token read from env only; never a literal; not logged (Phase-0 D-15). |
| T-02-08 | Spoofing | LIFF endpoint pointing at wrong Login channel | medium | accept | Console-config checkpoint verifies aud match before UAT. |
</threat_model>

<verification>
- `cd api && bun test test/auth-line.test.ts` green.
- `cd web && bun run build` succeeds; router resolves all paths.
- Rich Menu script typechecks; console checkpoint approved.
</verification>

<success_criteria>
A user opens the Rich Menu, lands in the LIFF SPA, and logs in with LINE (server-verified) or continues as guest. Router exposes every route path for later slices. Rich Menu provisioned (LINE-01).
</success_criteria>

<output>
Create `.planning/phases/02-line-storefront-payments-delivery/02-02-SUMMARY.md` when done.
</output>
