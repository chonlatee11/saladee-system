---
phase: 02-line-storefront-payments-delivery
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - api/src/config/delivery.ts
  - api/src/services/delivery.ts
  - api/src/routes/delivery.ts
  - api/src/routes/varieties.ts
  - api/src/routes/catalog.ts
  - api/src/index.ts
  - api/test/delivery.test.ts
autonomous: true
requirements: [DEL-01, DEL-02, DEL-03, DEL-04]

must_haves:
  truths:
    - "Delivery fee is computed server-authoritatively per (zone × method) as a flat rate from a TypeBox-validated config; the client never sets the fee (D-12)."
    - "Free shipping over ฿500 applies ONLY to self + general methods; on-demand and cold are always charged regardless of order value (D-15)."
    - "Freshness gating: a cart's allowed methods = the intersection of ALLOWED_METHODS[deliveryClass] across every variety (and box component); a very_fresh item forces self/cold only; a mixed box uses the strictest class (D-13/DEL-03)."
    - "GET /delivery/quote returns, for a given cart + zone, the allowed methods and the flat fee (0 when free-shipping applies), and the round's deliveryDate read-only (D-14/DEL-04)."
    - "Staff can set varieties.delivery_class + storage_tips/washing_tips via the existing catalog CRUD; the public catalog surfaces care fields on the variety detail (D-24)."
  artifacts:
    - "api/src/config/delivery.ts — zones[], freeShippingThresholdSatang, freeShippingMethods[] (TypeBox-validated at boot)."
    - "api/src/services/delivery.ts — ALLOWED_METHODS, allowedMethodsForCart(), computeDeliveryFee()."
    - "api/src/routes/delivery.ts — GET /delivery/quote (public read)."
  key_links:
    - "computeDeliveryFee() is imported by the 02-04 checkout API to snapshot delivery_fee_satang onto the order and drive the QR amount (Pitfall 5)."
    - "delivery_class read from varieties feeds the freshness intersection (schema from 02-01)."
---

<objective>
Deliver the server-authoritative delivery capability: config-driven zone×method flat-rate fees with a ฿500 free-shipping rule, freshness-based method gating, and care-content editing/surfacing — exposed as a public `GET /delivery/quote` the checkout UI consumes. Pure compute + config; no carrier API (deferred to Phase 4).

Purpose: Cover DEL-01..04 and D-24 care content with a tested fee/freshness engine the checkout slice snapshots onto orders.
Output: `config/delivery.ts`, `services/delivery.ts`, `routes/delivery.ts`, plus varieties/catalog edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md
@.planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md
@api/src/env.ts
@api/src/services/reservation.ts
@api/src/routes/catalog.ts
@api/src/routes/varieties.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delivery config + fee/freshness engine (pure, tested)</name>
  <files>api/src/config/delivery.ts, api/src/services/delivery.ts, api/test/delivery.test.ts</files>
  <behavior>
    - computeDeliveryFee(zoneId, method, subtotalSatang, cfg): returns the flat fee for (zone×method); throws method_not_available_in_zone when the zone lacks that method.
    - Free-shipping: subtotal >= 50000 satang AND method ∈ {self, general} → 0; on_demand/cold never free even over ฿500 (D-15).
    - allowedMethodsForCart(classes[]): intersection of ALLOWED_METHODS per class; [very_fresh] → ["self","cold"]; [normal] → all four; mixed [very_fresh, normal] → ["self","cold"] (strictest wins, D-13).
    - Config fails boot validation if a zone fee is negative or a method key is unknown.
  </behavior>
  <read_first>
    - api/src/env.ts (TypeBox validate-at-boot shape lines 45-52 — mirror for the config validator)
    - api/src/services/reservation.ts (pure exported helper style — boxAvailability)
    - .planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md (Delivery Fee Engine lines 370-402)
    - .planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md (delivery.ts + config/delivery.ts sections)
  </read_first>
  <action>
    Create api/src/config/delivery.ts: a committed TypeBox-validated DeliveryConfig with `zones: { id, nameTh, fees: Partial<Record<DeliveryMethod, number>> }[]` (satang), `freeShippingThresholdSatang: 50000`, `freeShippingMethods: ["self","general"]`. DeliveryMethod = "self" | "cold" | "on_demand" | "general". Validate at load and fail-fast (mirror env.ts). Seed at least two zones (e.g. samut_prakan self-delivery, upcountry general) per D-12.
    Create api/src/services/delivery.ts exporting: `ALLOWED_METHODS: Record<DeliveryClass, DeliveryMethod[]>` ({ very_fresh: ["self","cold"], normal: ["self","cold","on_demand","general"] }); `allowedMethodsForCart(classes)` returning the intersection; `computeDeliveryFee(zoneId, method, subtotalSatang, cfg)` per the behavior block. All money integer satang; never trust a client fee.
    Fill api/test/delivery.test.ts (Wave-0 scaffold): assert the fee matrix (zone×method), the free-shipping rule (self/general free over ฿500; on_demand/cold not), the freshness intersection (very_fresh, normal, mixed), and the method_not_available_in_zone throw.
  </action>
  <verify>
    <automated>cd api && bun test test/delivery.test.ts</automated>
  </verify>
  <done>The engine computes zone×method flat fees, applies ฿500 free-shipping to self/general only, and gates methods by the strictest freshness class; all cases tested.</done>
  <acceptance_criteria>
    - `bun test test/delivery.test.ts` passes with fee-matrix, free-shipping, freshness-intersection, and unavailable-method cases.
    - `grep -c 'freeShippingMethods' api/src/config/delivery.ts` >= 1.
    - `grep -c 'allowedMethodsForCart' api/src/services/delivery.ts` >= 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GET /delivery/quote + care-content edit/surface</name>
  <files>api/src/routes/delivery.ts, api/src/routes/varieties.ts, api/src/routes/catalog.ts, api/src/index.ts</files>
  <read_first>
    - api/src/routes/catalog.ts (public no-guard read shape; variety payload objects lines 169-184 and 343-361; price-resolution helpers)
    - api/src/routes/varieties.ts (CreateVarietyBody/UpdateVarietyBody lines 36-52; PUT patch-builder idiom `if (body.x !== undefined) patch.x = body.x`; requireRole guard)
    - api/src/index.ts (fixed composition order — append the new route via .use, do not reorder)
    - .planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md (delivery route + catalog/varieties MODIFY sections)
  </read_first>
  <action>
    Create api/src/routes/delivery.ts with `makeDeliveryRoutes(db)` (DI + default export, mirror catalog.ts) exposing `GET /delivery/quote` — PUBLIC, no requireRole (like catalog, D-03). Input (query or body): roundId, zoneId, and the cart's variety ids (+ box ids). The handler reads each variety's delivery_class, computes `allowedMethodsForCart`, computes `computeDeliveryFee` per requested method, reads `rounds.deliveryDate`, and returns `{ allowedMethods, fees: { method: satang }, deliveryDate }`. Fee is display-only server-authoritative. Compose it in index.ts via `.use(deliveryRoutes)` (append; do not reorder).
    Extend varieties.ts: add `deliveryClass` (t.Union of "very_fresh"|"normal"), `storageTips` (nullable), `washingTips` (nullable) to Create/Update bodies and the PUT patch-builder (staff-gated, existing requireRole("owner","admin")).
    Extend catalog.ts: add `storageTips`/`washingTips`/`deliveryClass` to the variety-detail payload objects (nullable; reads stay public). Care fields are surfaced on the variety detail (D-24).
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit && bun test test/delivery.test.ts test/catalog*.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>GET /delivery/quote returns allowed methods + fees + deliveryDate for a cart; staff can edit delivery_class + care fields; the public catalog surfaces care fields on the variety detail.</done>
  <acceptance_criteria>
    - `grep -c '/delivery/quote' api/src/routes/delivery.ts` >= 1 and it carries NO requireRole.
    - `grep -c 'deliveryClass\|storageTips' api/src/routes/varieties.ts` >= 1.
    - `grep -c 'storageTips\|washingTips' api/src/routes/catalog.ts` >= 1.
    - `bunx tsc --noEmit` passes; existing catalog tests stay green.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Config:** `api/src/config/delivery.ts` (zones, freeShippingThresholdSatang=50000, freeShippingMethods=["self","general"]).
- **Service:** `ALLOWED_METHODS`, `allowedMethodsForCart()`, `computeDeliveryFee()` in `api/src/services/delivery.ts`.
- **Route:** `GET /delivery/quote` (public) in `api/src/routes/delivery.ts`.
- **Edits:** `varieties.ts` (delivery_class + care fields write), `catalog.ts` (care fields on detail read).
- **DeliveryMethod type:** "self" | "cold" | "on_demand" | "general".
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LIFF client → /delivery/quote | Client supplies cart + zone; server owns the fee + allowed-method decision. |
| staff → varieties write | delivery_class edits gated to owner/admin. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | client-spoofed delivery fee | high | mitigate | Fee computed server-side from config; /delivery/quote is display-only; checkout (02-04) recomputes + snapshots, never trusts the client. |
| T-02-10 | Tampering | bypassing freshness restriction to pick a disallowed method | high | mitigate | allowedMethodsForCart intersection enforced at quote AND re-enforced at checkout (02-04); very_fresh forces self/cold. |
| T-02-11 | Elevation of Privilege | non-staff editing delivery_class/care fields | medium | mitigate | varieties writes keep requireRole("owner","admin"). |
</threat_model>

<verification>
- `cd api && bun test test/delivery.test.ts` green (fee/freshness matrix).
- `bunx tsc --noEmit` clean; catalog tests green.
- GET /delivery/quote returns allowed methods + fees + deliveryDate.
</verification>

<success_criteria>
Delivery fee computes by zone/method with ฿500 free-shipping (self/general only); disallowed methods blocked per product freshness; delivery date derives from the round; care content editable + surfaced. DEL-01..04 + D-24 covered.
</success_criteria>

<output>
Create `.planning/phases/02-line-storefront-payments-delivery/02-03-SUMMARY.md` when done.
</output>
