---
phase: 02-line-storefront-payments-delivery
plan: 07
type: execute
wave: 4
depends_on: ["02-01", "02-04"]
files_modified:
  - api/src/jobs/boss.ts
  - api/src/services/notify.ts
  - api/src/services/order-transition.ts
  - api/src/routes/webhook.ts
  - api/src/index.ts
  - api/test/hold-expiry.test.ts
  - api/test/notify.test.ts
autonomous: true
requirements: [PAY-03, ORD-04, LINE-03]

must_haves:
  truths:
    - "When a QR hold expires, the pg-boss worker cancels ONLY an order whose locked status is in {created, awaiting_payment} and releases its reserved stock in the SAME transaction via the shared applyTransition(onlyIfHold:true); a paid order is a safe no-op (D-09 / Pitfall 1)."
    - "The expiry job no-ops on an order with a payments row in awaiting_review/verifying so a human confirmation is never cancelled out from under (D-04 ↔ D-09)."
    - "A periodic safety-net sweep cancels any awaiting_payment order past holdExpiresAt, so a schedule/commit crash never strands reserved stock (Pitfall 2)."
    - "Status milestones {paid, packing/shipping, done, cancelled/hold-expired} push a Flex card (order summary + deep-link into the LIFF order page) to members with line_user_id ONLY; guests are skipped silently (D-21/22/23)."
    - "The push fires exactly once per transition, hooked inside the shared applyTransition() post-commit seam, so staff PATCH, slip-verify, and expiry all notify consistently (ORD-04)."
    - "The signature-validated webhook keeps raw-body-first ordering when extended — no body schema attached (LINE-03 / Phase-0 Pitfall 1)."
  artifacts:
    - "api/src/jobs/boss.ts — boss.work('hold-expiry') + expireHold() + the periodic sweep."
    - "api/src/services/notify.ts — buildOrderFlex() + pushOrderUpdate() (members only)."
    - "order-transition.ts onCommit wired to notify on milestones."
  key_links:
    - "expireHold() calls applyTransition(tx, orderId, 'cancelled', { onlyIfHold: true }) — the shared guard from 02-01."
    - "notify.ts consumes the injected line.client (line.plugin) pushMessage."
    - "The Flex deep link uses https://liff.line.me/<LIFF_ID>/orders/{orderId}."
---

<objective>
Complete the payment lifecycle back half: automatic stock release on hold expiry (with the paid-order and slip-under-review guards + a safety-net sweep), and Flex-card milestone notifications wired once into the shared transition. After this, an unpaid hold self-heals and a customer gets LINE updates on every milestone.

Purpose: Cover the release half of PAY-03, ORD-04 (auto LINE notify), and LINE-03 (webhook + status notify).
Output: the `hold-expiry` worker + sweep in `boss.ts`, `notify.ts`, and the transition notify hook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md
@.planning/phases/02-line-storefront-payments-delivery/02-PATTERNS.md
@api/src/jobs/boss.ts
@api/src/services/order-transition.ts
@api/src/routes/webhook.ts
@api/src/plugins/line.plugin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hold-expiry worker + safety-net sweep</name>
  <files>api/src/jobs/boss.ts, api/test/hold-expiry.test.ts</files>
  <read_first>
    - api/src/jobs/boss.ts (startJobs/boss.createQueue from 02-01 — add the work handler here)
    - api/src/services/order-transition.ts (applyTransition onlyIfHold from 02-01)
    - api/src/db/schema.ts (orders.holdExpiresAt, payments status from 02-01)
    - .planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md (pg-boss handler lines 283-298; Pitfall 1 & 2)
  </read_first>
  <action>
    In boss.ts startJobs(), register `boss.work("hold-expiry", async ([job]) => expireHold(job.data.orderId))` (v10+ array handler). Implement `expireHold(orderId)` inside `db.transaction`: skip (no-op) if a payments row for the order is in verifying/awaiting_review (D-04 ↔ D-09); otherwise call `applyTransition(tx, orderId, "cancelled", { onlyIfHold: true })` — which only cancels {created, awaiting_payment} and releases stock in the same tx. Add a periodic safety-net sweep: `boss.schedule("hold-sweep", "*/2 * * * *")` (or a cron queue) whose handler cancels every awaiting_payment order whose holdExpiresAt < now via the same expireHold path — authoritative self-heal for the schedule/commit crash window (Pitfall 2). Register the sweep under the same import.meta.main-guarded startJobs.
    Fill api/test/hold-expiry.test.ts: (a) an awaiting_payment order past its hold is cancelled and its reserved plants are released back exactly; (b) a paid order is a NO-OP (stock unchanged, still paid); (c) an order with an awaiting_review payment is skipped.
  </action>
  <verify>
    <automated>cd api && bun test test/hold-expiry.test.ts</automated>
  </verify>
  <done>Expiry releases only unpaid-hold stock, no-ops on paid + on slip-under-review, and the sweep self-heals stranded holds.</done>
  <acceptance_criteria>
    - `bun test test/hold-expiry.test.ts` passes the release, paid-no-op, and review-skip cases.
    - `grep -c 'onlyIfHold' api/src/jobs/boss.ts` >= 1.
    - `grep -c 'hold-sweep\|holdExpiresAt' api/src/jobs/boss.ts` >= 1 (safety-net sweep).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Flex milestone notifications wired into the shared transition + webhook extend</name>
  <files>api/src/services/notify.ts, api/src/services/order-transition.ts, api/src/routes/webhook.ts, api/src/index.ts, api/test/notify.test.ts</files>
  <read_first>
    - api/src/routes/webhook.ts (the line.client.replyMessage call site lines 48-52; raw-body signature validation — do NOT attach a body schema)
    - api/src/plugins/line.plugin.ts (line.client decoration; channelSecret)
    - api/src/services/order-transition.ts (the onCommit seam from 02-01)
    - .planning/phases/02-line-storefront-payments-delivery/02-RESEARCH.md (Notifications lines 349-368; D-21/22/23)
    - .planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md (Notification copy — milestones paid/packing/shipping/done/cancelled; deep-link button)
  </read_first>
  <action>
    Create api/src/services/notify.ts exporting `buildOrderFlex(order, milestone)` (a Flex bubble: order-summary body + a footer uri button "ดูคำสั่งซื้อ" deep-linking to https://liff.line.me/<VITE_LIFF_ID>/orders/{orderId}) and `pushOrderUpdate(line, order, milestone)` calling `line.client.pushMessage({ to: lineUserId, messages: [flex] })` ONLY when the order's customer has a line_user_id (guests skipped, D-23). Map transition→milestone: paid→paid card, packing/shipping→shipping card, done→done card, cancelled→cancelled/hold-expired card; push only on those milestones (D-21), never every transition.
    Wire the order-transition.ts onCommit hook to call pushOrderUpdate after commit so staff PATCH, slip-verify (02-06), and expiry (Task 1) all notify exactly once (ORD-04). The line client must be reachable to the transition callers — inject it (e.g. pass line into applyTransition opts or a module-level notifier configured at boot).
    Extend webhook.ts event handling for any Phase-2 canned reply needs WITHOUT attaching a body schema (keep raw-bytes-first; LINE-03 / Phase-0 Pitfall 1). Never log the channel secret.
    Fill api/test/notify.test.ts: a member (line_user_id present) on a paid transition triggers one pushMessage with a flex message; a guest (no line_user_id) triggers zero pushes; a non-milestone transition (e.g. created→awaiting_payment) triggers zero pushes. Mock the line client.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit && bun test test/notify.test.ts test/webhook*.test.ts 2>&1 | tail -6</automated>
  </verify>
  <done>Milestone transitions push exactly one Flex card to members only; guests and non-milestones push nothing; the webhook stays signature-valid with no body schema.</done>
  <acceptance_criteria>
    - `bun test test/notify.test.ts` passes the member-push, guest-skip, and non-milestone-skip cases.
    - `grep -c 'pushMessage' api/src/services/notify.ts` >= 1 and `grep -c 'line_user_id\|lineUserId' api/src/services/notify.ts` >= 1 (members only).
    - `grep -c 'flex' api/src/services/notify.ts` >= 1 (Flex, not plain text — D-22).
    - webhook.ts still has no body schema on the webhook route (raw-body preserved); existing webhook tests green.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Worker:** `boss.work('hold-expiry')` + `expireHold()` + `hold-sweep` periodic sweep in `api/src/jobs/boss.ts`.
- **Service:** `buildOrderFlex()`, `pushOrderUpdate()` in `api/src/services/notify.ts`.
- **Hook:** order-transition.ts onCommit → notify on milestones.
- **Edit:** webhook.ts extended event handling (raw-body preserved).
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pg-boss worker → DB | Cancels + releases stock; must reuse the guarded transition. |
| API → LINE push | Sends order data to a member's LINE. |
| LINE → webhook | Inbound events; signature-validated over raw bytes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-25 | Tampering | expiry cancelling a just-paid order → releases sold stock | high | mitigate | applyTransition onlyIfHold gates {created, awaiting_payment}; row-locked re-read (Pitfall 1). |
| T-02-26 | Denial of Service | stranded reserved stock when boss.send never ran | medium | mitigate | hold-sweep every 2 min over holdExpiresAt is authoritative (Pitfall 2). |
| T-02-27 | Spoofing | forged LINE webhook event | high | mitigate | Existing raw-body validateSignature preserved; no body schema attached (Phase-0 Pitfall 1). |
| T-02-28 | Information Disclosure | pushing order details to the wrong user | medium | mitigate | Push targets the order's own line_user_id only; guests skipped (D-23). |
| T-02-29 | Information Disclosure | channel secret/token in logs | high | mitigate | Env-only; never logged (Phase-0 D-15). |
</threat_model>

<verification>
- `cd api && bun test test/hold-expiry.test.ts test/notify.test.ts test/webhook*.test.ts` green.
- `bunx tsc --noEmit` clean.
- Full suite green at wave merge.
</verification>

<success_criteria>
An unpaid order's reserved stock auto-releases on hold expiry (never touching paid/under-review orders), a sweep self-heals stranded holds, and customers receive one Flex notification per milestone. PAY-03 (release), ORD-04, LINE-03 covered.
</success_criteria>

<output>
Create `.planning/phases/02-line-storefront-payments-delivery/02-07-SUMMARY.md` when done.
</output>
