---
phase: 00-foundation-platform
plan: 08
type: execute
wave: 1
depends_on: ["00-01", "00-06", "00-07"]
files_modified:
  - api/package.json
  - api/src/env.ts
  - api/src/index.ts
  - api/tests/cors.test.ts
  - bun.lock
autonomous: false
gap_closure: true
requirements: [PLAT-02, PLAT-05]
user_setup: []

must_haves:
  truths:
    - "GET /health with a Pages Origin returns an Access-Control-Allow-Origin header for that origin"
    - "OPTIONS /health preflight from the Pages origin returns 204/200 with Access-Control-Allow-Methods (no longer 404)"
    - "A disallowed origin does NOT receive the Pages origin in Access-Control-Allow-Origin (allowlist, not wildcard)"
    - "The web/ page at https://saladee-web.pages.dev shows 'Saladee API health: ok' (cross-origin Eden Treaty fetch succeeds)"
    - "Allowed origins are env-driven (CORS_ORIGINS) with a safe default, not hardcoded literals in the composition"
  artifacts:
    - path: "api/tests/cors.test.ts"
      provides: "Failing-first test asserting CORS response header + OPTIONS preflight on /health"
      contains: "access-control-allow-origin"
    - path: "api/src/index.ts"
      provides: "cors plugin composed BEFORE routes, origins from env"
      contains: "cors"
    - path: "api/src/env.ts"
      provides: "CORS_ORIGINS env var (comma-separated allowlist) with default"
      contains: "CORS_ORIGINS"
    - path: "api/package.json"
      provides: "@elysiajs/cors pinned dependency"
      contains: "@elysiajs/cors"
  key_links:
    - from: "api/src/index.ts"
      to: "api/src/env.ts CORS_ORIGINS"
      via: "env.CORS_ORIGINS split into the cors origin allowlist"
      pattern: "CORS_ORIGINS"
    - from: "web/src/main.ts getHealth()"
      to: "api /health (cross-origin)"
      via: "browser fetch now allowed by Access-Control-Allow-Origin"
      pattern: "api\\.health\\.get"
---

<objective>
Close the UAT Test 8 gap: the live web/ page (https://saladee-web.pages.dev) shows
"unreachable: health check failed (status 503)" because the API sends **no CORS headers**
and answers **OPTIONS preflight with 404**, so the browser blocks the cross-origin Eden
Treaty fetch. Add `@elysiajs/cors` (pinned) to the API, compose it BEFORE the routes with an
env-driven origin allowlist, and prove it with a failing-first test asserting the header +
preflight. After the CI auto-deploy (push to `develop`), re-verify the live
`Access-Control-Allow-Origin` on `/health` and that the Pages page renders `ok`.

Gap → root cause → fix → verification:
- **Gap** (UAT Test 8): Pages page reports `unreachable: health check failed (status 503)`.
- **Root cause** (verified live): API has no CORS config; `/health` returns 200 but with no
  `Access-Control-Allow-Origin`, and OPTIONS preflight is 404 → browser blocks the fetch →
  Eden Treaty reports the failed fetch as `error.status=503`. curl + bun unit tests never
  caught it because they do not enforce the browser same-origin policy.
- **Fix**: `@elysiajs/cors@1.4.2` composed before routes, allowlist from `CORS_ORIGINS`
  (default = Pages origin + localhost dev), covering OPTIONS preflight.
- **Verification**: RED→GREEN unit test (header + preflight) in the CI `bun test` gate, then a
  live re-check tied to Phase 0 Criterion 1/4 and UAT Test 8.

Purpose: Restore the D-09 Eden Treaty contract end-to-end across origins (Criterion 4 wiring) and
make the live HTTPS `/health` usable from the browser (Criterion 1).
Output: A pinned dependency, an env-driven CORS allowlist, a composed cors plugin, a proving test,
and a live re-verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/00-foundation-platform/00-UAT.md
@.planning/phases/00-foundation-platform/00-07-SUMMARY.md

<interfaces>
<!-- The executor should use these directly — no codebase exploration needed. -->

api/src/index.ts (current composition — cors must be inserted BEFORE dbPlugin/routes):
```typescript
export const app = new Elysia()
  .use(dbPlugin)
  .use(storagePlugin)
  .use(linePlugin)
  .use(authPlugin)
  .use(healthRoutes)      // GET /health -> { status: "ok" }
  .use(webhookRoutes)
  .use(filesRoutes)
  .use(authRoutes);
export type App = typeof app;
```

api/src/env.ts (TypeBox EnvSchema; add CORS_ORIGINS here — schema is the ONLY place
process.env is read; `Value.Default` applies schema defaults so a default keeps boot
working without a new GitHub secret):
```typescript
export const EnvSchema = t.Object({
  NODE_ENV: t.Union([t.Literal("development"), t.Literal("production"), t.Literal("test")]),
  PORT: t.String({ default: "3000" }),
  // ... DATABASE_URL, LINE_*, JWT_SECRET, R2_* ...
});
```

@elysiajs/cors (verified 2026-07-02):
- Latest = 1.4.2; peerDependencies `elysia >= 1.4.0` → compatible with the pinned elysia@1.4.29.
- Usage: `import { cors } from "@elysiajs/cors"` then `.use(cors({ origin: string[] }))`.
- When `origin` is a string[] allowlist, a matching request Origin is reflected in
  `Access-Control-Allow-Origin`; OPTIONS preflight is answered (204) with
  `Access-Control-Allow-Methods` — this is exactly what fixes the 404 preflight.

Biome (lint/format is MANDATORY per CLAUDE.md):
- Root config `biome.json`; `@biomejs/biome@2.5.2`; repo script `biome check .`.
- Run from repo root against the changed files:
  `bunx biome check api/src/env.ts api/src/index.ts api/tests/cors.test.ts`.

Live re-verify targets (from 00-07-SUMMARY):
- API: https://146.190.100.171.sslip.io/health
- Web: https://saladee-web.pages.dev
- CI: push to `develop` → deploy-api.yml (Docker test gate → SSH → `docker compose up -d --build`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add pinned @elysiajs/cors + env-driven allowlist + failing CORS test</name>
  <files>api/package.json, api/src/env.ts, api/tests/cors.test.ts, bun.lock</files>
  <behavior>
    - GET /health with `Origin: https://saladee-web.pages.dev` → response has header
      `access-control-allow-origin` equal to that origin (FAILS before Task 2 — no plugin yet).
    - OPTIONS /health with `Origin: https://saladee-web.pages.dev` and
      `Access-Control-Request-Method: GET` → status 204 (or 200) with an
      `access-control-allow-methods` header (FAILS before Task 2 — preflight is 404 today).
    - A disallowed origin (e.g. `https://evil.example`) → `access-control-allow-origin` is
      NOT that origin (allowlist, not wildcard). Asserting non-reflection.
    - `/health` still returns 200 `{ status: "ok" }` (no regression).
  </behavior>
  <action>
    Add `"@elysiajs/cors": "1.4.2"` to the `dependencies` block of api/package.json (exact pin,
    NO caret/tilde — matches CLAUDE.md's no-floating-versions rule; 1.4.2 is the current latest
    and its peer range `elysia >= 1.4.0` is satisfied by the pinned elysia@1.4.29). Run
    `bun install` from the repo root so the workspace `bun.lock` is regenerated and committed —
    CI runs `bun install --frozen-lockfile`, so a stale lockfile breaks the deploy gate.

    In api/src/env.ts, add a `CORS_ORIGINS` field to `EnvSchema`: a comma-separated allowlist
    string with a safe default so boot still passes without adding a new GitHub secret (env-driven
    per the fix requirement, not hardcoded in the composition). Use
    `CORS_ORIGINS: t.String({ default: "https://saladee-web.pages.dev,http://localhost:5173,http://localhost:3000" })`.
    Do not read process.env anywhere but this schema (existing convention). Note in a comment that
    changing the deployed web origin means overriding this env var, not editing code.

    Create api/tests/cors.test.ts (Bun test, mirror the style of api/tests/health.test.ts): import
    `app` from ../src/index and drive it with `app.handle(new Request(...))`. Write the three
    behavior assertions above using request `Origin` / `Access-Control-Request-Method` headers and
    reading `res.headers.get("access-control-allow-origin")` /
    `res.headers.get("access-control-allow-methods")`. This test needs no DB (uses only /health
    liveness), so it runs green in the CI gate once wired. Run it now and confirm it FAILS (RED)
    because no cors plugin is composed yet.
  </action>
  <verify>
    <automated>cd /home/chonlatee/Desktop/Lab/saladee-system && bun install >/dev/null 2>&1 && grep -q '"@elysiajs/cors": "1.4.2"' api/package.json && grep -q 'CORS_ORIGINS' api/src/env.ts && if (cd api && bun test tests/cors.test.ts) >/dev/null 2>&1; then echo "RED_FAILED_should_fail"; exit 1; else echo RED_OK; fi</automated>
  </verify>
  <done>@elysiajs/cors@1.4.2 pinned and lockfile updated; CORS_ORIGINS env allowlist added with a default; cors.test.ts exists and FAILS (RED, gated on the test's non-zero exit code) because the plugin is not yet composed.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — compose cors plugin BEFORE routes; full suite + Biome pass</name>
  <files>api/src/index.ts</files>
  <action>
    In api/src/index.ts, `import { cors } from "@elysiajs/cors"` and insert `.use(cors({ ... }))`
    as the FIRST plugin — immediately after `new Elysia()` and BEFORE `.use(dbPlugin)` — so it
    intercepts every request including OPTIONS preflight before any route runs. Build the origin
    allowlist from env: split `env.CORS_ORIGINS` on "," and trim each entry, e.g.
    `origin: env.CORS_ORIGINS.split(",").map((o) => o.trim())`. Keep methods to what the API uses
    (`["GET", "POST", "OPTIONS"]`) and allow the `Content-Type` and `Authorization` headers (the
    Eden Treaty client sends JSON and, for auth routes, a bearer token). Do NOT enable
    `credentials: true` with a reflected wildcard — the allowlist is explicit, which keeps the
    Access-Control-Allow-Origin scoped (threat T-00-25). The header comment in index.ts says the
    composition order is fixed by 00-01 for wave-2 slices; this gap-closure hotfix is the
    authorized exception — update that comment to record that cors is intentionally the first
    plugin. Do not touch any other route/plugin ordering.

    Re-run the CORS test (now GREEN), the full api suite, and Biome on the three changed files.
  </action>
  <verify>
    <automated>set -o pipefail; cd /home/chonlatee/Desktop/Lab/saladee-system && grep -q 'cors' api/src/index.ts && (cd api && bun test tests/cors.test.ts) && (cd api && bun test) && bunx biome check api/src/env.ts api/src/index.ts api/tests/cors.test.ts && echo GREEN_OK</automated>
  </verify>
  <done>cors plugin composed before routes with an env-driven allowlist; cors.test.ts passes (GREEN), the full api `bun test` suite is green (exit-code gated), and `biome check` is clean on the three changed files — the CI deploy gate will pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live re-verify CORS on the deployed stack (closes UAT Test 8)</name>
  <what-built>
    Committing this plan and pushing to `develop` triggers CI (deploy-api.yml: Docker test gate →
    SSH → `docker compose up -d --build` on the DigitalOcean droplet), redeploying the API with
    CORS enabled. This checkpoint confirms the LIVE fix and closes UAT Test 8.
  </what-built>
  <action>
    The deploy itself is automated by the CI pipeline (push to `develop` → deploy-api.yml). This is
    a verification checkpoint: after the workflow goes green, the human runs the curl checks and the
    browser reload in how-to-verify and signs off, or reports what failed.
  </action>
  <how-to-verify>
    After the deploy-api workflow completes (green), verify the live stack:

    1. Preflight is now answered (was 404): run
       `curl -i -X OPTIONS https://146.190.100.171.sslip.io/health -H 'Origin: https://saladee-web.pages.dev' -H 'Access-Control-Request-Method: GET'`
       → expect HTTP 204 (or 200) with an `access-control-allow-methods` header.
    2. Simple request carries the allow header: run
       `curl -i https://146.190.100.171.sslip.io/health -H 'Origin: https://saladee-web.pages.dev'`
       → expect HTTP 200 `{"status":"ok"}` AND a header
       `access-control-allow-origin: https://saladee-web.pages.dev`.
    3. Browser end-to-end (the actual UAT Test 8): hard-reload https://saladee-web.pages.dev
       → the page must now read `Saladee API health: ok` (NOT "unreachable ... status 503").
       Optionally open DevTools → Network → /health shows the Access-Control-Allow-Origin response
       header and no CORS console error.

    This maps to Phase 0 **Criterion 1** (HTTPS /health usable) and **Criterion 4** (Eden Treaty
    client wired end-to-end), and closes **UAT Test 8**.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify checkpoint against live infra. `curl -i -X OPTIONS https://146.190.100.171.sslip.io/health -H 'Origin: https://saladee-web.pages.dev' -H 'Access-Control-Request-Method: GET'` returns 204/200 with access-control-allow-methods, and `curl -i https://146.190.100.171.sslip.io/health -H 'Origin: https://saladee-web.pages.dev'` returns access-control-allow-origin for the Pages origin. Requires the deployed droplet; not runnable in CI.</automated>
  </verify>
  <resume-signal>Type "approved" if the Pages page shows "ok" and the allow-origin header is present; otherwise describe what you saw (e.g. header still missing, preflight status).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Pages origin) → API :443 | Cross-origin fetch; browser enforces CORS/same-origin policy |
| CORS_ORIGINS env → runtime allowlist | Config controls which origins are trusted for cross-origin reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-25 | Spoofing / Info Disclosure | overly-permissive CORS (wildcard origin) | mitigate | Explicit env-driven allowlist (Pages + localhost); never `origin: true`/`*` combined with credentials — Access-Control-Allow-Origin stays scoped to known origins |
| T-00-26 | Tampering | @elysiajs/cors supply-chain | mitigate | Exact-pin `1.4.2` (official Elysia plugin, peer `elysia >= 1.4.0`); lockfile committed; CI installs `--frozen-lockfile` |
| T-00-27 | Availability | preflight still failing after deploy | mitigate | Failing-first unit test in the CI gate + live OPTIONS/GET curl re-check before the gap is marked closed |
</threat_model>

<verification>
- api/package.json pins `@elysiajs/cors` at `1.4.2`; `bun.lock` updated and committed.
- api/src/env.ts adds `CORS_ORIGINS` (comma-separated) with a safe default; boot still passes.
- api/src/index.ts composes `cors` as the first plugin (before routes) using the env allowlist.
- api/tests/cors.test.ts asserts the allow-origin header + OPTIONS 204/200 preflight + non-reflection of a disallowed origin; the full `bun test` suite is green and `biome check` is clean.
- Live: OPTIONS /health → 204/200 with allow-methods; GET /health → allow-origin for the Pages origin; https://saladee-web.pages.dev renders "ok" (UAT Test 8 closed).
</verification>

<success_criteria>
- UAT Test 8 passes: the Pages page shows "Saladee API health: ok" (cross-origin fetch succeeds).
- Access-Control-Allow-Origin present on live /health for the Pages origin; OPTIONS preflight no longer 404.
- Fix is env-configurable (CORS_ORIGINS), version-pinned, Biome-clean, and covered by a regression test in the CI gate.
- Maps to Phase 0 Criterion 1 (HTTPS /health usable) and Criterion 4 (Eden Treaty wired end-to-end).
</success_criteria>

<output>
Create `.planning/phases/00-foundation-platform/00-08-SUMMARY.md` when done.
</output>
