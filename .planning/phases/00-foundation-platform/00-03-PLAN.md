---
phase: 00-foundation-platform
plan: 03
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - api/src/plugins/storage.plugin.ts
  - api/src/routes/files.ts
  - api/tests/docker-compose.minio.yml
  - api/tests/storage.test.ts
autonomous: true
requirements: [PLAT-02]
user_setup: []

must_haves:
  truths:
    - "A short-lived presigned PUT URL uploads an object, and a short-lived presigned GET URL downloads that same object (round-trip)"
    - "The storage client targets the account-scoped R2/S3 endpoint, not AWS by default"
    - "Presign TTLs are short (default 300s); no public-read ACL is set"
  artifacts:
    - path: "api/src/plugins/storage.plugin.ts"
      provides: "Bun S3Client → R2 with presignPut/presignGet helpers"
      contains: "presign"
    - path: "api/src/routes/files.ts"
      provides: "/files/presign (PUT) + /files/:key/url (GET) endpoints"
      contains: "/files"
    - path: "api/tests/storage.test.ts"
      provides: "presigned PUT→GET round-trip proof (Criterion 3)"
  key_links:
    - from: "api/src/routes/files.ts"
      to: "api/src/plugins/storage.plugin.ts"
      via: "storage.presignPut / storage.presignGet"
      pattern: "presign(Put|Get)"
    - from: "api/src/plugins/storage.plugin.ts"
      to: "R2 account endpoint"
      via: "S3Client endpoint https://<ACCOUNT_ID>.r2.cloudflarestorage.com"
      pattern: "r2.cloudflarestorage.com"
---

<objective>
Deliver the object-storage slice end-to-end: a Bun-native `S3Client` wired to Cloudflare R2 (account-scoped endpoint, private bucket) exposing short-TTL `presignPut`/`presignGet` helpers, and `/files` routes that mint them. Prove Criterion 3 with an automated presigned **PUT upload → GET download** round-trip against an S3-compatible server (MinIO in Docker) so the test runs in CI without a real R2 account; real R2 is validated later in 00-07.

Purpose: Criterion 3 ("a file uploads to and downloads from object storage via a short-lived signed URL") + the PLAT-03 private-bucket/signed-URL groundwork (Pitfall 4) that slips/photos depend on in Phase 2.
Output: A storage plugin + `/files` endpoints with a passing round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/00-foundation-platform/00-RESEARCH.md
@.planning/phases/00-foundation-platform/00-CONTEXT.md
@.planning/phases/00-foundation-platform/00-VALIDATION.md

<interfaces>
<!-- This plan REPLACES the stub bodies of api/src/plugins/storage.plugin.ts and
     api/src/routes/files.ts. It MUST NOT edit api/src/index.ts (already composes both). -->
From 00-01 (already present):
  api/src/env.ts -> `env` (R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET)
  api/src/plugins/storage.plugin.ts -> `export const storagePlugin` (STUB to replace, keep name "storage")
  api/src/routes/files.ts -> `export const filesRoutes` (STUB to replace)
  api/src/lib/logger.ts -> `log`

Contract this plan defines:
  storagePlugin decorates `storage` = {
    presignPut(key: string, ttl = 300): string,   // method PUT, expiresIn ttl
    presignGet(key: string, ttl = 300): string     // method GET, expiresIn ttl
  }
  POST /files/presign  { key } -> { url }   // presigned PUT (short TTL)
  GET  /files/:key/url          -> { url }   // presigned GET (short TTL)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storage plugin — Bun S3Client → R2 with short-TTL presign helpers</name>
  <read_first>
    - .planning/phases/00-foundation-platform/00-RESEARCH.md (Pattern 2 storage.plugin example; Pitfall 4 private bucket; Pitfall 6 account endpoint; Assumption A1 on presign arg shape)
    - .planning/phases/00-foundation-platform/00-CONTEXT.md (PLAT-03 private bucket + signed URL groundwork)
    - api/src/env.ts (R2_* vars)
    - api/src/plugins/storage.plugin.ts (stub to replace)
  </read_first>
  <action>
    Replace the `api/src/plugins/storage.plugin.ts` stub: construct `new S3Client({ accessKeyId: env.R2_ACCESS_KEY_ID, secretAccessKey: env.R2_SECRET_ACCESS_KEY, bucket: env.R2_BUCKET, endpoint: \`https://\${env.R2_ACCOUNT_ID}.r2.cloudflarestorage.com\` })` (import `S3Client` from `"bun"`; the account-scoped endpoint is required or the client targets AWS — Pitfall 6). Export `storagePlugin = new Elysia({ name: "storage" }).decorate("storage", { presignPut, presignGet })` where `presignPut(key, ttl=300)` calls the client presign for method PUT with `expiresIn: ttl` and `presignGet(key, ttl=300)` for method GET. Set NO public-read ACL and do not enable any public bucket URL (Pitfall 4). Before coding, confirm the exact Bun presign signature against the installed Bun 1.3.14 (`s3.presign(key, { method, expiresIn })` vs `s3.file(key).presign({...})` — Assumption A1) and use whichever the installed runtime exposes. Do NOT edit index.ts.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit -p tsconfig.json 2>&1 | tail -5 && grep -n "r2.cloudflarestorage.com" src/plugins/storage.plugin.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `storage.plugin.ts` constructs `S3Client` with `endpoint` containing `r2.cloudflarestorage.com` and reads all four R2 vars from `env` (no literals).
    - Source assertion: `presignPut`/`presignGet` default `ttl` to 300 (grep confirms `= 300`).
    - Source assertion: no `public-read` / `acl` public setting anywhere in the file.
    - Source assertion: `export const storagePlugin` retains `name: "storage"`; `api/src/index.ts` unchanged (git diff).
    - CLI output: `bunx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A private-R2 storage plugin exposes short-TTL presignPut/presignGet, correctly endpoint-scoped, decorated onto context for `/files`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /files routes + presigned PUT→GET round-trip test (MinIO)</name>
  <read_first>
    - .planning/phases/00-foundation-platform/00-RESEARCH.md (Pitfall 4 presigned upload/download; VALIDATION storage.test.ts row — Criterion 3)
    - api/src/plugins/storage.plugin.ts (Task 1 — storage.presign*)
    - api/src/routes/files.ts (stub to replace)
  </read_first>
  <behavior>
    - Test: request a presigned PUT for a key, HTTP PUT bytes to that URL → 200/2xx.
    - Test: request a presigned GET for the same key, HTTP GET that URL → the same bytes back.
    - Test: the object is NOT retrievable via an unsigned URL (bucket stays private).
  </behavior>
  <action>
    Replace the `api/src/routes/files.ts` stub: `POST /files/presign` with a TypeBox body `{ key: string }` returning `{ url: storage.presignPut(key, 300) }`; `GET /files/:key/url` returning `{ url: storage.presignGet(key, 300) }`. Constrain the key prefix (e.g. reject keys with `..` or leading `/`) per V12. Create `api/tests/docker-compose.minio.yml` with a `minio` service (S3-compatible) exposing an endpoint + test access key/secret + a pre-created private bucket (via a `mc`/init or the test's `beforeAll`). Create `api/tests/storage.test.ts` per the behavior block: point a Bun `S3Client` at the MinIO endpoint using test R2_* env, call the `/files/presign` route (in-process via `app.handle`) to obtain the PUT URL, `fetch(putUrl, { method:"PUT", body })`, then obtain a GET URL and `fetch(getUrl)` asserting the body matches; also assert an unsigned GET of the object returns 403/401. Use the account-endpoint pattern so the same code path exercises R2 in prod. Do NOT edit index.ts.
  </action>
  <verify>
    <automated>cd api && docker compose -f tests/docker-compose.minio.yml up -d && bun test tests/storage.test.ts; docker compose -f tests/docker-compose.minio.yml down -v</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `cd api && bun test tests/storage.test.ts` exits 0; presigned PUT upload then presigned GET download returns identical bytes.
    - Behavior: an unsigned GET of the uploaded object is rejected (403/401) — bucket is private (Pitfall 4).
    - Source assertion: `api/src/routes/files.ts` exposes `POST /files/presign` and `GET /files/:key/url`, validates the key (no `..`), and uses `storage.presign*` (never a manual SigV4).
    - Source assertion: `api/src/index.ts` unchanged (git diff) — parallel-safe.
  </acceptance_criteria>
  <done>Criterion 3 proven: a file round-trips through object storage via short-lived signed URLs, private-bucket-only, on the same code path prod R2 will use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /files/presign | Untrusted key input; presign only after key validation |
| presigned URL → R2 | Time-boxed capability (300s); bucket otherwise private |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-08 | Information Disclosure | public/over-broad object access (slips) | mitigate | Private R2 bucket, no public-read ACL, short-TTL (300s) presigned URLs only (Pitfall 4); test asserts unsigned GET is rejected |
| T-00-09 | Tampering | path traversal / key injection in presign | mitigate | Validate key prefix (reject `..`, leading `/`) before presign (V12) |
| T-00-10 | Info Disclosure | R2 credentials leak | mitigate | Credentials only from `env`; presign is synchronous local signing (no creds in the URL beyond the signature); never logged |
| T-00-11 | Spoofing | wrong endpoint → data sent to AWS not R2 | mitigate | Account-scoped `endpoint` asserted in source + test (Pitfall 6) |
</threat_model>

<verification>
- `cd api && bun test tests/storage.test.ts` green against MinIO (S3-compatible).
- Storage plugin endpoint-scoped to R2; presign TTL 300s; no public ACL.
- `git diff` shows `api/src/index.ts` untouched.
- NOTE: real Cloudflare R2 round-trip is verified in 00-07 (manual, needs the provisioned bucket).
</verification>

<success_criteria>
- Criterion 3: upload + download via short-lived signed URL — automated round-trip.
- PLAT-03 groundwork: private bucket + signed URLs, ready for Phase 2 slips.
</success_criteria>

<output>
Create `.planning/phases/00-foundation-platform/00-03-SUMMARY.md` when done.
</output>
