---
phase: 00-foundation-platform
plan: 04
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - api/src/plugins/line.plugin.ts
  - api/src/routes/webhook.ts
  - api/tests/webhook.test.ts
autonomous: true
requirements: [PLAT-05]
user_setup: []

must_haves:
  truths:
    - "A POST /webhook with a valid x-line-signature over the raw body is accepted and echoes the text message"
    - "A POST /webhook with an invalid or absent signature is rejected with 401"
    - "Signature validation runs over the RAW request bytes, before any JSON parse (survives Thai/non-ASCII)"
  artifacts:
    - path: "api/src/plugins/line.plugin.ts"
      provides: "@line/bot-sdk MessagingApiClient from env"
      contains: "MessagingApiClient"
    - path: "api/src/routes/webhook.ts"
      provides: "raw-body HMAC validation + echo reply + 401 reject"
      contains: "validateSignature"
    - path: "api/tests/webhook.test.ts"
      provides: "valid→echo / invalid→401 proof (Criterion 4 / D-06)"
  key_links:
    - from: "api/src/routes/webhook.ts"
      to: "@line/bot-sdk validateSignature"
      via: "validateSignature(rawBody, CHANNEL_SECRET, xLineSignature)"
      pattern: "validateSignature"
    - from: "api/src/routes/webhook.ts"
      to: "raw request bytes"
      via: "await request.text() before JSON.parse"
      pattern: "request.text\\(\\)"
---

<objective>
Deliver the LINE slice end-to-end: a `@line/bot-sdk` `MessagingApiClient` built from validated env, and a real `POST /webhook` that captures the **raw request bytes before any parse**, validates `x-line-signature` (HMAC over the raw body), echoes text messages back via the reply API, and rejects invalid/absent signatures with 401. Prove it with a Bun test that computes a real HMAC over a Thai/non-ASCII payload (guards against the raw-body landmine, Pitfall 1).

Purpose: Criterion 4 / D-06 ("LINE API client wired end-to-end, not stubbed; validates signature and echoes") + the webhook trust boundary that Phase 2 status-notifications extend.
Output: A signature-validated echo webhook with automated valid/invalid coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/00-foundation-platform/00-RESEARCH.md
@.planning/phases/00-foundation-platform/00-CONTEXT.md
@.planning/phases/00-foundation-platform/00-VALIDATION.md

<interfaces>
<!-- This plan REPLACES the stub bodies of api/src/plugins/line.plugin.ts and
     api/src/routes/webhook.ts. It MUST NOT edit api/src/index.ts (already composes both). -->
From 00-01 (already present):
  api/src/env.ts -> `env` (LINE_CHANNEL_SECRET, LINE_CHANNEL_ACCESS_TOKEN)
  api/src/plugins/line.plugin.ts -> `export const linePlugin` (STUB to replace, keep name "line")
  api/src/routes/webhook.ts -> `export const webhookRoutes` (STUB to replace)
  api/src/lib/logger.ts -> `log`

CRITICAL sequencing (Pitfall 1): the /webhook handler MUST read `await request.text()` to get
raw bytes and validate the signature BEFORE `JSON.parse`. Do NOT attach a TypeBox body schema to
/webhook (that forces Elysia to parse first and the raw bytes are lost — signature will fail on
real LINE traffic, especially Thai text).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LINE Messaging plugin from env</name>
  <read_first>
    - .planning/phases/00-foundation-platform/00-RESEARCH.md (Pitfall 1 raw body; State of the Art v11 MessagingApiClient; Assumption A5 11.0.2 vs 11.1.0)
    - .planning/phases/00-foundation-platform/00-CONTEXT.md (D-06 LINE end-to-end)
    - api/src/env.ts (LINE_CHANNEL_* vars)
    - api/src/plugins/line.plugin.ts (stub to replace)
  </read_first>
  <action>
    Replace the `api/src/plugins/line.plugin.ts` stub: construct `new messagingApi.MessagingApiClient({ channelAccessToken: env.LINE_CHANNEL_ACCESS_TOKEN })` from `@line/bot-sdk` (v11 surface — pinned 11.0.2). Export `linePlugin = new Elysia({ name: "line" }).decorate("line", { client, channelSecret: env.LINE_CHANNEL_SECRET })` so the webhook route gets the reply client and the secret from context. Do NOT edit index.ts.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit -p tsconfig.json 2>&1 | tail -5 && grep -n "MessagingApiClient" src/plugins/line.plugin.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `line.plugin.ts` builds a `MessagingApiClient` with the access token from `env` (no literal token).
    - Source assertion: `export const linePlugin` retains `name: "line"` and decorates `line` with `client` + `channelSecret`.
    - Source assertion: `api/package.json` still pins `@line/bot-sdk@11.0.2` (not floated).
    - CLI output: `bunx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A real LINE Messaging client is constructed from env and available to the webhook route (no stub).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /webhook — raw-body signature validation + echo + tests</name>
  <read_first>
    - .planning/phases/00-foundation-platform/00-RESEARCH.md (Pitfall 1 full example; VALIDATION webhook.test.ts row — Criterion 4/D-06; Assumption A2)
    - api/src/plugins/line.plugin.ts (Task 1 — line.client, line.channelSecret)
    - api/src/routes/webhook.ts (stub to replace)
  </read_first>
  <behavior>
    - Test: build a JSON LINE event body containing Thai text; compute `x-line-signature` = base64(HMAC-SHA256(rawBody, CHANNEL_SECRET)); POST → 200 and the reply API is invoked with the echoed text.
    - Test: POST the same body with a wrong signature → 401.
    - Test: POST with NO x-line-signature header → 401.
  </behavior>
  <action>
    Replace the `api/src/routes/webhook.ts` stub: `POST /webhook` handler that (1) reads `const raw = await request.text()` FIRST (raw bytes — Pitfall 1), (2) reads `x-line-signature` header, (3) calls `validateSignature(raw, line.channelSecret, sig)` from `@line/bot-sdk`; on false or missing sig set status 401 and return `"invalid signature"`, (4) only then `JSON.parse(raw)` and for each `events[]` where `type==="message" && message.type==="text"` call `line.client.replyMessage({ replyToken, messages:[{ type:"text", text: message.text }] })`, (5) return `"ok"` 200. Do NOT attach a TypeBox body schema to this route (would force a parse and destroy raw bytes). Create `api/tests/webhook.test.ts` per the behavior block: use Node/Bun `crypto` to compute the HMAC-SHA256 base64 signature over the exact raw JSON string (INCLUDING Thai characters, e.g. "สวัสดี"), stub/spy the reply client (inject a fake `line` decorate or mock `replyMessage`) to assert it is called with the echoed text, and drive requests via `app.handle(new Request(...))`. Do NOT edit index.ts.
  </action>
  <verify>
    <automated>cd api && bun test tests/webhook.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `cd api && bun test tests/webhook.test.ts` exits 0 with valid→200-echo, wrong-sig→401, missing-sig→401.
    - Behavior: the valid-signature case uses a payload with NON-ASCII (Thai) text and still validates — proving raw bytes survive (Pitfall 1 / A2).
    - Source assertion: `webhook.test.ts` reply spy is called with `text` equal to the incoming message text (echo).
    - Source assertion: `webhook.ts` calls `request.text()` BEFORE `JSON.parse`, calls `validateSignature`, and has NO TypeBox body schema on `/webhook`.
    - Source assertion: `api/src/index.ts` unchanged (git diff) — parallel-safe.
  </acceptance_criteria>
  <done>Criterion 4 / D-06 proven: valid-signature messages echo, forged/absent signatures are rejected 401, over the raw body — the LINE OA link is wired end-to-end, not stubbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LINE Platform → /webhook | Untrusted inbound events; authenticity proven only by HMAC over raw body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-12 | Spoofing | forged LINE webhook events | mitigate | `validateSignature` (constant-time) over RAW body; reject 401 on mismatch/absent (D-06); test covers wrong + missing sig |
| T-00-13 | Tampering | body re-serialization breaking HMAC | mitigate | Read `request.text()` before parse; no TypeBox schema on the route (Pitfall 1); Thai-payload test guards regressions |
| T-00-14 | Repudiation / Replay | replayed valid events | accept | Phase 0 echo is idempotent/harmless; replay-nonce/dedup deferred to Phase 2 when webhook drives state (noted) |
| T-00-15 | Info Disclosure | channel secret/token in logs | mitigate | Secret/token only from `env`; never logged; reply client built from env |
</threat_model>

<verification>
- `cd api && bun test tests/webhook.test.ts` green (valid echo + invalid/missing 401, Thai payload).
- Handler reads raw bytes before parse; no body schema on /webhook.
- `git diff` shows `api/src/index.ts` untouched.
- NOTE: real LINE OA delivery/echo is verified in 00-07 (manual, needs the provisioned Messaging channel + public HTTPS).
</verification>

<success_criteria>
- Criterion 4 / D-06: LINE client wired end-to-end; signature-validated echo; invalid rejected — automated.
</success_criteria>

<output>
Create `.planning/phases/00-foundation-platform/00-04-SUMMARY.md` when done.
</output>
