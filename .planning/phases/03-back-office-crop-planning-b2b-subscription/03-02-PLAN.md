---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/package.json
  - api/src/pdf/pack-slip.ts
  - api/src/pdf/label-slip.ts
  - api/src/fonts/Sarabun-Regular.ttf
  - api/src/fonts/Sarabun-SemiBold.ttf
  - api/tests/pdf-thai.test.ts
autonomous: true
requirements: [ORD-03]
user_setup:
  - service: Sarabun font (SIL OFL)
    why: "pdfmake ต้อง embed ฟอนต์ไทย ไม่งั้นภาษาไทยเป็น tofu (D-21)"
    dashboard_config:
      - task: "ดาวน์โหลด Sarabun-Regular.ttf + Sarabun-SemiBold.ttf (SIL OFL) วางที่ api/src/fonts/ — executor ทำผ่าน curl ได้ (Google Fonts / OFL mirror)"
        location: "api/src/fonts/"
must_haves:
  truths:
    - "pdfmake 0.3.11 บน Bun 1.3 render Thai (Sarabun) PDF buffer สำเร็จ ผ่าน PdfPrinter + on-disk TTF (ไม่ใช่ vfs blob) — A1 spike ผ่าน"
    - "renderPackSlip(doc) / renderLabelSlip(doc) คืน Buffer ที่ %PDF header ถูกต้อง และมี glyph ไทยจริง (ไม่ว่าง/ไม่ tofu)"
  artifacts:
    - api/src/pdf/pack-slip.ts
    - api/src/pdf/label-slip.ts
    - api/src/fonts/Sarabun-Regular.ttf
    - api/src/fonts/Sarabun-SemiBold.ttf
    - api/tests/pdf-thai.test.ts
  key_links:
    - "09-packing import renderPackSlip/renderLabelSlip — แผนนี้ต้องผ่านก่อน (Wave-0 risk gate A1)"
---

<objective>
Wave-0 risk spike (RESEARCH A1/Pitfall 3 — ความเสี่ยงสูงสุดของเฟส): พิสูจน์ว่า pdfmake render PDF ภาษาไทย (Sarabun) เป็น Buffer บน Bun ได้จริง ผ่าน `PdfPrinter` + TTF บนดิสก์ ก่อนที่ 09-packing จะพึ่งพา.

ต้องรันก่อน 09 เสมอ. ถ้า Bun/pdfkit เข้ากันไม่ได้ ให้ fallback vfs แบบมาตรฐานและบันทึกใน SUMMARY.

Purpose: ปลดล็อกความเสี่ยง D-21 ตั้งแต่ Wave 1.
Output: pdf render lib (pack/label) + Sarabun fonts + smoke test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-RESEARCH.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-PATTERNS.md
@api/src/services/notify.ts
</context>

<artifacts>
## Artifacts this plan produces
- `api/src/pdf/pack-slip.ts` — `renderPackSlip(doc: TDocumentDefinitions): Promise<Buffer>` + PdfPrinter Sarabun registration.
- `api/src/pdf/label-slip.ts` — `renderLabelSlip(doc): Promise<Buffer>` (sibling).
- `api/src/fonts/Sarabun-Regular.ttf`, `Sarabun-SemiBold.ttf` (SIL OFL, weights 400+600 ตาม UI-SPEC typography).
- `api/tests/pdf-thai.test.ts` — smoke test.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: ติดตั้ง pdfmake + วาง Sarabun TTF + PdfPrinter render lib</name>
  <read_first>03-RESEARCH.md §Pattern 6 (PdfPrinter, ไม่ใช่ vfs), 03-PATTERNS.md pack-slip section, api/src/services/notify.ts (baht(satang) helper 55–60, buildOrderFlex doc-tree style 67–119)</read_first>
  <files>api/package.json, api/src/fonts/Sarabun-Regular.ttf, api/src/fonts/Sarabun-SemiBold.ttf, api/src/pdf/pack-slip.ts, api/src/pdf/label-slip.ts</files>
  <action>
`cd api &amp;&amp; bun add pdfmake` (0.3.11 — อยู่ใน CLAUDE.md table แล้ว; Package Legitimacy Audit ผ่าน Approved). ดาวน์โหลด Sarabun-Regular.ttf + Sarabun-SemiBold.ttf (SIL OFL) วาง `api/src/fonts/`.

สร้าง `pack-slip.ts`: register `PdfPrinter` ด้วย `Sarabun: { normal: "src/fonts/Sarabun-Regular.ttf", bold: "src/fonts/Sarabun-SemiBold.ttf" }`; export `renderPackSlip(doc)` ที่ `createPdfKitDocument({ ...doc, defaultStyle: { font: "Sarabun" } })` แล้ว collect chunks เป็น Buffer (คืน Promise, resolve on 'end', reject on 'error'). เงินในเอกสาร = helper แบบ `baht(satang)` (`toLocaleString("th-TH")`) จาก notify.ts. `label-slip.ts` เป็น sibling: `renderLabelSlip(doc)` โครงเดียวกัน.

ห้ามใช้ vfs base64 blob เว้นแต่ PdfPrinter พังบน Bun (บันทึก fallback ใน SUMMARY).
  </action>
  <verify>
    <automated>cd api &amp;&amp; bunx tsc --noEmit 2>&amp;1 | head -10</automated>
  </verify>
  <done>pdfmake ติดตั้ง; Sarabun TTF 2 ไฟล์อยู่ใน fonts/; renderPackSlip/renderLabelSlip typecheck ผ่าน.</done>
</task>

<task type="auto">
  <name>Task 2: smoke test — Thai PDF buffer จริง (A1)</name>
  <read_first>api/tests/ โครงสร้าง test เดิม, 03-RESEARCH.md Pitfall 3 (สัญญาณ: buffer ว่าง / .end() ไม่ยิง / tofu)</read_first>
  <files>api/tests/pdf-thai.test.ts</files>
  <action>
เขียน smoke: เรียก `renderPackSlip` ด้วย doc ตัวอย่างที่มีข้อความไทย (เช่น "ใบแพ็ค รอบส่ง — ผักสลัด"). assert: (1) ผลเป็น Buffer ยาว &gt; 1000 bytes, (2) 5 ไบต์แรก = `%PDF-` (magic header), (3) ไม่ throw / Promise resolve ภายใน timeout. นี่คือ gate A1: ยืนยัน pdfkit stream ยิง 'end' บน Bun และ embed Sarabun ได้.
  </action>
  <verify>
    <automated>cd api &amp;&amp; bun test tests/pdf-thai.test.ts 2>&amp;1 | tail -12</automated>
  </verify>
  <done>pdf-thai.test.ts เขียว; buffer มี %PDF header, ยาวสมเหตุผล; A1 spike ผ่าน (หรือบันทึก fallback ถ้าจำเป็น).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| font file → runtime | TTF จากภายนอก ต้อง OFL license ถูกต้อง (A6) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | pdfmake/pdfkit บน Bun (A1) | high | mitigate | PdfPrinter + on-disk TTF; smoke test buffer+%PDF; fallback vfs บันทึก |
| T-03-SC | Tampering | `bun add pdfmake` | high | accept | pdfmake อยู่ใน CLAUDE.md + Package Legitimacy Audit = Approved (no postinstall) — ไม่มี [ASSUMED]/[SUS] |
</threat_model>

<verification>
- pdf-thai.test.ts เขียว (Buffer + %PDF + ไทยจริง).
- typecheck ผ่าน.
</verification>

<success_criteria>
D-21 PDF path พิสูจน์ทำงานบน Bun; 09-packing พึ่งพาได้.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-02-SUMMARY.md` when done
</output>
