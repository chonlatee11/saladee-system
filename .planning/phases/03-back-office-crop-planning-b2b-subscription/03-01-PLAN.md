---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/db/schema.ts
  - api/drizzle/0004_phase3.sql
  - api/drizzle/0004_phase3.down.sql
  - api/src/env.ts
  - api/src/index.ts
  - api/src/routes/crop.ts
  - api/src/routes/harvest.ts
  - api/src/routes/subscriptions.ts
  - api/src/routes/b2b.ts
  - api/src/routes/packing.ts
  - api/src/routes/reports.ts
  - api/src/routes/dashboard.ts
  - api/src/routes/settings.ts
autonomous: true
requirements: [ADM-02, ADM-03, CROP-01, CROP-02, CROP-05, CROP-06, CUST-02, CUST-05, SALE-03, INV-10]
must_haves:
  truths:
    - "migration 0004 apply ขึ้น + rollback ลง สะอาด บน DATABASE_URL_DIRECT"
    - "ตารางใหม่ทั้งหมด (planting_batches, planting_mix_templates(+items), harvest_logs, subscriptions(+skips), subscription_orders, standing_orders(+items), quota_overflow_flags, settings) มีจริงใน schema"
    - "varieties เพิ่ม days-to-harvest/survival%/harvest-window/shelf-life (D-01); round_stock เพิ่ม is_manual_override (D-03); orders เพิ่ม packed_at (D-20); customers เพิ่ม b2b_status/credit-terms (D-08/11)"
    - "index.ts compose stub router ใหม่ 8 ตัวต่อท้าย chain โดยไม่ reorder ของเดิม; App type ยัง export"
    - "bun test เดิม 187 ยังเขียว (schema/migration additive ไม่ทำของเดิมพัง)"
  artifacts:
    - api/src/db/schema.ts
    - api/drizzle/0004_phase3.sql
    - api/drizzle/0004_phase3.down.sql
    - api/src/routes/crop.ts
    - api/src/routes/harvest.ts
    - api/src/routes/subscriptions.ts
    - api/src/routes/b2b.ts
    - api/src/routes/packing.ts
    - api/src/routes/reports.ts
    - api/src/routes/dashboard.ts
    - api/src/routes/settings.ts
  key_links:
    - "forecast/reservation จะเขียน round_stock.quota_plants / reserve() เดิม — schema ต้องไม่แตะ guard"
    - "subscription_orders UNIQUE(subscription_id, round_id) = idempotency ของ pg-boss generator (Pitfall 2)"
    - "index.ts fixed-order invariant: append เท่านั้น ห้าม reorder (Phase-0)"
---

<objective>
Backbone ของ Phase 3 — วางฐาน schema + migration + env + route-stub composition ให้ทุก slice (Wave 2/3) เติมไฟล์ตัวเองแบบ disjoint โดยไม่แตะ index.ts/schema.ts อีก (แบบเดียวกับ Phase-0 fixed-order invariant).

ผู้ใช้ (แอดมิน/ฟาร์ม): หลังแผนนี้ยังไม่เห็น UI ใหม่ — นี่คือฐานที่ทำให้ slice ถัดไปส่งของ end-to-end ได้. เป็น BLOCKING backbone: ทุกแผนอื่น depends_on 03-01.

Purpose: กัน oversell-regression (ไม่แตะ reserve guard), กัน parallel file-conflict, ให้ migration reversible.
Output: ตารางใหม่ ~9 ตัว + column ต่อขยาย, migration 0004 (up+down), env keys ใหม่, stub route 8 ไฟล์ composed ใน index.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-CONTEXT.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-RESEARCH.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-PATTERNS.md
@api/src/db/schema.ts
@api/drizzle/0003_slip_delivery_consent.down.sql
@api/src/index.ts
@api/src/env.ts
</context>

<artifacts>
## Artifacts this plan produces (new symbols)
- Enums: `subscriptionStatusEnum` (active/paused/cancelled), `b2bStatusEnum` (pending/approved/rejected).
- Tables: `plantingBatches`, `plantingMixTemplates`, `plantingMixItems`, `harvestLogs`, `subscriptions`, `subscriptionSkips`, `subscriptionOrders`, `standingOrders`, `standingOrderItems`, `quotaOverflowFlags`, `settings`.
- Columns: `varieties.{daysToHarvest,survivalPct,harvestWindowDays,shelfLifeDays}`, `roundStock.isManualOverride`, `orders.packedAt`, `customers.{b2bStatus,creditTerms,b2bApprovedAt}`.
- Migration: `api/drizzle/0004_phase3.sql` + hand-written `0004_phase3.down.sql`.
- Env keys: `HAIRCUT_DEFAULT_PCT`, `B2B_QUOTA_CEILING_PCT` (+ web-admin origin added to `CORS_ORIGINS` default).
- Stub routers (default instance + `makeXRoutes(db)` factory, empty body): `cropRoutes`, `harvestRoutes`, `subscriptionsRoutes`, `b2bRoutes`, `packingRoutes`, `reportsRoutes`, `dashboardRoutes`, `settingsRoutes` — composed at END of `index.ts`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: ต่อขยาย schema.ts — enums + ตารางใหม่ + column</name>
  <read_first>api/src/db/schema.ts (header 6–12 conventions, varieties 61–75, roundStock 104–118, customers 161–168, orders 209–232, payments partial-unique idiom 294–300)</read_first>
  <files>api/src/db/schema.ts</files>
  <action>
เพิ่มใน schema.ts ตาม convention เดิม (id uuid defaultRandom pk; createdAt timestamp withTimezone defaultNow notNull; snake_case column ↔ camelCase key; money = integer satang; pgEnum ประกาศก่อนตารางที่ใช้).

ประกาศ 2 enum ใหม่: `subscriptionStatusEnum` = ('active','paused','cancelled'); `b2bStatusEnum` = ('pending','approved','rejected').

ต่อขยาย `varieties` (D-01/CROP-01): `daysToHarvest integer notNull`, `survivalPct integer notNull` (0–100 = per-variety haircut D-02), `harvestWindowDays integer notNull default 1`, `shelfLifeDays integer notNull`.

ต่อขยาย `roundStock`: `isManualOverride boolean notNull default false` (D-03/Pitfall 5 — publish ข้ามแถวที่ override เอง).

ต่อขยาย `orders`: `packedAt timestamp withTimezone` nullable (D-20 pack state).

ต่อขยาย `customers` (D-08/D-11): `b2bStatus b2bStatusEnum` nullable (null = ลูกค้า B2C ปกติ), `creditTerms text` nullable (บันทึกเงื่อนไข ไม่บังคับวงเงิน — D-11), `b2bApprovedAt timestamp withTimezone` nullable.

ตารางใหม่:
- `plantingBatches` (CROP-02): varietyId uuid → varieties, plantDate timestamp, plantCount integer, bed text nullable.
- `plantingMixTemplates` (CROP-06): name text, active boolean default true; `plantingMixItems`: templateId → template, varietyId → varieties, plantCount integer.
- `harvestLogs` (CROP-05/INV-10, D-05 1 batch=1 lot): batchId uuid → plantingBatches (UNIQUE — 1 batch 1 lot), harvestedAt timestamp, actualPlants integer, actualGrams integer, wasteGrams integer default 0, lotCode text notNull, bestBefore timestamp notNull.
- `subscriptions` (SALE-03): customerId → customers, packageCode text (S/M/L), packageValueSatang integer, frequency text, status subscriptionStatusEnum notNull default 'active'.
- `subscriptionSkips` (D-14): subscriptionId → subscriptions, roundId → rounds, UNIQUE(subscription_id, round_id).
- `subscriptionOrders` (D-13 idempotency): subscriptionId → subscriptions, roundId → rounds, orderId → orders, `uniqueIndex` UNIQUE(subscription_id, round_id) — mirror idiom `payments_trans_ref_idx`.
- `standingOrders` (CUST-05): customerId → customers, active boolean default true; `standingOrderItems`: standingId → standingOrders, varietyId → varieties, plantsPerRound integer.
- `quotaOverflowFlags` (D-10): roundId → rounds, varietyId → varieties, shortfall integer, source text ('b2b'|'subscription'), resolvedAt timestamp nullable.
- `settings` (D-22): key text primaryKey, value jsonb notNull, updatedAt timestamp defaultNow notNull.

ห้ามแตะ `quotaPlants`/`reservedPlants` logic หรือ guard ใด ๆ — เพิ่ม column/ตารางเท่านั้น (additive).
  </action>
  <verify>
    <automated>cd api &amp;&amp; bunx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <done>schema.ts typecheck ผ่าน; enum+ตารางใหม่+column ครบตามรายการ; ไม่มีการแก้ reserve/release/guard.</done>
</task>

<task type="auto">
  <name>Task 2: migration 0004 (up+down) + env keys + stub routers + index compose</name>
  <read_first>api/drizzle/0003_slip_delivery_consent.sql + .down.sql (reversible idiom ทั้ง 17 บรรทัด: drop index → child → column → pgEnum LAST, IF EXISTS), api/src/env.ts (EnvSchema 8–82), api/src/index.ts (compose tail), api/src/routes/varieties.ts (makeXRoutes factory 66/197)</read_first>
  <files>api/drizzle/0004_phase3.sql, api/drizzle/0004_phase3.down.sql, api/src/env.ts, api/src/index.ts, api/src/routes/crop.ts, api/src/routes/harvest.ts, api/src/routes/subscriptions.ts, api/src/routes/b2b.ts, api/src/routes/packing.ts, api/src/routes/reports.ts, api/src/routes/dashboard.ts, api/src/routes/settings.ts</files>
  <action>
สร้าง migration ผ่าน `drizzle-kit generate` (ตั้งชื่อ 0004_phase3) จาก schema task 1. ตรวจ SQL ที่ได้ให้ครบตาราง/column ใหม่.

เขียน `0004_phase3.down.sql` ด้วยมือ (drizzle-kit ไม่ออก down) ตาม idiom 0003: drop index/unique → drop child table (items/skips/subscription_orders/flags ก่อน parent) → drop column ที่เพิ่ม (varieties/round_stock/orders/customers) → DROP TYPE enum ใหม่ LAST, ใส่ `IF EXISTS` ทุกบรรทัด. รัน migration บน `DATABASE_URL_DIRECT` (Pitfall 4).

env.ts (EnvSchema): เพิ่ม `HAIRCUT_DEFAULT_PCT` (Integer 0–100, default 90 — fallback เมื่อ variety ไม่ตั้งค่า), `B2B_QUOTA_CEILING_PCT` (Integer 0–100, default 100 — เพดานสัดส่วนโควตาที่ B2B+subscription จองได้ต่อรอบ, D-10). อย่าเพิ่ม secret ใหม่. เพิ่ม origin ของ web-admin เข้า default ของ `CORS_ORIGINS` (Pitfall 6 — ไม่งั้น preflight บล็อกทุก mutation).

สร้าง stub router 8 ไฟล์ ตามแบบ `makeXRoutes(database = defaultDb)` + `export const xRoutes = makeXRoutes()` โดย body เป็น `new Elysia()` เปล่า (ยังไม่มี route) — slice จะเติมเอง. ต่อ `.use(cropRoutes).use(harvestRoutes).use(subscriptionsRoutes).use(b2bRoutes).use(packingRoutes).use(reportsRoutes).use(dashboardRoutes).use(settingsRoutes)` ต่อท้าย chain ของ index.ts (หลัง meOrdersRoutes) — ห้าม reorder ของเดิม; `export type App` ต้องยังอยู่ (Eden).
  </action>
  <verify>
    <automated>cd api &amp;&amp; bun run db:migrate &amp;&amp; bun run db:down 0004_phase3 &amp;&amp; bun run db:migrate &amp;&amp; bunx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <done>migrate up → down → up สะอาด (ไม่มี "cannot drop type ... depends on it"); env boot-validate ผ่าน; index.ts compose 8 stub ต่อท้าย; App type export.</done>
</task>

<task type="auto">
  <name>Task 3: regression gate — suite เดิมยังเขียว</name>
  <read_first>api/tests/ (โครงสร้าง test เดิม), STATE.md (187 passing)</read_first>
  <files>api/drizzle/0004_phase3.sql</files>
  <action>
รัน suite เต็มยืนยันว่า schema/migration additive ไม่ทำ 187 tests เดิมพัง (โดยเฉพาะ reservation/oversell/hold-expiry). ถ้ามี test แตะ round_stock ต้องแก้เฉพาะ fixture ที่กระทบ column ใหม่ (default ครบแล้วจึงไม่ควรกระทบ). ห้ามแก้ logic เดิม.
  </action>
  <verify>
    <automated>cd api &amp;&amp; bun test 2>&amp;1 | tail -15</automated>
  </verify>
  <done>bun test เต็มเขียว (≥187 pass, 0 fail).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration → DB | DDL rollback ต้อง reversible ไม่ทิ้ง orphan type/journal |
| new columns → reserve guard | ต้อง additive ห้ามเปิดช่อง oversell |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | 0004 migration reversibility | high | mitigate | hand-written down (drop children→parent, enum LAST, IF EXISTS); verify up→down→up |
| T-03-02 | Tampering | round_stock guard regression | critical | mitigate | additive only; full `bun test` gate (oversell/hold-expiry) must stay green |
| T-03-03 | Info disclosure | CORS_ORIGINS widening | medium | mitigate | add only exact web-admin origin; secrets stay in env, none added |
</threat_model>

<verification>
- migrate up→down→up สะอาดบน DATABASE_URL_DIRECT.
- `bun test` เต็มเขียว.
- typecheck ผ่าน; index.ts มี 8 stub ต่อท้ายไม่ reorder.
</verification>

<success_criteria>
schema+migration+env+stubs พร้อมให้ Wave 2/3 เติมไฟล์ disjoint; ไม่มี oversell-regression.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-01-SUMMARY.md` when done
</output>
