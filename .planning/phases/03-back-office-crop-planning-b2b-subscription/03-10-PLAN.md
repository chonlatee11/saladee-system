---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 10
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - api/src/routes/dashboard.ts
  - api/tests/dashboard.test.ts
  - web-admin/src/views/Dashboard.vue
  - web-admin/src/composables/useDashboard.ts
autonomous: false
requirements: [ADM-01, ADM-02]
must_haves:
  truths:
    - "แดชบอร์ดสรุป: ยอดขายวันนี้/รอบนี้, ออเดอร์ค้างชำระ, ของใกล้หมดรอบ, ผลผลิตคาดรอบหน้า (ADM-01, D-23 4 การ์ด)"
    - "เพิ่มการ์ด B2B/subscription: standing/subscription ที่ครบกำหนดรอบนี้ + ธง B2B overflow (D-10/D-23)"
    - "aggregate SQL อยู่ฝั่ง server (Drizzle sql+groupBy), staff-gated GET; เงินเป็น integer satang จนถึงจุดแสดง"
    - "route dashboard.ts guard = staff (owner/admin)"
  artifacts:
    - api/src/routes/dashboard.ts
    - web-admin/src/views/Dashboard.vue
  key_links:
    - "อ่าน round_stock.quota_plants (forecast รอบหน้า), orders (unpaid/sales), quota_overflow_flags (03-06) — read-only aggregate"
---

<objective>
## Slice user story
**As a** เจ้าของ/แอดมิน, **I want to** เห็นแดชบอร์ดสรุปยอดขายวันนี้/รอบนี้ ออเดอร์ค้างชำระ ของใกล้หมด ผลผลิตรอบหน้า และสถานะ B2B/subscription ในหน้าเดียว, **so that** ตัดสินใจดำเนินงานรอบนี้ได้ทันที.

Vertical slice: web-admin (Dashboard) → API (dashboard.ts aggregate, staff-gated) → DB (read-only aggregates). ครอบ Success Criterion 5 ส่วน dashboard (ADM-01, D-23).

Purpose: มุมมองสรุปสำหรับเจ้าของฟาร์ม.
Output: dashboard 5 การ์ด end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-RESEARCH.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-PATTERNS.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-UI-SPEC.md
@api/src/routes/prices.ts
@web-admin/src/components/DataTable.vue
</context>

<artifacts>
## Artifacts this plan produces
- `api/src/routes/dashboard.ts` (fill stub) — staff-gated GET /dashboard: sales today/round, unpaid count, near-sold-out, next-round forecast yield, standing+subscription due, overflow flags.
- web-admin: `Dashboard.vue` (5 cards), `composables/useDashboard.ts`.
- Test: `dashboard.test.ts` (aggregate shape + staff guard).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: dashboard.ts aggregate route (staff-gated)</name>
  <files>api/src/routes/dashboard.ts, api/tests/dashboard.test.ts</files>
  <read_first>03-RESEARCH.md §Pattern 8 (aggregate SQL server-side), 03-PATTERNS.md dashboard section (prices.ts /resolve computed GET 129–177), api/src/routes/prices.ts</read_first>
  <behavior>
    - GET /dashboard?roundId คืน { salesTodaySatang, salesRoundSatang, unpaidCount, nearSoldOut:[{variety, remaining}], nextRoundForecast:[{variety, plants}], subsDue, standingDue, overflowFlags:[...] }
    - เงินเป็น integer satang; near-sold-out = quota−reserved ต่ำกว่าเกณฑ์
    - guard staff (owner/admin); grower/packer/customer 403
  </behavior>
  <action>
เติม `routes/dashboard.ts`: `const staff = requireRole("owner","admin")`; GET /dashboard aggregate ด้วย Drizzle `sql`+`groupBy` (ยอดวันนี้/รอบ, unpaid count, near-sold-out จาก round_stock quota−reserved, next-round forecast จาก quota_plants รอบถัดไป, subscription/standing due รอบนี้, quota_overflow_flags ที่ยังไม่ resolved). คืน shaped JSON (mirror /resolve). เงิน integer satang. TypeBox query (roundId optional).
  </action>
  <verify>
    <automated>cd api &amp;&amp; bun test tests/dashboard.test.ts 2>&amp;1 | tail -12</automated>
  </verify>
  <done>dashboard.test เขียว; aggregate shape ครบ 5 หมวด; guard staff.</done>
</task>

<task type="auto">
  <name>Task 2: web-admin Dashboard view (5 cards)</name>
  <read_first>03-UI-SPEC.md §Key Screen (dashboard states: loading skeleton/populated/no-open-round/error) + §Typography (Display KPI 28px) + §Color (status badges) + §Copywriting (empty "ยังไม่มีรอบขายที่เปิดอยู่"), web-admin/src/components/DataTable.vue</read_first>
  <files>web-admin/src/views/Dashboard.vue, web-admin/src/composables/useDashboard.ts</files>
  <action>
`useDashboard.ts`: TanStack Query (dashboard) — loading/error states.

`Dashboard.vue`: 4 การ์ดเกณฑ์ (ยอดวันนี้/รอบนี้ · ค้างชำระ · ใกล้หมดรอบ · ผลผลิตรอบหน้า) + การ์ด B2B/subscription (due รอบนี้ + ธง overflow warning/destructive) (D-23). KPI number = Display 28px/600, tabular numerics, `฿` เท่าขนาดตัวเลข. loading = skeleton cards; no-open-round empty (copy + ปุ่มไปวางแผนปลูก); error. ไทยทั้งหมด.
  </action>
  <verify>
    <automated>cd web-admin &amp;&amp; bunx vue-tsc --noEmit 2>&amp;1 | head -15</automated>
  </verify>
  <done>Dashboard typecheck ผ่าน; 5 การ์ด + states ตาม UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ตรวจ dashboard 5 การ์ด</name>
  <action>ตรวจสอบด้วยมือตามขั้นตอนใน how-to-verify ด้านล่าง</action>
  <what-built>แดชบอร์ด 5 การ์ด (ยอด/ค้างชำระ/ใกล้หมด/ผลผลิตรอบหน้า/B2B-subscription+overflow).</what-built>
  <how-to-verify>
1. login admin → หน้าแรก Dashboard: ตัวเลขตรงกับข้อมูลรอบปัจจุบัน (ยอดขาย, ค้างชำระ)
2. ตรวจการ์ดใกล้หมด + ผลผลิตรอบหน้า (จาก forecast 03-05)
3. ถ้ามี overflow flag (03-06) → เห็นธงเตือน
4. ไม่มีรอบเปิด → empty state
  </how-to-verify>
  <resume-signal>พิมพ์ "approved" หรือบอกปัญหา</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| dashboard API → aggregates | staff-gated read-only |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-26 | Elevation | dashboard endpoint | high | mitigate | requireRole("owner","admin") |
| T-03-27 | Tampering | SQL injection ใน aggregate | high | mitigate | Drizzle parameterized sql (ไม่ string concat) |
</threat_model>

<verification>
- dashboard.test เขียว.
- web-admin typecheck ผ่าน; checkpoint ผ่าน.
</verification>

<success_criteria>
ADM-01 ส่งมอบ; แดชบอร์ด 5 การ์ด end-to-end.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-10-SUMMARY.md` when done
</output>
