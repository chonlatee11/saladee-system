---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 08
type: execute
wave: 3
depends_on: ["03-01", "03-06", "03-07"]
files_modified:
  - web/src/views/SubscriptionSignup.vue
  - web/src/views/SubscriptionManage.vue
  - web/src/views/B2bAccount.vue
  - web/src/router.ts
  - web/src/api.ts
  - web/src/stores/subscription.ts
autonomous: false
requirements: [SALE-03, CUST-02, CUST-05]
must_haves:
  truths:
    - "ลูกค้าใน LIFF สมัครกล่องผัก: เลือกแพ็กเกจ (S/M/L by value) + ความถี่ → ยืนยัน (D-12, SALE-03)"
    - "สมาชิกจัดการ: หยุดชั่วคราว/ข้ามรอบ/ยกเลิก ได้จนถึง cut-off; หลัง cut-off แสดง locked notice ว่ามีผลรอบถัดไป (D-14)"
    - "ลูกค้า B2B: เห็น gate 'รอการอนุมัติ' เมื่อ pending; อนุมัติแล้วเห็นราคาส่ง + ตั้งออเดอร์ประจำได้ (D-08/09)"
    - "แสดงรายละเอียด substitution ในออเดอร์ (คู่กับ Flex push จาก 03-07, D-16)"
  artifacts:
    - web/src/views/SubscriptionSignup.vue
    - web/src/views/SubscriptionManage.vue
    - web/src/views/B2bAccount.vue
    - web/src/stores/subscription.ts
  key_links:
    - "เรียก subscription endpoints (03-07) + b2b endpoints (03-06); reuse Phase-2 LIFF contract/tokens verbatim (ไม่มี token ใหม่)"
    - "self-service pause/skip/cancel ผูก ownership customerId=session + cut-off gate ฝั่ง server (03-07)"
---

<objective>
## Slice user story
**As a** ลูกค้าใน LINE (สมาชิกกล่องผัก / ลูกค้า B2B), **I want to** สมัคร/จัดการกล่องผักและจัดการบัญชี B2B ได้ในหน้า LIFF มือถือ, **so that** สั่งแบบรายรอบและเห็นราคาส่งได้จบใน LINE.

Vertical slice (customer surface): web/ LIFF เพิ่มหน้า subscription signup/manage + B2B account บน contract Phase-2 เดิม (inherit tokens/patterns verbatim ตาม UI-SPEC). เรียก endpoints ที่ 03-06/03-07 สร้างไว้.

Purpose: ปิดฝั่งลูกค้าของ SALE-03/CUST-02/CUST-05 (หลังบ้าน+admin อยู่ 06/07).
Output: LIFF subscription + B2B customer end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-CONTEXT.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-UI-SPEC.md
@.planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md
@web/src/router.ts
@web/src/api.ts
@web/src/views/Checkout.vue
@web/src/stores/cart.ts
</context>

<artifacts>
## Artifacts this plan produces
- web/ views: `SubscriptionSignup.vue` (package S/M/L + frequency), `SubscriptionManage.vue` (pause/skip/cancel + after-cutoff notice), `B2bAccount.vue` (pending gate / wholesale visible / standing order).
- `web/src/stores/subscription.ts` (reactive singleton, mirror cart.ts).
- `web/src/router.ts` (MODIFY — add 3 routes), `web/src/api.ts` (no change unless helper).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: LIFF subscription signup + manage views</name>
  <read_first>03-UI-SPEC.md §web/ LIFF customer additions (subscription sign-up/manage states) + §Copywriting ("สมัครสมาชิกกล่องผัก"/"ยืนยันแพ็กเกจ") + §Subscription self-service confirmations (pause/skip/cancel/after-cutoff), 02-UI-SPEC.md (mobile tokens), web/src/views/Checkout.vue (SFC pattern), web/src/stores/cart.ts</read_first>
  <files>web/src/views/SubscriptionSignup.vue, web/src/views/SubscriptionManage.vue, web/src/stores/subscription.ts, web/src/router.ts</files>
  <action>
`subscription.ts` store: mirror cart.ts (reactive singleton + sessionStorage guard). 

`SubscriptionSignup.vue`: เลือกแพ็กเกจ S/M/L (by value) + ความถี่ → ยืนยัน (primary CTA "สมัครสมาชิกกล่องผัก"/"ยืนยันแพ็กเกจ") → POST subscription (03-07); empty "ยังไม่ได้สมัครกล่องผัก" + active-member view.

`SubscriptionManage.vue`: active view + pause-confirm/skip-confirm/cancel-confirm (copy D-14) + after-cutoff locked notice ("รอบนี้เลยเวลาปรับแล้ว (ปิดรับ {cutoffTime})…"). เรียก endpoints 03-07 (ownership+cut-off server เป็น authority). 

router.ts (MODIFY): เพิ่ม route signup/manage. inherit Phase-2 mobile tokens verbatim (ไม่มี token ใหม่).
  </action>
  <verify>
    <automated>cd web &amp;&amp; bunx vue-tsc --noEmit 2>&amp;1 | head -15</automated>
  </verify>
  <done>2 views + store typecheck ผ่าน; states/copy ตาม UI-SPEC; ผูก subscription endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: LIFF B2B account view (pending gate / wholesale / standing) + substitution detail</name>
  <read_first>03-UI-SPEC.md §web/ (B2B wholesale view states: pending-approval/approved/standing-order-set; substitution notice in-order detail) + §Empty states (B2B pending), web/src/views/Checkout.vue</read_first>
  <files>web/src/views/B2bAccount.vue, web/src/router.ts</files>
  <action>
`B2bAccount.vue`: state pending-approval gate (copy "บัญชี B2B กำลังรอการอนุมัติ…") / approved (แสดงราคาส่ง — เรียก wholesale endpoint 03-06 ที่ gate visibility) / standing-order set (primary CTA "ตั้งออเดอร์ประจำ" → 03-06 standing). เพิ่ม in-order substitution detail line (คู่กับ Flex push 03-07, copy D-16 "รอบนี้มีการปรับผักในกล่องของคุณ…"). router.ts เพิ่ม route. inherit Phase-2 tokens.
  </action>
  <verify>
    <automated>cd web &amp;&amp; bun run build 2>&amp;1 | tail -12</automated>
  </verify>
  <done>B2bAccount view build ผ่าน (vite); pending/approved/standing states; substitution detail แสดง.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ตรวจ LIFF subscription + B2B customer</name>
  <action>ตรวจสอบด้วยมือตามขั้นตอนใน how-to-verify ด้านล่าง</action>
  <what-built>LIFF ลูกค้า: สมัคร/จัดการกล่องผัก (pause/skip/cancel + cut-off lock), บัญชี B2B (pending → อนุมัติ → ราคาส่ง → standing).</what-built>
  <how-to-verify>
1. เปิด LIFF (มือถือ/preview) → "สมัครสมาชิกกล่องผัก": เลือกแพ็กเกจ+ความถี่ → ยืนยัน
2. หน้า "จัดการสมาชิก": pause/skip/cancel ก่อน cut-off ได้; หลัง cut-off เห็น locked notice
3. บัญชี B2B ที่ยัง pending → เห็น gate รออนุมัติ; หลัง admin อนุมัติ (03-06) → เห็นราคาส่ง + ตั้ง standing ได้
  </how-to-verify>
  <resume-signal>พิมพ์ "approved" หรือบอกปัญหา</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LIFF customer → subscription/b2b API | self-service ต้องผูก ownership + cut-off (server authority) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20 | Elevation | customer แก้ subscription/standing คนอื่น | high | mitigate | server ownership check (03-06/07); UI ไม่ใช่ authority |
| T-03-21 | Info disclosure | B2B price ก่อนอนุมัติ | high | mitigate | wholesale endpoint gate บน approved (03-06); pending gate ฝั่ง UI |
| T-03-22 | Business logic | แก้หลัง cut-off | medium | mitigate | cut-off gate server (D-14); locked notice UI |
</threat_model>

<verification>
- web/ typecheck + build ผ่าน.
- checkpoint UI ผ่าน.
</verification>

<success_criteria>
ฝั่งลูกค้าของ SALE-03/CUST-02/CUST-05 ส่งมอบใน LIFF; inherit Phase-2 contract.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-08-SUMMARY.md` when done
</output>
