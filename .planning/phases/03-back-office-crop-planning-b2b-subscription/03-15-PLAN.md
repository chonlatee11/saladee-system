---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 15
type: execute
wave: 1
depends_on: ["03-06"]
files_modified:
  - web-admin/src/views/StandingOrders.vue
autonomous: true
gap_closure: true
requirements: [CUST-05]
must_haves:
  truths:
    - "The standing-orders table basket column shows human-readable variety names (e.g. กรีนโอ๊ค ×20, เรดโอ๊ค ×10) once the varieties query resolves — never a raw UUID prefix left frozen on screen"
    - "The customer column likewise shows the customer name once the customers query resolves"
    - "Name resolution is reactive: rows rendered before the lookup lists finish loading update in place when they arrive"
  artifacts:
    - "web-admin/src/views/StandingOrders.vue — display labels joined into the reactive rows computed, consumed by the table via accessorKey"
  key_links:
    - "TanStack Table memoizes accessor results per row in its value cache — lookups must live INSIDE the data fed to DataTable's data prop so a late-resolving varieties/customers query produces new row objects and invalidates the cache"
---

<objective>
Close UAT gap #3 (minor, test 4): the standing-order basket column renders
variety UUID prefixes ("6fb95842 ×20") instead of names. Root cause: the basket
and customer columns resolve names through column accessorFn closures over the
useVarieties()/useB2bCustomers() query results; TanStack Table caches accessor
values per row, so cells rendered before those queries resolve keep the
id-prefix fallback. Join the display labels into the reactive rows computed so
the table re-renders with names when the lookups arrive (CUST-05 / D-09 staff
surface).

Purpose: staff cannot read standing baskets — the D-09/D-10 admin surface is
unusable for verifying reserved demand.
Output: StandingOrders.vue with reactive name joins; typecheck + build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-UAT.md
@web-admin/src/views/StandingOrders.vue
@web-admin/src/composables/useCrop.ts
</context>

## Artifacts this phase produces

- `web-admin/src/views/StandingOrders.vue` — a display-row type extending StandingRow with `customerLabel: string` and `basketLabel: string`; the `rows` computed maps each standing order to include both labels (recomputing when `varieties`/`customers` query data changes); the "customer" and "basket" columns switch from accessorFn calls to `accessorKey: "customerLabel"` / `accessorKey: "basketLabel"`.

<tasks>

<task type="auto">
  <name>Task 1: Join variety/customer display names into the reactive rows computed</name>
  <files>web-admin/src/views/StandingOrders.vue</files>
  <read_first>
    - web-admin/src/views/StandingOrders.vue (whole file — rows/varietyList/approvedCustomers computeds at lines 53-75; customerName/varietyName/basketSummary helpers at lines 68-73; columns at lines 77-82; the create/edit modal below reuses varietyList)
    - web-admin/src/composables/useCrop.ts lines 34-43 (useVarieties — TanStack Query over the public GET /varieties, returns an array of variety rows with id + name)
    - web-admin/src/components/DataTable.vue (how the data/columns props feed the TanStack table instance)
  </read_first>
  <action>
    Fix the frozen UUID-prefix cells by moving name resolution INTO the row data
    (UAT gap test 4, CUST-05):
    1. Define a DisplayRow interface = StandingRow plus customerLabel: string and
       basketLabel: string.
    2. Change the `rows` computed to return DisplayRow[]: for each standing order
       compute customerLabel via the customers query data (name ?? an 8-char id
       prefix while unresolved) and basketLabel by mapping items through the
       varietyList lookup (name ?? 8-char id prefix), formatted
       "{name} ×{plantsPerRound}" joined with ", ", or "—" when empty. Because
       the computed reads varieties.value and customers.value, a late-resolving
       query produces a NEW array → DataTable's data prop changes → TanStack's
       per-row value cache is rebuilt with real names.
    3. Repoint the columns: id "customer" → accessorKey "customerLabel"; id
       "basket" → accessorKey "basketLabel". Drop the accessorFn closures for
       these two columns. The "total" and "active" columns stay as-is.
    4. Keep (or inline) the varietyName/customerName helpers only if the modal
       or confirm-dialog templates still use them; delete them if now unused so
       no dead lookup path remains.
    5. Add a short comment on the rows computed explaining WHY labels are joined
       into row data (TanStack Table memoizes accessor results per row; lookups
       outside the data prop go stale when queries resolve after first render).
  </action>
  <verify>
    <automated>cd web-admin && bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web-admin && bun run typecheck` exits 0
    - `cd web-admin && bun run build` exits 0
    - `grep -c "basketLabel" web-admin/src/views/StandingOrders.vue` ≥ 2 (rows computed + column accessorKey)
    - `grep -c "customerLabel" web-admin/src/views/StandingOrders.vue` ≥ 2
    - `grep -c "accessorKey: \"basketLabel\"" web-admin/src/views/StandingOrders.vue` == 1
    - Behavior: with the dev seed, the ตะกร้าประจำรอบ column shows variety names ("กรีนโอ๊ค ×20") after data loads — re-checked in the phase UAT walk
  </acceptance_criteria>
  <done>Standing-order rows carry precomputed customerLabel/basketLabel that recompute when the varieties/customers queries resolve; the table never freezes UUID prefixes on screen; web-admin typecheck and build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser → staff API reads | display-only refactor; no new inputs cross any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15-01 | Information Disclosure | StandingOrders.vue rendering | low | accept | client-side label join over data the staff session already receives; no new endpoint, no new data exposure |
</threat_model>

<verification>
- `cd web-admin && bun run typecheck && bun run build` — green.
- UAT re-walk of test 4's minor item: standing basket shows names, not id prefixes.
</verification>

<success_criteria>
- The standing-orders table renders variety and customer names reactively; UUID prefixes appear only as a transient fallback while lookups load, never frozen.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-15-SUMMARY.md` when done.
</output>
