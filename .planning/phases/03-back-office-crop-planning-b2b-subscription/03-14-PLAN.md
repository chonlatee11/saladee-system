---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 14
type: execute
wave: 1
depends_on: ["03-08"]
files_modified:
  - api/scripts/provision-rich-menu.ts
  - web/src/views/CatalogView.vue
  - web/src/views/SubscriptionSignup.vue
  - web/tests/catalog-view.test.ts
  - web/tests/subscription-view.test.ts
autonomous: true
gap_closure: true
requirements: [SALE-03, CUST-02]
user_setup:
  - service: LINE Official Account
    why: "Re-provisioning the Rich Menu against LINE requires channel credentials and a new 6-cell menu image — Claude updates the script but cannot run it"
    env_vars:
      - name: LINE_CHANNEL_ACCESS_TOKEN
        source: "LINE Developers Console → Messaging API → Channel access token (already configured for prior provisioning)"
      - name: LIFF_ID
        source: "LINE Developers Console → LIFF → the existing LIFF app id"
    dashboard_config:
      - task: "Produce a NEW 6-cell 2500×1686 rich-menu image (top: สั่งผักรอบนี้ / ราคาผักรอบนี้ / ติดตามออเดอร์; bottom: สมาชิกกล่องผัก / ติดต่อร้าน / ความรู้เรื่องผัก) then run: cd api && LIFF_ID=<id> bun run scripts/provision-rich-menu.ts ./rich-menu.png"
        location: "operator machine (script re-provisions idempotently)"
must_haves:
  truths:
    - "A LINE customer can reach the subscription sign-up (SALE-03) from a Rich Menu button that deep-links {liff}/subscription"
    - "A LINE customer can reach /subscription AND /b2b (CUST-02) from visible links inside the LIFF catalog screen — no hand-built deep link required"
    - "On the subscription signup, the currently-selected package is obvious: accent ring + check badge, matching the delivery-method tile pattern"
    - "The selected frequency tile carries the same strengthened treatment"
  artifacts:
    - "api/scripts/provision-rich-menu.ts — 6-area menu including a สมาชิกกล่องผัก → /subscription button"
    - "web/src/views/CatalogView.vue — member quick-links row (subscription + B2B)"
    - "web/src/views/SubscriptionSignup.vue — ring-2 + check-badge selected state on package and frequency tiles"
  key_links:
    - "Rich Menu deep-link paths must match web/src/router.ts registered routes (/subscription, /contact, /care) exactly"
    - "Selected-state treatment replicates web/src/components/DeliveryMethodTiles.vue — the approved 02-UI-SPEC accent ring + check pattern"
---

<objective>
Close UAT gap #1 (major, test 6) and gap #2 (minor): the LIFF /subscription and
/b2b views shipped in 03-08 have NO entry point (Rich Menu deep-links only 5
unrelated routes; no in-app nav), and the subscription signup's selected-package
state is too subtle to read on mobile. Add discoverable entry points (Rich Menu
button + in-LIFF links) and strengthen the selected-state visual per the approved
02-UI-SPEC accent pattern (D-12 package pick surface).

Purpose: SALE-03 subscription sign-up and CUST-02 B2B account are unreachable for
real customers; the discoverability gap blocks phase UAT.
Output: 6-button Rich Menu provisioning script, member links on the catalog
screen, an unmistakable selected-package/frequency state, updated view tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-UAT.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-UI-SPEC.md
@.planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md
@api/scripts/provision-rich-menu.ts
@web/src/router.ts
</context>

## Artifacts this phase produces

- `api/scripts/provision-rich-menu.ts` — `request.areas` grows from 5 to 6 (bottom row becomes 3 cells); new bottom-left button สมาชิกกล่องผัก → `${liffBase}/subscription`; updated header docblock (6-button menu + new-image note).
- `web/src/views/CatalogView.vue` — new member quick-links row: two `RouterLink`s (`/subscription` — สมาชิกกล่องผัก, `/b2b` — ลูกค้าขายส่ง (B2B)) rendered under the h1 in every screen state.
- `web/src/views/SubscriptionSignup.vue` — selected package tile: `border-accent ring-2 ring-accent bg-accent/5` + a ✓ check badge (`bg-accent text-white`); selected frequency tile: same ring treatment.
- `web/tests/catalog-view.test.ts` — assertions that the rendered catalog contains links to `/subscription` and `/b2b`.
- `web/tests/subscription-view.test.ts` — assertion that the selected package tile carries the ring class and check badge.

<tasks>

<task type="auto">
  <name>Task 1: Extend the Rich Menu provisioning script to 6 areas with a subscription button</name>
  <files>api/scripts/provision-rich-menu.ts</files>
  <read_first>
    - api/scripts/provision-rich-menu.ts (whole file — the 5-area request object at lines 60-89, idempotency + image pipeline below it)
    - web/src/router.ts (registered paths — /subscription is the sign-up route; /subscription/manage and /b2b also exist)
  </read_first>
  <action>
    Restructure request.areas from 5 to 6 cells (grid stays 2500×1686, two rows of
    843px height):
    - Top row UNCHANGED: สั่งผักรอบนี้ → liffBase (x0 w833), ราคาผักรอบนี้ →
      `${liffBase}/prices` (x833 w834), ติดตามออเดอร์ → `${liffBase}/orders`
      (x1667 w833).
    - Bottom row becomes THREE cells at y843 h843: สมาชิกกล่องผัก →
      `${liffBase}/subscription` (x0 w833), ติดต่อร้าน → `${liffBase}/contact`
      (x833 w834), ความรู้เรื่องผัก → `${liffBase}/care` (x1667 w833).
    /b2b intentionally gets no Rich Menu cell (niche audience) — Task 2 gives it
    an in-app link; note this in a comment near the areas array. Update the
    header docblock: the menu is now the 6-button "saladee-main" (3-top +
    3-bottom grid), and the operator must supply a NEW 6-cell 2500×1686 image
    when re-running. Keep MENU_NAME, the idempotent delete-by-name loop, the
    image resize/compress pipeline, and setDefaultRichMenu untouched. Keep each
    label as a comment beside its area, mirroring the existing style.
  </action>
  <verify>
    <automated>cd api && bun build scripts/provision-rich-menu.ts --target=bun --outdir=/tmp/rich-menu-check && grep -v "^\s*//" scripts/provision-rich-menu.ts | grep -c "bounds:"</automated>
  </verify>
  <acceptance_criteria>
    - `cd api && bun build scripts/provision-rich-menu.ts --target=bun --outdir=/tmp/rich-menu-check` exits 0 (script still compiles)
    - `grep -v "^\s*//" api/scripts/provision-rich-menu.ts | grep -c "bounds:"` outputs exactly 6
    - `grep -c "liffBase}/subscription" api/scripts/provision-rich-menu.ts` ≥ 1
    - `grep -c "liffBase}/contact" api/scripts/provision-rich-menu.ts` ≥ 1 and `grep -c "liffBase}/care" api/scripts/provision-rich-menu.ts` ≥ 1 (existing buttons survive)
    - All six bounds tile the 2500×1686 canvas with no overlap (x offsets 0/833/1667 per row, widths 833/834/833, rows y=0 and y=843, height 843)
  </acceptance_criteria>
  <done>The provisioning script defines a 6-button menu whose new bottom-left cell deep-links {liff}/subscription; re-running it (operator step, see user_setup) publishes the new menu idempotently.</done>
</task>

<task type="auto">
  <name>Task 2: Add in-LIFF member quick-links (subscription + B2B) to the catalog screen</name>
  <files>web/src/views/CatalogView.vue, web/tests/catalog-view.test.ts</files>
  <read_first>
    - web/src/views/CatalogView.vue (whole file — RouterLink is already imported; template branches errored/empty/list under the h1)
    - web/tests/catalog-view.test.ts (SSR view-test pattern: mount with the injectable loader prop, assert rendered HTML)
    - .planning/phases/02-line-storefront-payments-delivery/02-UI-SPEC.md accent-reservation rules (secondary links use neutral ink + border, NOT accent; 44px minimum hit area)
  </read_first>
  <action>
    In CatalogView.vue add a member quick-links row directly under the h1
    heading and BEFORE the errored/empty/list branches, so it renders in every
    screen state (the catalog at "/" is the LIFF root + Rich Menu default entry
    + the router's wildcard fallback — links here are always reachable). The row
    contains two RouterLinks side by side (flex gap-sm):
    - to="/subscription", label สมาชิกกล่องผัก
    - to="/b2b", label ลูกค้าขายส่ง (B2B)
    Style per 02-UI-SPEC: neutral treatment (rounded-lg border border-border
    bg-white text-ink, text-[14px] or text-[15px], min-h-[44px] via padding,
    centered) — do NOT use the accent color (accent is reserved for the single
    primary action per screen; the sticky checkout CTA owns it here). Add a brief
    script comment citing UAT gap test 6 / SALE-03 / CUST-02 discoverability.
    Extend web/tests/catalog-view.test.ts: with the existing mocked-loader mount,
    assert the rendered HTML contains href="/subscription" and href="/b2b" (both
    in the has-items state and in the empty state if the test already covers it).
  </action>
  <verify>
    <automated>cd web && bun test tests/catalog-view.test.ts && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && bun test tests/catalog-view.test.ts` exits 0 with new assertions for href="/subscription" and href="/b2b"
    - `grep -c "/subscription" web/src/views/CatalogView.vue` ≥ 1 and `grep -c "/b2b" web/src/views/CatalogView.vue` ≥ 1
    - `cd web && bun run build` exits 0
    - The links row is OUTSIDE the v-if/v-else-if chain (renders in errored, empty, and has-items states)
  </acceptance_criteria>
  <done>Opening the LIFF root shows two tappable links to /subscription and /b2b regardless of catalog state; view test pins them.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen the selected package/frequency state on subscription signup</name>
  <files>web/src/views/SubscriptionSignup.vue, web/tests/subscription-view.test.ts</files>
  <read_first>
    - web/src/views/SubscriptionSignup.vue (package tiles ~lines 165-187, frequency tiles ~lines 189-207 — current selected class is only border-accent bg-accent/5)
    - web/src/components/DeliveryMethodTiles.vue (the APPROVED selected pattern to replicate: selected tile gets border-accent ring-2 ring-accent; a check badge span with border-accent bg-accent text-white containing the ✓ glyph; aria-pressed)
    - web/tests/subscription-view.test.ts (existing mount/assert pattern)
  </read_first>
  <action>
    Replicate the DeliveryMethodTiles selected treatment (02-UI-SPEC reserved
    accent use #2: "active/selected state ring + check on a chosen … pack
    option") on both tile groups, per UAT gap test 6 (user: cannot tell which
    package is selected):
    - Package tiles: selected class becomes border-accent ring-2 ring-accent
      bg-accent/5 (unselected stays border-border bg-white). Inside each tile's
      header row, render a check badge when selected — a small rounded-full span
      (~20px, border border-accent bg-accent text-white) containing ✓, next to
      the package label; unselected tiles render the same-size span with
      border-border bg-white and no glyph (layout does not shift on selection),
      exactly as DeliveryMethodTiles does. Keep aria-pressed bindings.
    - Frequency tiles: selected class becomes border-accent ring-2 ring-accent
      bg-accent/5 text-ink font-semibold (unselected stays border-border
      bg-white text-muted). No check badge needed on the narrow frequency pills
      if it crowds them — the ring + weight change is the required minimum.
    Extend web/tests/subscription-view.test.ts: after selecting a package (or
    mounting with a preselected store state, following the file's existing
    pattern), assert the selected package button's class list contains
    ring-accent and its HTML contains the ✓ glyph, and that aria-pressed="true"
    is on exactly one package tile.
  </action>
  <verify>
    <automated>cd web && bun test tests/subscription-view.test.ts && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && bun test tests/subscription-view.test.ts` exits 0 including the new selected-state assertions (ring-accent class + ✓ glyph + single aria-pressed="true")
    - `grep -c "ring-accent" web/src/views/SubscriptionSignup.vue` ≥ 2 (package + frequency tiles)
    - `grep -c "✓" web/src/views/SubscriptionSignup.vue` ≥ 1
    - `cd web && bun run build` exits 0
    - Full web suite still green: `cd web && bun test` exits 0
  </acceptance_criteria>
  <done>The chosen package tile shows an accent ring + filled ✓ badge and the chosen frequency shows an accent ring — visually unmistakable on mobile, consistent with the delivery-method tiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LINE Rich Menu → LIFF deep links | menu content is operator-published; script reads channel token from validated env only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14-01 | Information Disclosure | provision-rich-menu.ts | medium | mitigate | channel access token stays env-only, never a literal, never logged (existing T-02-07 handling preserved — task changes areas only) |
| T-03-14-02 | Elevation of Privilege | /b2b link visible to all LIFF users | low | accept | the link only navigates; B2bAccount.vue + /me/b2b/* are session-gated server-side (03-06/03-08) — a non-B2B user just sees the apply flow |
</threat_model>

<verification>
- `cd web && bun test && bun run build` — all view tests + production build green.
- `cd api && bun build scripts/provision-rich-menu.ts --target=bun --outdir=/tmp/rich-menu-check` — script compiles.
- Live Rich Menu re-provisioning is an operator step (user_setup) verified during UAT re-run.
</verification>

<success_criteria>
- Rich Menu definition has 6 areas including {liff}/subscription; script compiles and remains idempotent.
- Catalog screen surfaces links to /subscription and /b2b in every state.
- Selected package/frequency is visually obvious (ring + check) and test-pinned.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-14-SUMMARY.md` when done.
</output>
