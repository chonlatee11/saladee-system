---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 13
type: execute
wave: 1
depends_on: ["03-06"]
files_modified:
  - api/src/routes/catalog.ts
  - api/tests/catalog.test.ts
  - .planning/phases/03-back-office-crop-planning-b2b-subscription/deferred-items.md
autonomous: true
gap_closure: true
requirements: [CUST-02]
must_haves:
  truths:
    - "An unauthenticated GET /catalog response contains NO wholesale price: every variety round entry has prices.b2b === null and every box round entry has priceSatang.b2b === null"
    - "GET /catalog/rounds/:id applies the same gate — anonymous callers get b2b null"
    - "A customer session whose b2b_status is 'approved' still receives the b2b tier in the catalog response (Authorization: Bearer <session>)"
    - "A pending/rejected/plain-B2C customer session gets b2b null, same as anonymous"
    - "The approved-B2B LIFF flow is unbroken: GET /me/b2b/prices (b2b.ts) is untouched and still serves the gated wholesale price"
  artifacts:
    - "api/src/routes/catalog.ts — session-aware b2b tier gate wired through both GET endpoints"
    - "api/tests/catalog.test.ts — anonymous/approved/pending gate cases"
  key_links:
    - "catalog.ts reuses wholesaleVisible() from api/src/services/b2b.ts (the 03-06 / D-08 / T-03-21 gate) — one visibility rule, not a second copy"
    - "session resolution mirrors the .derive bearer+verifySession pattern already proven in api/src/routes/b2b.ts"
---

<objective>
Close UAT gap #4 (USER DECISION, locked): the public catalog currently leaks the
wholesale (b2b) tier price to every caller. Gate/omit the b2b tier for callers
without approved B2B status, reusing the existing wholesaleVisible() rule from
03-06 (D-08 / T-03-21), per CUST-02. Do not break the approved-B2B flow
(/me/b2b/prices) or the anonymous LIFF B2C catalog.

Purpose: wholesale-price / PDPA exposure flagged in 03-UAT.md "Follow-up decision"
and deferred-items.md — user decided to gate it.
Output: gated catalog endpoints + regression tests + deferred-items.md marked resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-UAT.md
@api/src/routes/catalog.ts
@api/src/routes/b2b.ts
@api/src/services/b2b.ts
</context>

## Artifacts this phase produces

- `api/src/routes/catalog.ts` — new local `bearer(headers)` helper (mirrors the private one in `b2b.ts`); a `.derive` that resolves `session: Session | null` per request; a per-request `showB2b` boolean threaded into `roundEntry(...)` and the box `priceSatang` mapping in BOTH `GET /catalog` and `GET /catalog/rounds/:id`; when `showB2b` is false, `prices.b2b` and `priceSatang.b2b` are `null` (keys stay present — response shape unchanged, both fields are already nullable).
- `api/tests/catalog.test.ts` — updated existing b2b-price assertions (anonymous now expects null) + new cases: approved customer session sees b2b, pending customer session does not.
- `.planning/phases/03-back-office-crop-planning-b2b-subscription/deferred-items.md` — catalog b2b-exposure item marked resolved by this plan.

<tasks>

<task type="auto">
  <name>Task 1: Gate the b2b tier in the public catalog behind wholesaleVisible()</name>
  <files>api/src/routes/catalog.ts, api/tests/catalog.test.ts</files>
  <read_first>
    - api/src/routes/catalog.ts (whole file — roundEntry() builds prices at lines ~109-112; boxes build priceSatang at line ~269; /catalog/rounds/:id reuses roundEntry at ~345)
    - api/src/routes/b2b.ts lines 25-35 (private bearer() helper) and lines 105-118 (the .derive session-resolution pattern: bearer(headers) → verifySession(token) → session | null, try/catch swallows invalid tokens)
    - api/src/services/b2b.ts lines 82-94 (wholesaleVisible(db, customerId) — returns true only for b2bStatus === "approved")
    - api/src/plugins/auth.plugin.ts (Session type, verifySession, issueSession — issueSession(userId, role) is the test-side token mint, see api/tests/b2b-approval.test.ts lines 8-35 for the fire() pattern)
    - api/tests/catalog.test.ts (existing DI-factory test harness: makeCatalogRoutes(db) + routes.handle(new Request(...)); seeds via ./seed helpers)
  </read_first>
  <action>
    Implement the b2b visibility gate per the locked user decision (03-UAT.md
    "Follow-up decision") reusing the D-08 / T-03-21 rule:

    1. In api/src/routes/catalog.ts import { type Session, verifySession } from
       "../plugins/auth.plugin" and { wholesaleVisible } from "../services/b2b".
       Add a module-local bearer(headers) helper identical in behavior to the one
       in b2b.ts (reads headers.authorization ?? headers.Authorization, strips the
       "Bearer " prefix).
    2. Inside makeCatalogRoutes(database), add a .derive on the returned Elysia
       instance that resolves session: Session | null exactly like b2b.ts lines
       110-118 (missing token → null; verifySession throw → null). The catalog
       stays OPEN (D-03) — the session is optional context, never a 401 gate.
    3. In each of the two GET handlers compute once per request:
       showB2b = session !== null && session.role === "customer" &&
       await wholesaleVisible(database, session.sub). Staff tokens and anonymous
       callers get showB2b = false (staff read wholesale via the staff /prices
       routes, not the public catalog).
    4. Thread showB2b into the price shaping: give roundEntry(...) an extra
       showB2b parameter; when false the b2b key of the returned prices object is
       null instead of tierPricePayload(resolveTierPrice(relevant, "b2b", today),
       units). Same for the box mapping: when false, priceSatang.b2b is null
       instead of priceForTier("b2b"). Keys must remain present (both are already
       nullable in the response shape) so Eden Treaty consumers do not break.
    5. Update the file-header comment block (lines ~10-12, "resolved b2c/b2b
       tiered prices") to state the b2b tier is gated to approved-B2B customer
       sessions (D-08 / T-03-21); anonymous and non-approved callers receive null.
    6. Update api/tests/catalog.test.ts: existing assertions that read the b2b
       tier from an anonymous response now expect null. Add gate cases using the
       DI harness: insert a customers row with b2bStatus "approved" (and one with
       "pending"), mint tokens via issueSession(customerId, "customer"), send
       Authorization: Bearer <token> on the request — approved sees the resolved
       b2b payload (pricePerKgSatang KG_B2B), pending and anonymous see null.
       Also assert /catalog/rounds/:id applies the same gate for at least the
       anonymous + approved pair. Do NOT touch api/tests/b2b-approval.test.ts —
       /me/b2b/prices behavior is out of scope and must keep passing.
  </action>
  <verify>
    <automated>cd api && bun test tests/catalog.test.ts tests/b2b-approval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd api && bun test tests/catalog.test.ts` exits 0 and includes at least 3 new/updated gate assertions (anonymous → b2b null, approved session → b2b non-null, pending session → b2b null)
    - `cd api && bun test tests/b2b-approval.test.ts` exits 0 unchanged (approved-B2B /me flow unbroken)
    - `grep -c "wholesaleVisible" api/src/routes/catalog.ts` ≥ 2 (import + at least one call)
    - `grep -c "verifySession" api/src/routes/catalog.ts` ≥ 2 (import + derive)
    - Response-shape guard: `grep -c "b2b:" api/src/routes/catalog.ts` ≥ 2 — the b2b keys still exist (nulled, not removed)
    - Full suite still green: `cd api && bun test` exits 0
  </acceptance_criteria>
  <done>Anonymous and non-approved callers of GET /catalog and GET /catalog/rounds/:id receive prices.b2b === null and priceSatang.b2b === null; an approved-B2B customer bearer session receives the b2b tier; /me/b2b/prices untouched; api test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the catalog b2b-exposure deferred item resolved</name>
  <files>.planning/phases/03-back-office-crop-planning-b2b-subscription/deferred-items.md</files>
  <read_first>
    - .planning/phases/03-back-office-crop-planning-b2b-subscription/deferred-items.md (the "catalog GET / exposes b2b tier price unconditionally" bullet)
  </read_first>
  <action>
    Edit the first bullet of the "03-08 deferred" section: annotate it as RESOLVED
    by plan 03-13 (gap closure, 2026 date of execution) — state that the public
    catalog now nulls the b2b tier for callers without an approved-B2B customer
    session via wholesaleVisible(), per the user decision recorded in 03-UAT.md.
    Keep the original text (strike-through or a "Resolved:" sub-line) so history
    stays readable. Leave the vue-tsc bullet untouched.
  </action>
  <verify>
    <automated>grep -c "03-13" .planning/phases/03-back-office-crop-planning-b2b-subscription/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "03-13" .planning/phases/03-back-office-crop-planning-b2b-subscription/deferred-items.md` ≥ 1
    - The vue-tsc devDep bullet is still present verbatim
  </acceptance_criteria>
  <done>deferred-items.md shows the catalog b2b exposure as resolved by 03-13; the unrelated vue-tsc item remains open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous internet → GET /catalog | untrusted, unauthenticated reads of the public catalog |
| LIFF customer session → GET /catalog | optional bearer token; must be verified before it widens visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13-01 | Information Disclosure | catalog.ts GET /catalog + /catalog/rounds/:id | high | mitigate | b2b tier nulled unless wholesaleVisible(session.sub) — the same single rule as /me/b2b/prices (T-03-21) |
| T-03-13-02 | Spoofing | catalog.ts .derive session resolution | medium | mitigate | jose verifySession(); any invalid/forged token resolves to session=null → b2b stays hidden (fail-closed) |
| T-03-13-03 | Denial of Service | extra wholesaleVisible() DB read per catalog request | low | accept | single indexed PK lookup, only executed when a customer bearer token is present; anonymous hot path unchanged |
</threat_model>

<verification>
- `cd api && bun test` — full suite green (catalog gate cases included).
- Manual spot-check (optional): `curl -s localhost:3000/catalog | grep -o '"b2b":null' | head` shows nulls with no Authorization header.
</verification>

<success_criteria>
- Wholesale tier is invisible to anonymous/non-approved catalog callers on both catalog endpoints.
- Approved-B2B customers keep wholesale visibility (catalog + /me/b2b/prices).
- No response-shape break: b2b keys present as null; LIFF web build unaffected (web/ never read catalog b2b).
- deferred-items.md updated.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-13-SUMMARY.md` when done.
</output>
