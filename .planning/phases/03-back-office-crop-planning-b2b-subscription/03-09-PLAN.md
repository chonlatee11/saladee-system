---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 09
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - api/src/routes/packing.ts
  - api/src/pdf/pack-slip.ts
  - api/src/pdf/label-slip.ts
  - api/tests/packing-queue.test.ts
  - web-admin/src/views/Packing.vue
  - web-admin/src/composables/usePacking.ts
autonomous: false
requirements: [ORD-03, ADM-02]
must_haves:
  truths:
    - "พนักงานแพ็กเห็นคิวงานจัดกลุ่มตามรอบส่ง → แล้วตามรูปแบบ/โซน (route) จาก orders.deliveryMethod/deliveryZone snapshot เดิม (ORD-03, D-20)"
    - "ทำเครื่องหมาย 'แพ็คแล้ว' ต่อออเดอร์ (orders.packed_at)"
    - "พิมพ์ใบแพ็ค/ป้ายส่งเป็น PDF ภาษาไทย (Sarabun) ผ่าน pdfmake (D-21) — endpoint คืน application/pdf"
    - "route packing.ts guard = packer (owner/admin/packer); grower/customer 403"
  artifacts:
    - api/src/routes/packing.ts
    - api/src/pdf/pack-slip.ts (docDefinition builder)
    - api/src/pdf/label-slip.ts (docDefinition builder)
    - web-admin/src/views/Packing.vue
  key_links:
    - "import renderPackSlip/renderLabelSlip จาก 03-02 (Wave-0 spike ต้องผ่านก่อน)"
    - "queue = single indexed query order by (round_id, delivery_zone) — ไม่มี join ใหม่ (D-20)"
---

<objective>
## Slice user story
**As a** พนักงานแพ็ก (packer), **I want to** เห็นคิวงานแพ็กจัดกลุ่มตามรอบส่งและเส้นทาง แล้วพิมพ์ใบแพ็ก/ป้ายส่งเป็น PDF และทำเครื่องหมายว่าแพ็คแล้ว, **so that** แพ็กของตามเส้นทางได้เร็วและถูกต้อง.

Vertical slice: web-admin (Packing) → API (packing.ts, packer-gated + PDF endpoints) → DB (orders.packed_at + delivery snapshot เดิม). ครอบ Success Criterion 4 (ORD-03, D-20/21). ต้องพึ่ง PDF spike 03-02.

Purpose: ปิดงานแพ็ก + พิสูจน์ PDF ไทยใช้จริง.
Output: pack queue + PDF pack/label + mark-packed end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-RESEARCH.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-PATTERNS.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-UI-SPEC.md
@api/src/routes/stock.ts
@api/src/pdf/pack-slip.ts
@api/src/services/notify.ts
@web-admin/src/components/DataTable.vue
</context>

<artifacts>
## Artifacts this plan produces
- `api/src/routes/packing.ts` (fill stub) — packer-gated: GET queue (grouped), PATCH mark-packed, GET pack-slip.pdf, GET label-slip.pdf.
- `api/src/pdf/pack-slip.ts` (EXTEND — add `buildPackSlipDoc(orders)` docDefinition), `label-slip.ts` (`buildLabelSlipDoc(order)`).
- web-admin: `Packing.vue`, `composables/usePacking.ts`.
- Test: `packing-queue.test.ts` (grouping by route + mark-packed + pdf 200/application-pdf).
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: packing.ts route (queue + mark-packed + PDF endpoints) + docDefinition builders</name>
  <files>api/src/routes/packing.ts, api/src/pdf/pack-slip.ts, api/src/pdf/label-slip.ts, api/tests/packing-queue.test.ts</files>
  <read_first>03-RESEARCH.md §Pattern 5 (queue grouping) + Pattern 6 (PdfPrinter), 03-PATTERNS.md packing/pdf sections, api/src/routes/stock.ts (staff GET), api/src/pdf/pack-slip.ts (renderPackSlip จาก 03-02), api/src/services/notify.ts (baht helper)</read_first>
  <behavior>
    - GET /packing/queue?roundId คืน orders (paid) จัดกลุ่ม round → deliveryZone/method, order by (round_id, delivery_zone)
    - PATCH /packing/:orderId/packed ตั้ง orders.packed_at
    - GET /packing/pack-slip.pdf?roundId คืน 200 + Content-Type application/pdf + Buffer (%PDF)
    - GET /packing/label-slip.pdf?orderId คืน 200 + application/pdf
    - guard packer/admin/owner; grower/customer 403
  </behavior>
  <action>
เติม `routes/packing.ts`: `const packer = requireRole("owner","admin","packer")` (D-19). GET queue = single indexed query บน orders.deliveryMethod/deliveryZone snapshot เดิม order by (round_id, delivery_zone) — ไม่มี join ใหม่ (D-20). PATCH mark-packed ตั้ง packed_at. PDF endpoints เรียก `buildPackSlipDoc`/`buildLabelSlipDoc` → `renderPackSlip`/`renderLabelSlip` (03-02) แล้ว set `Content-Type: application/pdf`, คืน Buffer.

EXTEND pdf/pack-slip.ts + label-slip.ts: เพิ่ม `buildPackSlipDoc(orders)` (order lines จัดกลุ่มตาม route, mono grid, ไม่พึ่งสีสื่อความหมาย — print-safe ตาม UI-SPEC) และ `buildLabelSlipDoc(order)` (customer/address/round block). เงิน = baht(satang) helper. คง renderX จาก 03-02.
  </action>
  <verify>
    <automated>cd api &amp;&amp; bun test tests/packing-queue.test.ts 2>&amp;1 | tail -12</automated>
  </verify>
  <done>packing-queue.test เขียว; queue grouping ถูก; mark-packed; PDF endpoints คืน application/pdf + %PDF; guard packer.</done>
</task>

<task type="auto">
  <name>Task 2: web-admin Packing queue view</name>
  <read_first>03-UI-SPEC.md §Key Screen (packing queue states: grouped by round→route / to-pack/packed / print / nothing-to-pack) + §Copywriting ("ทำเครื่องหมายว่าแพ็คแล้ว"/"พิมพ์ใบแพ็ค / ป้ายส่ง (PDF)") + §Layout (PDF pack/label), web-admin/src/components/DataTable.vue</read_first>
  <files>web-admin/src/views/Packing.vue, web-admin/src/composables/usePacking.ts</files>
  <action>
`usePacking.ts`: TanStack Query (queue by round) + mutation mark-packed invalidate.

`Packing.vue`: คิวจัดกลุ่มตามรอบ → route (section header); DataTable ต่อ group แสดง per-order pack state (to-pack/packed badge) + ปุ่ม "ทำเครื่องหมายว่าแพ็คแล้ว"; ปุ่ม "พิมพ์ใบแพ็ค / ป้ายส่ง (PDF)" เปิด/ดาวน์โหลด PDF จาก endpoint; empty "ยังไม่มีรายการที่ต้องแพ็ค"; error. ไทยทั้งหมด; accent = 1 primary/หน้า.
  </action>
  <verify>
    <automated>cd web-admin &amp;&amp; bunx vue-tsc --noEmit 2>&amp;1 | head -15</automated>
  </verify>
  <done>view typecheck ผ่าน; grouping/states/copy ตาม UI-SPEC; ปุ่มพิมพ์ผูก PDF endpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ตรวจ packing queue + PDF ไทย</name>
  <action>ตรวจสอบด้วยมือตามขั้นตอนใน how-to-verify ด้านล่าง</action>
  <what-built>คิวแพ็กจัดกลุ่มตามเส้นทาง + พิมพ์ PDF ไทย (Sarabun) + ทำเครื่องหมายแพ็คแล้ว.</what-built>
  <how-to-verify>
1. login packer/admin → "คิวแพ็ค": เห็นออเดอร์ paid จัดกลุ่มตามรอบ→โซน/วิธีส่ง
2. กด "พิมพ์ใบแพ็ค / ป้ายส่ง (PDF)" → เปิด PDF ภาษาไทยอ่านออก (Sarabun ไม่ tofu), print-safe
3. กด "ทำเครื่องหมายว่าแพ็คแล้ว" → สถานะเปลี่ยน
  </how-to-verify>
  <resume-signal>พิมพ์ "approved" หรือบอกปัญหา</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| packing API → orders | packer-gated; PDF มีข้อมูลลูกค้า (PDPA) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-23 | Elevation | packing endpoints | high | mitigate | requireRole("owner","admin","packer"); grower/customer 403 |
| T-03-24 | Info disclosure | PDF ที่อยู่ลูกค้า (PDPA) | high | mitigate | endpoint packer-gated; ไม่ cache สาธารณะ; ไม่ log ข้อมูลลูกค้า |
| T-03-25 | Tampering | pdfmake Thai render | high | mitigate | พึ่ง 03-02 spike ที่ผ่านแล้ว; test %PDF |
</threat_model>

<verification>
- packing-queue.test เขียว.
- web-admin typecheck ผ่าน; checkpoint PDF ผ่าน.
</verification>

<success_criteria>
ORD-03 ส่งมอบ; queue by route + PDF ไทย + mark-packed.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-09-SUMMARY.md` when done
</output>
