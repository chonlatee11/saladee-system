---
phase: 03-back-office-crop-planning-b2b-subscription
plan: 11
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - api/src/routes/reports.ts
  - api/tests/reports.test.ts
  - web-admin/src/views/Reports.vue
  - web-admin/src/components/ReportChart.vue
  - web-admin/src/composables/useReports.ts
autonomous: false
requirements: [MKT-04, ADM-02]
must_haves:
  truths:
    - "รายงานยอดขายกรองตามช่วงเวลา/ช่องทาง/สินค้า/รอบ + สินค้าขายดี + ลูกค้าซื้อซ้ำ + AOV (MKT-04, D-24 full analytics)"
    - "กราฟ interactive (Chart.js) + ใช้ channel↔color mapping คงที่ (B2C เขียว/B2B ฟ้า/Subscription เหลือง) ตาม UI-SPEC"
    - "ส่งออก CSV ผ่าน papaparse (escape ไทย/quote/newline ถูกต้อง — ไม่ hand-roll join) (D-24)"
    - "aggregate SQL ฝั่ง server, staff-gated GET; เงิน integer satang จนถึงจุดแสดง"
  artifacts:
    - api/src/routes/reports.ts
    - web-admin/src/views/Reports.vue
    - web-admin/src/components/ReportChart.vue
  key_links:
    - "chart.js/vue-chartjs/papaparse ติดตั้งแล้วใน 03-03 scaffold — plan นี้ import เท่านั้น"
    - "channel↔color mapping ต้องตรง UI-SPEC chart palette ทุกกราฟ"
---

<objective>
## Slice user story
**As a** เจ้าของ/แอดมิน, **I want to** ดูรายงานยอดขายแบบ analytics (กราฟ+ตัวกรองตามช่วง/ช่องทาง/สินค้า/รอบ, ขายดี, ซื้อซ้ำ, AOV) และส่งออก CSV, **so that** วิเคราะห์ธุรกิจและวางแผนได้.

Vertical slice: web-admin (Reports + charts) → API (reports.ts aggregates, staff-gated) → DB (read-only aggregates). ครอบ Success Criterion 5 ส่วน reports (MKT-04, D-24 — เจ้าของเลือก full analytics โดยตั้งใจ ไม่ใช่ table-only).

Purpose: analytics เต็มรูปแบบ + CSV export ที่ปลอดภัยกับภาษาไทย.
Output: reports charts + filters + CSV end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-CONTEXT.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-RESEARCH.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-PATTERNS.md
@.planning/phases/03-back-office-crop-planning-b2b-subscription/03-UI-SPEC.md
@api/src/routes/prices.ts
@web-admin/src/components/DataTable.vue
</context>

<artifacts>
## Artifacts this plan produces
- `api/src/routes/reports.ts` (fill stub) — staff-gated GET /reports: by period/channel/product/round, best-sellers, repeat-customers, AOV.
- web-admin: `Reports.vue` (filter strip + charts + CSV), `components/ReportChart.vue` (vue-chartjs wrapper, fixed palette), `composables/useReports.ts`.
- Test: `reports.test.ts` (aggregate shape + filters + guard).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: reports.ts aggregate route (staff-gated, filters)</name>
  <files>api/src/routes/reports.ts, api/tests/reports.test.ts</files>
  <read_first>03-RESEARCH.md §Pattern 8 (aggregate + papaparse note), 03-PATTERNS.md reports section (prices.ts /resolve), api/src/routes/prices.ts</read_first>
  <behavior>
    - GET /reports?from&to&channel&product&round คืน { series:[{key,label,valueSatang,count}], bestSellers:[...], repeatCustomers:number, aovSatang:number }
    - filter ตาม period/channel(b2c/b2b/subscription)/product/round
    - เงิน integer satang; guard staff (owner/admin); grower/packer/customer 403
  </behavior>
  <action>
เติม `routes/reports.ts`: `const staff = requireRole("owner","admin")`; GET /reports aggregate ด้วย Drizzle `sql`+`groupBy` (by period/channel/product/round), best-sellers (top by qty), repeat-customers (customer ที่มี >1 order), AOV (Σvalue/Σorders). TypeBox query filters. คืน shaped JSON; เงิน satang. รองรับ export shape (rows) สำหรับ CSV (client papaparse; ถ้าใหญ่มาก stream ฝั่ง server — MVP client พอ, A5).
  </action>
  <verify>
    <automated>cd api &amp;&amp; bun test tests/reports.test.ts 2>&amp;1 | tail -12</automated>
  </verify>
  <done>reports.test เขียว; aggregate+filters+bestsellers+AOV+repeat; guard staff.</done>
</task>

<task type="auto">
  <name>Task 2: web-admin Reports view + ReportChart + CSV export</name>
  <read_first>03-UI-SPEC.md §Key Screen (reports states) + §Color (chart categorical palette — B2C เขียว/B2B ฟ้า/Subscription เหลือง คงที่) + §Layout (charts + filter strip + CSV) + §Copywriting ("ส่งออก CSV", empty "ไม่มีข้อมูลในช่วงที่เลือก"), 03-RESEARCH.md Don't Hand-Roll (papaparse)</read_first>
  <files>web-admin/src/views/Reports.vue, web-admin/src/components/ReportChart.vue, web-admin/src/composables/useReports.ts</files>
  <action>
`useReports.ts`: TanStack Query (reports ตาม filters) — loading/error.

`ReportChart.vue`: wrapper บน vue-chartjs (Chart.js 4) — bar/line/pie; ใช้ channel↔color mapping คงที่จาก UI-SPEC (B2C `#2E7D32`, B2B `#1565C0`, Subscription `#B26A00`, …); cap 6 series → "อื่น ๆ" grey. chart title 20px/600, axis+legend 14px.

`Reports.vue`: filter strip (period/channel/product/round) + charts (best-sellers, repeat customers, AOV, by channel/product/round) + primary CTA "ส่งออก CSV" (papaparse `unparse(rows)` — escape ไทย/quote/newline; **ห้าม** `rows.map(r=>r.join(','))`) + no-data empty + loading + error. ไทยทั้งหมด.
  </action>
  <verify>
    <automated>cd web-admin &amp;&amp; bun run build 2>&amp;1 | tail -12</automated>
  </verify>
  <done>Reports build (vite) ผ่าน; charts palette คงที่; CSV ผ่าน papaparse; states ตาม UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ตรวจ reports charts + CSV</name>
  <action>ตรวจสอบด้วยมือตามขั้นตอนใน how-to-verify ด้านล่าง</action>
  <what-built>รายงาน analytics: กราฟ + ตัวกรอง + CSV export (ภาษาไทยถูกต้อง).</what-built>
  <how-to-verify>
1. login admin → "รายงาน": เลือกช่วงเวลา/ช่องทาง → กราฟอัปเดต; สี channel คงที่ (B2C เขียว/B2B ฟ้า/Subscription เหลือง)
2. ดู best-sellers, AOV, repeat customers
3. กด "ส่งออก CSV" → เปิดไฟล์ ตรวจว่าคอลัมน์ไทย/คอมมาในข้อมูลไม่เพี้ยน
4. ช่วงไม่มีข้อมูล → empty state
  </how-to-verify>
  <resume-signal>พิมพ์ "approved" หรือบอกปัญหา</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reports API → aggregates | staff-gated read-only |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-28 | Elevation | reports endpoint | high | mitigate | requireRole("owner","admin") |
| T-03-29 | Tampering | SQL injection ใน filter | high | mitigate | Drizzle parameterized sql; TypeBox validate filters |
| T-03-30 | Tampering | CSV injection/mangle ไทย | medium | mitigate | papaparse unparse (RFC-4180 escape); ไม่ hand-roll |
</threat_model>

<verification>
- reports.test เขียว.
- web-admin build ผ่าน; checkpoint ผ่าน.
</verification>

<success_criteria>
MKT-04 ส่งมอบ; full analytics + CSV.
</success_criteria>

<output>
Create `.planning/phases/03-back-office-crop-planning-b2b-subscription/03-11-SUMMARY.md` when done
</output>
