---
phase: 04-web-store-marketing-scale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/index.ts
  - api/src/routes/coupons.ts
  - api/src/routes/loyalty.ts
  - api/src/routes/broadcasts.ts
  - api/src/routes/tracking.ts
  - api/src/routes/product-images.ts
  - web-admin/src/router.ts
  - web-admin/src/views/CouponComposer.vue
  - web-admin/src/views/LoyaltySettings.vue
  - web-admin/src/views/BroadcastComposer.vue
  - web-admin/src/views/CarrierTrackingForm.vue
  - web-admin/src/views/ProductImages.vue
autonomous: true
requirements: [MKT-01, CUST-03, MKT-03, LINE-04, DEL-05]
must_haves:
  truths:
    - "The API boots with all five new stub routers composed in index.ts (health/ready still 200)."
    - "web-admin builds; the five new nav routes render their stub views behind existing role gates."
  artifacts:
    - "Stub routers: coupons.ts, loyalty.ts, broadcasts.ts, tracking.ts, product-images.ts — each an Elysia group, composed once in index.ts after settingsRoutes."
    - "web-admin router.ts frozen entries + stub views: /coupons, /loyalty, /broadcasts, /tracking, /product-images."
  key_links:
    - "index.ts composition order is fixed here so Wave-2 slices fill only their own module, never editing index.ts (00-01/03-01 seam idiom)."
    - "web-admin router.ts is frozen here so Wave-2 slices only replace their stub view SFC, never editing router.ts."
  prohibitions:
    - "MUST NOT implement feature logic here — stubs only (routes return 501/empty, views render an EmptyState placeholder)."
    - "MUST NOT edit any Phase-3 view or router entry other than adding the five new routes."
---

<objective>
Freeze the Phase-4 composition surface so all Wave-2 slices execute as disjoint-file parallel work: compose five new stub routers in index.ts (fixed order), and register five new web-admin routes + stub view SFCs in the frozen router.ts. This mirrors the proven 00-01/03-01 "fixed composition, frozen router" decision that lets parallel slices fill only their own module.

Purpose: Prevent index.ts / router.ts merge conflicts between the five parallel Wave-2 slices.
Output: five stub routers composed; five admin routes + stub views registered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-web-store-marketing-scale/04-PATTERNS.md
@.planning/phases/04-web-store-marketing-scale/04-UI-SPEC.md
@api/src/index.ts
@web-admin/src/router.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose five stub routers in index.ts</name>
  <files>api/src/index.ts, api/src/routes/coupons.ts, api/src/routes/loyalty.ts, api/src/routes/broadcasts.ts, api/src/routes/tracking.ts, api/src/routes/product-images.ts</files>
  <read_first>
    - api/src/index.ts (lines 43-81 the .use() composition chain) — append order after settingsRoutes.
    - api/src/routes/settings.ts (top-of-file Elysia group export shape) — the export idiom every stub router copies.
    - api/src/routes/crop.ts (requireRole usage) — the RBAC plugin import for the real slices later.
  </read_first>
  <action>
    Create five stub routers, each exporting an Elysia group named to match (couponsRoutes, loyaltyRoutes, broadcastsRoutes, trackingRoutes, productImagesRoutes) with a single placeholder route that returns HTTP 501 (not implemented) so the seam is composed but inert. Each stub carries a header comment noting which Wave-2 plan fills it (coupons→03, loyalty→03, broadcasts→06, tracking→05, product-images→04). Compose all five in index.ts .use(...) chain AFTER settingsRoutes, preserving the fixed order. Do not add feature logic.
  </action>
  <verify>
    <automated>cd api && bun test tests/health.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - App boots; health.test.ts green (composition does not break boot).
    - `grep -c "Routes)" api/src/index.ts` increases by 5 vs before (five new .use lines).
    - Each stub file exports its named Elysia group.
  </acceptance_criteria>
  <done>Five stub routers composed in fixed order; API boots green.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze five web-admin routes + stub views</name>
  <files>web-admin/src/router.ts, web-admin/src/views/CouponComposer.vue, web-admin/src/views/LoyaltySettings.vue, web-admin/src/views/BroadcastComposer.vue, web-admin/src/views/CarrierTrackingForm.vue, web-admin/src/views/ProductImages.vue</files>
  <read_first>
    - web-admin/src/router.ts (lines 34-175 route array + meta idiom) — copy the RouteRecordRaw + AdminRouteMeta shape; note the file header rule (frozen after this plan).
    - web-admin/src/components/AppShell.vue (how meta.title/icon/group build the sidebar) — pick valid lucide icon names + Thai group labels.
    - web-admin/src/views/Settings.vue (a simple existing view) — the SFC scaffold + EmptyState usage for the stubs.
  </read_first>
  <action>
    In router.ts add five lazy-imported routes with meta (roles, Thai title, lucide icon, group), matching UI-SPEC copy and RBAC (per Security V4 requireRole owner|admin):
    /coupons → CouponComposer.vue, title "คูปอง", group "การตลาด", roles OWNER_ADMIN;
    /loyalty → LoyaltySettings.vue, title "สะสมแต้ม", group "การตลาด", roles OWNER_ADMIN;
    /broadcasts → BroadcastComposer.vue, title "บรอดแคสต์", group "การตลาด", roles OWNER_ADMIN;
    /tracking → CarrierTrackingForm.vue, title "ติดตามพัสดุ", group "จัดส่ง", roles OWNER_ADMIN;
    /product-images → ProductImages.vue, title "รูปสินค้า", group "สินค้า", roles OWNER_ADMIN.
    Add them before the catch-all redirect. Create each view as a stub SFC rendering the UI-SPEC empty-state heading for that surface (e.g. CouponComposer → "ยังไม่มีคูปอง"). Update the router.ts header comment to state the frozen list now includes the five Phase-4 routes. Do not implement forms/tables here.
  </action>
  <verify>
    <automated>cd web-admin && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web-admin && bun run build` succeeds (typecheck + build) with five new lazy routes.
    - router.ts contains exactly five new RouteRecordRaw entries; no Phase-3 entry changed.
    - Each stub view is a valid SFC importable by its route.
  </acceptance_criteria>
  <done>Five admin routes + stub views frozen in router.ts; web-admin builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin nav → admin endpoints | Nav visibility is cosmetic; server requireRole is the authority — stubs must not imply access without it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | new admin routes | high | mitigate | Routes carry meta.roles=OWNER_ADMIN; real endpoints (Wave 2) gate with requireRole server-side (nav gate is cosmetic only) |
| T-04-05 | Tampering | index.ts composition | low | accept | Stub routers return 501; no feature surface exposed until Wave-2 fills them |
</threat_model>

<verification>
- API boots (health green) with five stub routers composed.
- web-admin builds with five frozen routes + stub views.
</verification>

<success_criteria>
Composition surface frozen: five stub routers in index.ts, five admin routes + stub views in router.ts; both apps build/boot; Wave-2 slices can now fill their own module without touching index.ts or router.ts.
</success_criteria>

<artifacts_produced>
- Stub routers: coupons.ts, loyalty.ts, broadcasts.ts, tracking.ts, product-images.ts (composed in index.ts)
- web-admin routes: /coupons, /loyalty, /broadcasts, /tracking, /product-images
- Stub views: CouponComposer.vue, LoyaltySettings.vue, BroadcastComposer.vue, CarrierTrackingForm.vue, ProductImages.vue
</artifacts_produced>

<output>
Create `.planning/phases/04-web-store-marketing-scale/04-02-SUMMARY.md` when done.
</output>
