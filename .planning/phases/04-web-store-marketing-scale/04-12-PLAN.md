---
phase: 04-web-store-marketing-scale
plan: 12
type: execute
wave: 2
depends_on: ["04-11"]
files_modified:
  - api/src/services/broadcast.ts
  - api/tests/broadcast-flex.test.ts
  - web-admin/src/views/BroadcastComposer.vue
autonomous: false
gap_closure: true
requirements: [MKT-03, LINE-04]
must_haves:
  truths:
    - "An admin composes a marketing broadcast as a styled LINE Flex card (hero image, headline, body, CTA button) and consented recipients receive the Flex card, not plain text."
    - "Every Flex broadcast carries a non-empty altText (LINE requirement) so it renders in notification/preview surfaces."
    - "Plain-text broadcasts still work unchanged as a fallback; the consent-scoped audience + ≤500 chunking (PDPA/NFR-04) are untouched."
  artifacts:
    - "api/src/services/broadcast.ts: buildBroadcastFlex(fields) canonical bubble + normalizeMessages hardened to guarantee altText on a stored Flex message."
    - "web-admin/src/views/BroadcastComposer.vue: message-type toggle capturing Flex fields and storing the Flex message as messageJson."
  key_links:
    - "composer Flex fields → messageJson (Flex message object) → POST /broadcasts → runBroadcast → normalizeMessages → multicast (Flex)."
    - "Flex hero image reuses product image URLs surfaced by the 04-11 catalog coverUrl work."
  prohibitions:
    - "MUST NOT alter resolveAudience / consent filtering / chunking — Flex is a message-shape change only (PDPA invariants intact)."
    - "MUST NOT send a Flex message without altText — LINE rejects it; normalizeMessages injects a fallback altText if missing."
---

<objective>
Close UAT Gap 2 (test 3, minor enhancement — "ส่งได้ แต่อยากให้สวยๆแบบ flex message"). The broadcast currently sends a plain {type:"text"} message; the user wants a styled LINE Flex Message. Add a canonical marketing Flex bubble (hero image, headline, body, CTA button linking to store/promo), let the web-admin composer capture those Flex fields and store them as the Flex messageJson, and guarantee the api multicasts the Flex with a required altText while keeping the plain-text path as a fallback. The consent-scoped audience and ≤500 chunking (Pitfall 3/7, PDPA/NFR-04) are unchanged — this is a message-shape enhancement only.

Purpose: Marketing broadcasts look like a real campaign card in LINE, raising engagement without touching PDPA-safe delivery.
Output: Flex builder + altText-hardened normalize + tests; composer Flex fields; UAT re-check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-store-marketing-scale/04-UAT.md

# The gap artifacts + the existing Flex idiom to mirror
@api/src/services/broadcast.ts
@web-admin/src/views/BroadcastComposer.vue
@api/src/services/notify.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flex bubble builder + altText-hardened normalizeMessages</name>
  <files>api/src/services/broadcast.ts, api/tests/broadcast-flex.test.ts</files>
  <behavior>
    - buildBroadcastFlex({heroImageUrl, headline, body, ctaLabel, ctaUrl}) returns a messagingApi.FlexMessage with type "flex", a non-empty altText derived from headline, a bubble hero image (heroImageUrl), headline + body text in the body, and a footer uri button (ctaLabel → ctaUrl).
    - buildBroadcastFlex omits the hero when heroImageUrl is empty (body-only bubble still valid) and omits the CTA button when ctaUrl is empty.
    - normalizeMessages(flexMessageObject) returns it as a single-element array and guarantees a non-empty altText (injects a fallback altText when the stored Flex object has none/empty).
    - normalizeMessages(plainTextObject) and normalizeMessages(undefined) still return the existing plain-text behavior (fallback preserved).
  </behavior>
  <action>
    In api/src/services/broadcast.ts, add an exported `buildBroadcastFlex(fields)` that builds the canonical marketing bubble, mirroring the Flex idiom in notify.ts buildOrderFlex (same messagingApi.FlexMessage typing, `as const` layout literals): a `bubble` with an optional `hero` image (aspectMode cover) when heroImageUrl is set, a vertical `body` box containing a bold headline text (wrap) + a smaller body text (wrap), and a `footer` with a primary uri button (label ctaLabel, uri ctaUrl) when ctaUrl is set. Set `altText` to a non-empty string derived from headline (e.g. the headline, trimmed, capped to LINE's altText length; fall back to a default marketing string when headline is empty).

    Harden `normalizeMessages(messageJson)` so that when the stored object (or array element) is a Flex message (type === "flex"), it guarantees a non-empty `altText`: if altText is missing/empty, inject a default marketing altText before returning. Keep the existing branches: array passes through (after the altText guard per element), a plain object passes through, and undefined/other returns the existing default text message. Do not change resolveAudience, chunk, or runBroadcast's delivery logic.

    Add api/tests/broadcast-flex.test.ts (a pure unit test — no DB needed, unlike broadcast-audience.test.ts) asserting the buildBroadcastFlex shape (type flex, non-empty altText, hero present/absent per input, footer button present/absent per input) and the normalizeMessages altText guarantee for a Flex object with and without altText, plus the preserved plain-text fallback for undefined input.
  </action>
  <verify>
    <automated>cd api && bun test tests/broadcast-flex.test.ts</automated>
  </verify>
  <done>buildBroadcastFlex produces a valid Flex bubble with guaranteed altText; normalizeMessages passes Flex through with altText and preserves the plain-text fallback; new test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Composer captures Flex fields and stores the Flex messageJson</name>
  <files>web-admin/src/views/BroadcastComposer.vue</files>
  <action>
    Add a message-type toggle to the composer form: "ข้อความธรรมดา" (plain text) | "การ์ด Flex" (Flex). Keep the existing plain-text fields/behavior for the plain path (messagePayload() unchanged — the fallback).

    In Flex mode, capture the fields: hero image URL (heroImageUrl), headline (พาดหัว), body text (ข้อความ), CTA label (ปุ่ม), CTA link (ลิงก์ปุ่ม, reuse/rename the existing promoLink). Build the SAME Flex message JSON structure that buildBroadcastFlex produces on the api side (type "flex", altText derived from headline, bubble hero/body/footer) and set that object as `messageJson` in the saveDraft body. The api already accepts messageJson (t.Unknown) and normalizeMessages guarantees altText, so no contract change is needed.

    Suggest a product-image hero: since 04-11 surfaces catalog coverUrl, the heroImageUrl field placeholder/help text should hint the admin can paste a product photo URL (public R2 asset). Do not fetch/validate images here — a free-form https URL field is enough for the gap fix. Update `validate()` so Flex mode requires headline + body (as plain mode requires message). Keep the single accent CTA "ส่งบรอดแคสต์" and the consent-scoped audience preview flow unchanged.
  </action>
  <verify>
    <automated>cd web-admin && grep -v '^//' src/views/BroadcastComposer.vue | grep -c 'heroImageUrl' && bun run type-check 2>&1 | tail -5 || npx vue-tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>The composer offers plain-text and Flex modes; Flex mode captures hero/headline/body/CTA and posts a Flex messageJson; plain-text path unchanged; typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Flex broadcast renders on device (UAT test 3 re-check)</name>
  <action>Manual verification per how-to-verify below; compose + send a Flex broadcast and confirm the styled card arrives on a consented test LINE account, the plain-text fallback still delivers, and an opted-out account receives neither.</action>
  <what-built>The composer can build a Flex marketing card and the api multicasts it (Flex with altText) to the consent-scoped audience; plain text still works as a fallback.</what-built>
  <how-to-verify>
    1. In web-admin, open บรอดแคสต์, choose the "การ์ด Flex" mode.
    2. Fill hero image URL (a public product photo URL — e.g. a catalog coverUrl from 04-11), headline, body, CTA label + link (store/promo).
    3. Save draft / preview — confirm the consent-scoped audience count still shows (PDPA copy unchanged) and is > 0 for your consented test account.
    4. Send. On the real test LINE account (marketing-consented, LINE-linked), confirm a styled Flex card arrives with the hero image, headline, body, and a tappable CTA button — NOT plain text.
    5. Send a plain-text broadcast too and confirm the fallback still delivers as text.
    6. Confirm an opted-out account receives neither (consent invariant intact).
  </how-to-verify>
  <resume-signal>Type "approved" if the Flex card renders on device and plain-text fallback still works, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→API (/broadcasts) | owner|admin authors messageJson (Flex) — trusted staff, requireRole gated |
| API→LINE (multicast) | Flex message + altText sent to consented recipients only (resolveAudience) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12-01 | Tampering | composer heroImageUrl / ctaUrl | low | mitigate | admin-only (requireRole owner|admin); LINE requires https uri actions; free-form URL authored by trusted staff, stored as messageJson snapshot |
| T-04-12-02 | Spoofing/PDPA | audience targeting | high | mitigate | resolveAudience (latest marketing consent + non-null line_user_id) is UNCHANGED — a Flex broadcast cannot reach a non-consented customer any more than a text one |
| T-04-12-03 | Denial of Service | malformed Flex → LINE 400 | medium | mitigate | normalizeMessages guarantees non-empty altText; buildBroadcastFlex omits hero/CTA when fields empty so no invalid empty-uri/empty-url node is emitted |
</threat_model>

<verification>
- `cd api && bun test tests/broadcast-flex.test.ts` passes (builder + altText guarantee + text fallback).
- `cd api && bun test tests/broadcast-audience.test.ts` still passes (consent + chunking untouched).
- web-admin typecheck passes.
- Manual UAT re-check of test 3 enhancement: Flex card renders on device; plain-text fallback works; opt-out excluded.
</verification>

<success_criteria>
Admin composes a Flex marketing card; consented recipients receive a styled Flex message (with altText); plain-text broadcasts still work; PDPA consent-scoped audience + chunking unchanged. UAT test 3 enhancement satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-web-store-marketing-scale/04-12-SUMMARY.md` when done.
</output>
