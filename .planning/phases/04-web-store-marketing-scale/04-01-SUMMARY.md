---
phase: 04-web-store-marketing-scale
plan: 01
subsystem: database
tags: [drizzle, postgres, migration, schema, coupons, loyalty, broadcasts, delivery-tracking, settings, env]

# Dependency graph
requires:
  - phase: 03-crop-planning-subscriptions
    provides: "0004_phase3 schema + settings HOT_KEYS allow-list + self-resetting test harness idiom"
provides:
  - "Phase-4 additive migration 0005_phase4 (up + hand-written down)"
  - "coupons, coupon_redemptions, loyalty_ledger, broadcasts, customer_tags, variety_images, box_images tables"
  - "delivery_status pgEnum + orders.discount_satang/carrier/tracking_number/delivery_status/tracking_updated_at columns"
  - "CARRIER_PROVIDER env key (default manual)"
  - "settings HOT_KEYS: loyaltyEarnRate, loyaltyPointBaht, pdpaPolicyVersion, pdpaPolicytext + defaults"
  - "0005 registered in migrate.test.ts + all 37 self-resetting tests (regression gate honest)"
affects: [04-02, 04-03, 04-04, 04-05, 04-06, 04-07, 04-08, 04-09, 04-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive migration idiom (0004 shape): CREATE TYPE -> CREATE TABLE children -> ALTER ADD COLUMN DEFAULT -> FKs -> indexes; down drops children first, enum types last"
    - "Wave-0 blocking backbone: one migration for the whole phase, registered in every schema-resetting test before feature waves"

key-files:
  created:
    - api/drizzle/0005_phase4.sql
    - api/drizzle/0005_phase4.down.sql
    - api/drizzle/meta/0005_snapshot.json
  modified:
    - api/src/db/schema.ts
    - api/src/env.ts
    - api/src/services/settings.ts
    - api/tests/migrate.test.ts
    - "api/tests/*.test.ts (37 self-resetting harnesses)"

key-decisions:
  - "0005_phase4.sql generated by drizzle-kit generate (schema.ts is source of truth) so db:generate shows no pending diff; down hand-written"
  - "loyalty balance = SUM of append-only loyalty_ledger rows (no mutable balance column), mirroring consent_logs"
  - "per-customer coupon cap = UNIQUE(coupon_id, customer_id); once-per-order earn = partial UNIQUE(order_id) WHERE kind='earn'"
  - "loyalty/PDPA defaults hardcoded in defaultHotSettings (loyaltyEarnRate 1/100baht, loyaltyPointBaht 1, pdpaPolicyVersion 1.0); route PUT schema unchanged (loyalty admin wiring deferred to a later plan)"

patterns-established:
  - "Guarded-UPDATE-ready coupon schema: global_used bumped by conditional UPDATE like reserve() (NFR-02 idiom reused for MKT-01)"
  - "Product-image gallery child tables keep varieties.imageUrl/boxes.imageUrl as the cover (backward compatible, D-28)"

requirements-completed: [MKT-01, CUST-03, MKT-03, LINE-04, DEL-05]

coverage:
  - id: D1
    description: "Migration 0005_phase4 applies (up), rolls back (down), and re-applies (up) cleanly with no residue"
    requirement: "MKT-01"
    verification:
      - kind: integration
        ref: "tests/migrate.test.ts#migration up->down->up (Criterion 2)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Phase-4 tables/enum/columns present in schema and DB; round_stock reservation columns untouched"
    requirement: "DEL-05"
    verification:
      - kind: integration
        ref: "tests/migrate.test.ts#up: identity + all commerce tables and enums exist"
        status: pass
      - kind: other
        ref: "grep -i round_stock drizzle/0005_phase4.sql (no match)"
        status: pass
    human_judgment: false
  - id: D3
    description: "settings HOT_KEYS extended with loyalty + PDPA keys; secret-shaped key still rejected (422); GET stays secret-free"
    requirement: "CUST-03"
    verification:
      - kind: integration
        ref: "tests/settings.test.ts#a secret-shaped key in the body is refused by the schema (422)"
        status: pass
      - kind: integration
        ref: "tests/settings.test.ts#NO secret value or secret-named key ever appears in the response"
        status: pass
    human_judgment: false
  - id: D4
    description: "0005 registered in all 37 self-resetting tests; full suite green (regression gate honest)"
    requirement: "MKT-03"
    verification:
      - kind: integration
        ref: "cd api && bun test (304 pass / 0 fail)"
        status: pass
    human_judgment: false

# Metrics
duration: ~20min
completed: 2026-07-17
status: complete
---

# Phase 4 Plan 01: Wave-0 Schema Backbone Summary

**Additive migration 0005_phase4 adds coupons/loyalty/broadcasts/tags/product-image tables, a delivery_status enum + orders tracking/discount columns, the CARRIER_PROVIDER env key and loyalty/PDPA settings keys — registered across migrate.test.ts + all 37 self-resetting tests so the 304-test regression gate stays honest.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-07-17
- **Tasks:** 2
- **Files modified:** 44 (schema, 2 migration SQL, meta snapshot+journal, env, settings, migrate.test + 37 tests)

## Accomplishments
- One additive migration (0005_phase4) covering every Phase-4 table/column/enum, generated from schema.ts (drizzle-kit) with a hand-written reversible down
- delivery_status pgEnum declared before the orders ALTER; orders gains discount_satang (net-payable), carrier, tracking_number, delivery_status, tracking_updated_at
- CARRIER_PROVIDER env (default "manual") + loyalty economics/PDPA policy keys in the settings HOT_KEYS allow-list with sensible defaults
- 0005 registered in migrate.test.ts (up->down->up proof) and all 37 self-resetting test harnesses; full suite 304 pass / 0 fail
- round_stock reservation columns untouched (NFR-02 oversell guard intact); no secret-shaped key in HOT_KEYS

## Task Commits

Each task was committed atomically:

1. **Task 1: Schema + migration 0005 + env + settings keys** - `c083917` (feat)
2. **Task 2: Register 0005 in every self-resetting test + full suite green** - `e736afc` (test)

## Files Created/Modified
- `api/drizzle/0005_phase4.sql` - drizzle-generated additive migration (7 tables, delivery_status enum, 5 orders columns, FKs, indexes)
- `api/drizzle/0005_phase4.down.sql` - hand-written reverse (indexes, children, coupons parent, orders columns, delivery_status type last)
- `api/drizzle/meta/0005_snapshot.json` + `_journal.json` - drizzle-kit generate artifacts (keeps db:generate diff-clean)
- `api/src/db/schema.ts` - deliveryStatus enum, 7 new tables, 5 new orders columns, `index` import
- `api/src/env.ts` - CARRIER_PROVIDER (default "manual")
- `api/src/services/settings.ts` - loyalty + PDPA keys in HOT_KEYS, HotSettings interface, defaults, asString getter
- `api/tests/migrate.test.ts` - PHASE4 up/down constants + 0005 up/down/up assertions
- `api/tests/*.test.ts` (37 files) - drop 0005 before 0004 (teardown), apply 0005 after 0004 (setup)

## Decisions Made
- Generated 0005_phase4.sql via `drizzle-kit generate --name phase4` (schema.ts = source of truth), verified `db:generate` reports no pending diff; hand-wrote the down.
- Loyalty balance modeled as append-only ledger SUM; per-customer coupon cap + once-per-order earn enforced as DB unique/partial-unique indexes (reuse of the NFR-02 in-DB-property idiom).
- Loyalty/PDPA defaults hardcoded in `defaultHotSettings`; the route PUT schema was left unchanged (loyalty/PDPA admin write-path is a later Phase-4 plan), keeping the secret-rejection boundary intact.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None. NOTICE lines during test teardown are IF-EXISTS clean-slate skips (harmless, matches the 0004 idiom).

## User Setup Required
None - no external service configuration required. (CARRIER_PROVIDER defaults to "manual"; no new secret needed.)

## Next Phase Readiness
- Wave-0 backbone in place — feature waves (coupons, loyalty, broadcasts, tracking, product-images, crop-recommend, web-store) can now build against live tables/columns/enum.
- Reservation guard untouched; regression gate green at 304 tests.

---
*Phase: 04-web-store-marketing-scale*
*Completed: 2026-07-17*

## Self-Check: PASSED
- All created files exist (0005_phase4.sql, 0005_phase4.down.sql, meta/0005_snapshot.json, 04-01-SUMMARY.md).
- All task commits present (c083917, e736afc).
