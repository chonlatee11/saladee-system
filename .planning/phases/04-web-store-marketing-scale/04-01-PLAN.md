---
phase: 04-web-store-marketing-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/db/schema.ts
  - api/src/env.ts
  - api/src/services/settings.ts
  - api/drizzle/0005_phase4.sql
  - api/drizzle/0005_phase4.down.sql
  - api/tests/migrate.test.ts
autonomous: true
requirements: [MKT-01, CUST-03, MKT-03, LINE-04, DEL-05]
must_haves:
  truths:
    - "`cd api && bun test` is fully green with migration 0005 applied (no false-positive from schema-only types)."
    - "0005 applies (up), rolls back (down), and re-applies (up) cleanly in migrate.test.ts."
    - "Every self-resetting test drops 0005 before 0004 on teardown and applies 0005 after 0004 on setup."
  artifacts:
    - "api/drizzle/0005_phase4.sql + 0005_phase4.down.sql (additive; children/enum drop order in down)."
    - "schema.ts: coupons, couponRedemptions, loyaltyLedger, broadcasts, customerTags, varietyImages, boxImages tables; deliveryStatus pgEnum; orders.discountSatang, orders.carrier, orders.trackingNumber, orders.deliveryStatus, orders.trackingUpdatedAt columns."
    - "env.ts: CARRIER_PROVIDER (default \"manual\")."
    - "settings.ts HOT_KEYS: loyaltyEarnRate, loyaltyPointBaht, pdpaPolicyVersion, pdpaPolicyText keys added."
  key_links:
    - "0005 registered in ALL 38 self-resetting tests + migrate.test.ts down/up sequences (Pitfall 4)."
    - "deliveryStatus pgEnum declared BEFORE the orders ALTER that references it (migration ordering)."
  prohibitions:
    - "MUST NOT touch round_stock reservation columns (quota_plants/reserved_plants) — oversell guard untouched (NFR-02)."
    - "MUST NOT add any secret-shaped key to HOT_KEYS (secrets stay in env.ts)."
    - "MUST NOT use notNull-without-default on any ADD COLUMN against a populated table (0003/0004 additive idiom)."
---

<objective>
Wave-0 [BLOCKING] backbone: add every Phase-4 table/column/enum in ONE additive migration (0005_phase4), register it in every self-resetting test, add the CARRIER_PROVIDER env key and the loyalty + PDPA-policy HOT_KEYS. This precedes all feature waves.

Purpose: Build/type checks pass off the schema config even without a live migration — so a missing 0005 is a false-positive green. This plan makes the live DB match the types and keeps the 200+ test regression gate honest (the migration-0004 lesson: STATE.md 03-01).
Output: schema.ts growth, 0005 up/down SQL, test registration across 38 files + migrate.test.ts, env + settings keys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-store-marketing-scale/04-RESEARCH.md
@.planning/phases/04-web-store-marketing-scale/04-PATTERNS.md
@.planning/phases/04-web-store-marketing-scale/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + migration 0005 + env + settings keys</name>
  <files>api/src/db/schema.ts, api/drizzle/0005_phase4.sql, api/drizzle/0005_phase4.down.sql, api/src/env.ts, api/src/services/settings.ts</files>
  <read_first>
    - api/src/db/schema.ts (lines 1-20 header idiom; 27-65 pgEnum block; 181-260 customers/orders; 285-296 backInStockRequests; 332-360 consentLogs; 491+ settings) — the additive idiom + camelCase↔snake_case mapping.
    - api/drizzle/0004_phase3.sql AND 0004_phase3.down.sql (whole files) — CREATE TYPE → CREATE TABLE children → ALTER ADD COLUMN DEFAULT → FKs → UNIQUE indexes ordering; down drops children before parents, enums last.
    - api/src/env.ts (lines 36-44) — SLIP_VERIFY_PROVIDER default idiom to mirror for CARRIER_PROVIDER.
    - api/src/services/settings.ts (lines 6-14 secret warning; 25-30 HOT_KEYS map; 61-95 get/set) — HOT_KEYS is the allow-list.
  </read_first>
  <action>
    In schema.ts add, following the existing additive idiom (per D-07/08/09/11/12/21/22/26/28):
    (1) deliveryStatus pgEnum "delivery_status" with values pending, handed_to_carrier, in_transit, delivered, failed — declared in the pgEnum block BEFORE the orders table alter.
    (2) coupons table: id uuid pk, code text unique-not-null, discountKind (text: "percent"|"baht"), discountValue integer, minSubtotalSatang integer default 0, globalLimit integer nullable, globalUsed integer notNull default 0, perCustomerLimit integer notNull default 1, applicability jsonb (segment/channel targeting per D-13; default targets b2c), active boolean notNull default true, expiresAt timestamptz nullable, createdAt timestamptz default now.
    (3) couponRedemptions table: id uuid pk, couponId fk→coupons, customerId fk→customers, orderId fk→orders, createdAt; UNIQUE(coupon_id, customer_id) per D-08.
    (4) loyaltyLedger table: id uuid pk, customerId fk→customers, orderId fk→orders nullable, kind text ("earn"|"redeem"), points integer (positive earn / negative redeem), createdAt; partial UNIQUE(order_id) WHERE kind='earn' for once-per-order earn idempotency (D-12).
    (5) broadcasts table: id uuid pk, title text, segment jsonb (predefined type + tags per D-15), messageJson jsonb (Flex/image/link per D-18), status text ("draft"|"scheduled"|"sent"|"failed"), scheduledAt timestamptz nullable, sentCount integer default 0, createdAt.
    (6) customerTags table: id uuid pk, customerId fk→customers, tag text, createdAt; index on customerId (D-15 manual tags).
    (7) varietyImages table: id uuid pk, varietyId fk→varieties, url text, sort integer default 0, createdAt; boxImages table mirror for boxes (D-28 gallery; varieties.imageUrl / boxes.imageUrl remain the cover, backward compatible).
    (8) orders ALTER: add discountSatang integer notNull default 0 (net-payable support, Pitfall 2); carrier text nullable; trackingNumber text nullable; deliveryStatus deliveryStatus-enum nullable; trackingUpdatedAt timestamptz nullable (DEL-05).
    Hand-write 0005_phase4.sql to match schema.ts exactly (drizzle-kit generate may seed it, but verify it mirrors the 0004 shape) and 0005_phase4.down.sql dropping children before parents and the delivery_status TYPE last. NEVER touch round_stock columns.
    In env.ts add CARRIER_PROVIDER: t.String({ default: "manual" }) mirroring SLIP_VERIFY_PROVIDER.
    In settings.ts add to HOT_KEYS: loyaltyEarnRate, loyaltyPointBaht, pdpaPolicyVersion, pdpaPolicyText (D-11 economics + owner PDPA versioning). Add sensible getHotSettings defaults (loyaltyEarnRate: 1 point per 100 baht subtotal; loyaltyPointBaht: 1 point = 1 baht; pdpaPolicyVersion: current version string). No secret-shaped keys.
  </action>
  <verify>
    <automated>cd api && bun test tests/migrate.test.ts tests/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - migrate.test.ts passes with the new 0005 up/down pair registered (up→down→up reversible).
    - `cd api && bun run db:generate` (or the project drizzle generate script) produces no pending diff after 0005 is written (schema.ts and SQL agree).
    - settings.test.ts still green; a PUT with a secret-shaped key is still rejected (HOT_KEYS closed allow-list).
    - grep confirms `round_stock` reserved/quota columns are unchanged in 0005_phase4.sql (no ALTER on round_stock).
  </acceptance_criteria>
  <done>0005 migration + schema + env + settings keys land; migrate.test.ts and settings.test.ts green; no round_stock edit.</done>
</task>

<task type="auto">
  <name>Task 2: Register 0005 in every self-resetting test + full suite green</name>
  <files>api/tests/migrate.test.ts, api/tests/*.test.ts</files>
  <read_first>
    - 04-PATTERNS.md "Shared Patterns: Migration 0005" — the exact register idiom and the enumerated 38-file list.
    - api/tests/catalog-crud.test.ts (setup/teardown region, ~lines 58-65) — the file() up/down sequence to copy.
    - api/tests/migrate.test.ts (lines 22-30 constant block; 70+ down ordering) — add PHASE4_UP/PHASE4_DOWN constants after PHASE3.
  </read_first>
  <action>
    In migrate.test.ts add PHASE4_UP="drizzle/0005_phase4.sql" and PHASE4_DOWN="drizzle/0005_phase4.down.sql" constants and extend the up→down→up assertion chain to cover 0005 (newest down first, newest up last).
    In every self-resetting test harness that resets the schema, add to teardown (newest DOWN first) a `.file("drizzle/0005_phase4.down.sql").catch(() => {})` BEFORE the 0004 down, and to setup (oldest UP first) a `.file("drizzle/0005_phase4.sql")` AFTER the 0004 up. The 38 files are enumerated in 04-PATTERNS.md: auth-boundary, auth-line, b2b-approval, boxes-crud, box-order, box-reservation, cancel-release, catalog-crud, catalog, consent, created-hold-sweep, crop, dashboard, forecast-publish, harvest-log, hold-expiry, hold-idempotent, me-account, member-checkout-linkage, migrate, multi-round-order, notify, order-endpoint-race, order-snapshot, order-transition, packing-queue, prices-resolution, reorder, reports, reservation, round-cutoff, settings, slip-dedup, soldout-notify, staff-login, standing-reserve, subscription-gen, verified-slip-park. Only edit files that actually reset the schema (grep for the 0004 file() call to confirm before editing each). Because Phase-4 tables FK into varieties/customers/orders/rounds, a test that resets must drop 0005 first or the 0004/0001 down fails.
  </action>
  <verify>
    <automated>cd api && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `cd api && bun test` runs the full suite green (was 187 pass / 0 fail on develop; must stay 0 fail with 0005 registered).
    - `grep -rl "0004_phase3.down.sql" api/tests` — every file returned also contains "0005_phase4.down.sql".
    - No test teardown fails on a foreign-key drop-order error.
  </acceptance_criteria>
  <done>Full suite green; 0005 registered in every schema-resetting test alongside 0004.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | An unregistered/incorrect migration silently diverges the live DB from the compiled types (false-positive green). |
| settings write → config | A secret-shaped key entering HOT_KEYS would leak via the settings API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | round_stock reservation columns | high | mitigate | 0005 is additive only; grep-assert no ALTER on round_stock; oversell guard (NFR-02) untouched |
| T-04-02 | Information Disclosure | settings HOT_KEYS | high | mitigate | Only non-secret loyalty/PDPA keys added; PUT body additionalProperties:false rejects secret-shaped keys (settings.test.ts) |
| T-04-03 | Denial of Service | test regression gate | medium | mitigate | Register 0005 in all self-resetting tests so teardown drop-order never fails (Pitfall 4) |
</threat_model>

<verification>
- `cd api && bun test` full suite green after 0005 registered.
- migrate.test.ts proves 0005 up→down→up reversible.
- No round_stock change in 0005; no secret key in HOT_KEYS.
</verification>

<success_criteria>
Migration 0005 exists, applies/rolls back cleanly, is registered in every self-resetting test, and the full suite is green; CARRIER_PROVIDER env + loyalty/PDPA HOT_KEYS present; reservation guard untouched.
</success_criteria>

<artifacts_produced>
- Tables: coupons, coupon_redemptions, loyalty_ledger, broadcasts, customer_tags, variety_images, box_images
- Enum: delivery_status (pending, handed_to_carrier, in_transit, delivered, failed)
- Columns on orders: discount_satang, carrier, tracking_number, delivery_status, tracking_updated_at
- Migration: 0005_phase4.sql + 0005_phase4.down.sql
- env: CARRIER_PROVIDER
- settings HOT_KEYS: loyaltyEarnRate, loyaltyPointBaht, pdpaPolicyVersion, pdpaPolicyText
</artifacts_produced>

<output>
Create `.planning/phases/04-web-store-marketing-scale/04-01-SUMMARY.md` when done.
</output>
