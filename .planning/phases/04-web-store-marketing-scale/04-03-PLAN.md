---
phase: 04-web-store-marketing-scale
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - api/src/services/coupon.ts
  - api/src/services/loyalty.ts
  - api/src/services/order-transition.ts
  - api/src/routes/orders.ts
  - api/src/routes/payments.ts
  - api/src/routes/coupons.ts
  - api/src/routes/loyalty.ts
  - web-admin/src/views/CouponComposer.vue
  - web-admin/src/views/LoyaltySettings.vue
  - api/tests/coupon.test.ts
  - api/tests/coupon-race.test.ts
  - api/tests/loyalty.test.ts
  - api/tests/checkout-discount.test.ts
autonomous: true
requirements: [MKT-01, CUST-03]
must_haves:
  truths:
    - "An admin creates a coupon (percent/baht, min, expiry, global + per-customer limit, applicability); a customer applies it at checkout and the order total + PromptPay QR reflect the discount."
    - "A member earns points when an order reaches paid, and can redeem points as a baht discount at checkout; balance = SUM(ledger)."
    - "A discounted order's expected slip amount matches the discounted total (auto-paid, not parked in awaiting_review)."
  artifacts:
    - "coupon.ts redeemCouponGuarded() (guarded UPDATE + UNIQUE per-customer arbiter)."
    - "loyalty.ts earnPoints()/redeemPointsGuarded()/getBalance() (append-only ledger)."
    - "orders.ts accepts couponCode + redeemPoints, computes discountSatang pre-QR; payments.ts expectedAmountSatang subtracts discountSatang."
    - "coupons.ts admin CRUD + loyalty.ts GET balance routes; CouponComposer.vue + LoyaltySettings.vue."
  key_links:
    - "Discount composes into netSatang BEFORE buildPromptPayPayload, keeping netSatang % 100 === 0 (whole-baht QR)."
    - "earnPoints runs in-tx on the paid transition via applyTransition, ON CONFLICT DO NOTHING (once-per-order)."
  prohibitions:
    - "MUST NOT trust a client-sent price/discount/points value — client sends only couponCode (string) + redeemPoints (bounded integer); server resolves the money."
    - "MUST NOT let a percent coupon produce fractional satang in the QR amount (floor discounts to whole baht)."
    - "MUST NOT open a second reservation path — discount is applied inside the existing POST /orders tx after reserve() succeeds."
    - "MUST NOT earn/redeem points for a guest (non-member) order."
---

<objective>
The money slice: coupons (MKT-01) and loyalty points (CUST-03) compose into the existing POST /orders transaction and PromptPay path. Admin creates coupons and tunes loyalty economics; customers apply a coupon and redeem points at checkout; both discounts reduce the total before the QR is built, and the slip's expected amount is corrected so a discounted order auto-pays.

Purpose: Deliver Success Criterion 2 without touching the oversell guard. This is the riskiest slice (money + whole-baht QR invariant), so it is one coherent plan.
Output: coupon + loyalty services, order/payment composition edits, admin surfaces, invariant tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-web-store-marketing-scale/04-RESEARCH.md
@.planning/phases/04-web-store-marketing-scale/04-PATTERNS.md
@.planning/phases/04-web-store-marketing-scale/04-UI-SPEC.md
@api/src/services/reservation.ts
@api/src/routes/orders.ts
@api/src/routes/payments.ts
@api/src/services/order-transition.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coupon.ts + loyalty.ts services + unit/race tests</name>
  <files>api/src/services/coupon.ts, api/src/services/loyalty.ts, api/tests/coupon.test.ts, api/tests/coupon-race.test.ts, api/tests/loyalty.test.ts</files>
  <read_first>
    - api/src/services/reservation.ts (lines 12-14 sql-only rule; 25 DbOrTx type; 32-47 reserve() guarded UPDATE) — copy the guarded conditional-UPDATE shape verbatim.
    - api/src/routes/payments.ts (lines 67-79 isUniqueViolation walks .cause 5 deep) — the 23505 arbiter helper.
    - api/src/services/settings.ts (61-95 getHotSettings) — read loyaltyEarnRate / loyaltyPointBaht.
    - api/tests/reservation.test.ts (N-way race harness) — the concurrent-redemption test shape for coupon-race.test.ts.
    - 04-PATTERNS.md "coupon.ts" + "loyalty.ts" sections — Patterns 1/2/3 line anchors.
  </read_first>
  <behavior>
    - coupon: valid code within global+per-customer limit, not expired, subtotal ≥ min → returns discountSatang (whole baht) and records a redemption.
    - coupon: exhausted global limit → guarded UPDATE returns zero rows → OrderError("coupon_exhausted",409).
    - coupon: same customer second use of a per-customer-1 coupon → 23505 → OrderError("coupon_already_used",409).
    - coupon: N concurrent redemptions of a global-limit-1 coupon → exactly one success (race test, mirror reservation.test.ts).
    - loyalty: earnPoints inserts a positive ledger row once per order (second call = no-op via ON CONFLICT).
    - loyalty: redeemPointsGuarded bounded ≤ available balance and ≤ payable; over-redeem throws OrderError("points_insufficient",409).
    - loyalty: getBalance = SUM(points).
  </behavior>
  <action>
    Create coupon.ts: redeemCouponGuarded(tx, code, customerId, subtotalSatang, tier) runs a parameterized guarded UPDATE on coupons (SET global_used = global_used + 1 WHERE code AND active AND (expires_at IS NULL OR expires_at > now()) AND (global_limit IS NULL OR global_used < global_limit) RETURNING discount_kind, discount_value, min_subtotal_satang, applicability); reject on zero rows; enforce min_subtotal and applicability (D-13 default b2c) in-code; compute discountSatang in WHOLE BAHT (percent → floor to 100-satang multiples; baht → already whole) and cap at subtotal; then INSERT coupon_redemptions(couponId, customerId, orderId) and translate 23505 via isUniqueViolation → "coupon_already_used". Accept DbOrTx; must run inside the order tx. sql template only, no string concat.
    Create loyalty.ts: earnPoints(tx, orderId) reads loyaltyEarnRate from settings, computes points from the order subtotal (A4: earn on subtotal), INSERT loyalty_ledger(kind='earn') ON CONFLICT (order_id) WHERE kind='earn' DO NOTHING; redeemPointsGuarded(tx, customerId, orderId, points) validates points ≤ getBalance and converts to baht via loyaltyPointBaht, floors to whole baht, caps ≤ payable, INSERT kind='redeem' negative row, returns discountSatang; getBalance(db, customerId)=SUM. Gate earn/redeem on membership (caller passes isMember; guest never earns — Pitfall 5).
  </action>
  <verify>
    <automated>cd api && bun test tests/coupon.test.ts tests/coupon-race.test.ts tests/loyalty.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - coupon-race.test.ts: N concurrent redemptions of a limit-1 coupon → exactly 1 success, N-1 rejects.
    - coupon.test.ts: expiry, min-subtotal, per-customer-cap (23505→409) all covered and green.
    - loyalty.test.ts: earn idempotent (double-call = one row), redeem bounded, balance = SUM.
  </acceptance_criteria>
  <done>coupon.ts + loyalty.ts services green with race + idempotency + bound tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose discount into orders.ts + payments.ts + earn on paid</name>
  <files>api/src/routes/orders.ts, api/src/routes/payments.ts, api/src/services/order-transition.ts, api/tests/checkout-discount.test.ts</files>
  <read_first>
    - api/src/routes/orders.ts (lines 77-149 body schemas; 424-476 subtotal/fee/QR block; 488-599 tx + reserve at 571) — where to add couponCode/redeemPoints and the discount composition.
    - api/src/routes/payments.ts (lines 131 GET qr total; 183 slip expectedAmountSatang) — both must subtract discountSatang.
    - api/src/services/order-transition.ts (lines 149-176 cancelled branch) — copy its shape for the paid branch.
    - 04-PATTERNS.md "orders.ts" + "payments.ts" + "loyalty.ts/order-transition" — Pattern 6 + Pitfall 1/2.
  </read_first>
  <behavior>
    - A checkout with a valid percent coupon yields netSatang % 100 === 0 and the QR encodes the discounted whole-baht amount.
    - The persisted order has discount_satang > 0 and total reflects subtotal + fee - discount.
    - payments.ts expectedAmountSatang for that order equals the discounted total; a correct slip auto-pays (not awaiting_review).
    - Entering paid inserts exactly one earn ledger row (re-entering paid does not double-credit).
  </behavior>
  <action>
    In orders.ts CreateOrderBody add optional couponCode (t.String) and redeemPoints (t.Integer minimum 1 maximum bounded). Inside the tx AFTER reserve() succeeds and BEFORE buildPromptPayPayload: let discountSatang=0; if couponCode → discountSatang += redeemCouponGuarded(tx, couponCode, customerId, subtotalSatang, tier); if redeemPoints and isMember → discountSatang += redeemPointsGuarded(tx, customerId, orderId, redeemPoints); netSatang = max(0, subtotalSatang + (deliveryFeeSatang ?? 0) - discountSatang); assert netSatang % 100 === 0; build QR from netSatang/100; persist discount_satang on the order and store net total. Client never sends a price/discount value (server resolves).
    In payments.ts change BOTH the GET /orders/:id/qr total (line 131) and the slip expectedAmountSatang (line 183) to subtract ord.discountSatang (or read the stored net total) so a discounted order's expected amount matches — REQUIRED (Pitfall 2).
    In order-transition.ts add, mirroring the cancelled branch, an in-tx branch: if (next === "paid" && current !== "paid" && order.isMember) await earnPoints(tx, orderId). Durable in-tx write (not the fire-and-forget notifier).
    Add checkout-discount.test.ts asserting: whole-baht QR invariant after percent coupon + points; expectedAmountSatang subtracts discount; earn-on-paid fires once.
  </action>
  <verify>
    <automated>cd api && bun test tests/checkout-discount.test.ts tests/order-transition.test.ts tests/order-endpoint-race.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - checkout-discount.test.ts: netSatang % 100 === 0 after a percent coupon + points; expectedAmountSatang equals discounted total; a matching slip auto-pays.
    - order-transition.test.ts green with the added paid-earn branch (existing transitions unbroken).
    - order-endpoint-race.test.ts still green (reservation path unchanged).
  </acceptance_criteria>
  <done>Discount composes pre-QR keeping whole-baht invariant; expectedAmount corrected; earn-on-paid idempotent; races unbroken.</done>
</task>

<task type="auto">
  <name>Task 3: Coupon admin CRUD + loyalty routes + admin views</name>
  <files>api/src/routes/coupons.ts, api/src/routes/loyalty.ts, web-admin/src/views/CouponComposer.vue, web-admin/src/views/LoyaltySettings.vue</files>
  <read_first>
    - api/src/routes/crop.ts (requireRole owner|admin CRUD shape) — the admin route idiom.
    - api/src/routes/reports.ts (line 86 requireRole GET) — read-only member/admin route shape for loyalty balance.
    - api/src/services/settings.ts (setHotSettings) — LoyaltySettings writes loyaltyEarnRate/loyaltyPointBaht.
    - web-admin/src/components/DataTable.vue + web-admin/src/views/Settings.vue — the DataTable + hot-config form to reuse.
    - 04-UI-SPEC.md "New-surface copy inventory" (coupon/points copy) + "New Component Inventory" — CouponComposer / LoyaltySettings copy + accent rules.
  </read_first>
  <action>
    Replace the coupons.ts stub with admin CRUD (requireRole owner|admin): POST create coupon (code, discountKind, discountValue, minSubtotalSatang, globalLimit, perCustomerLimit, applicability, expiresAt), GET list, PATCH deactivate. Replace the loyalty.ts stub with GET /loyalty/balance (member-gated, reuse the me-orders member gate keyed on customers.line_user_id) returning getBalance. Build CouponComposer.vue: DataTable of coupons + create form using UI-SPEC copy ("สร้างคูปอง", empty "ยังไม่มีคูปอง"), accent only on the primary create CTA, deactivate confirm dialog ("ปิดใช้งานคูปองนี้?"). Build LoyaltySettings.vue reusing the Phase-3 settings hot-config form to edit loyaltyEarnRate + loyaltyPointBaht.
  </action>
  <verify>
    <automated>cd api && bun test tests/coupon.test.ts && cd ../web-admin && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - coupons.ts admin routes gated by requireRole owner|admin (a non-admin token → 403); covered by an added case in coupon.test.ts.
    - loyalty balance route returns SUM for a member, 401/403 for guest/missing token.
    - web-admin builds; CouponComposer + LoyaltySettings render (no longer stubs).
  </acceptance_criteria>
  <done>Admin coupon CRUD + loyalty balance route + both admin views live and building.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /orders | Client may send couponCode/redeemPoints; must never send a resolved price/discount. |
| checkout → order tx | Coupon limit + points balance are DB properties enforced atomically inside the tx. |
| discount → PromptPay QR | Discount must keep the whole-baht QR + expected-amount invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering/EoP | coupon over-redemption (race/replay) | high | mitigate | Guarded UPDATE global limit + UNIQUE(coupon,customer) per-customer, both in the order tx (Pattern 1/2); coupon-race.test.ts |
| T-04-07 | Tampering | client-inflated discount/points | high | mitigate | Server resolves discount + points value; client sends only code + bounded integer (mirrors T-01-06 price snapshot) |
| T-04-08 | Tampering/DoS | discount vs expected-amount desync | high | mitigate | Persist discount_satang; payments.ts expectedAmountSatang subtracts it in both computations (Pitfall 2); checkout-discount.test.ts |
| T-04-09 | Tampering | fractional-satang QR from percent coupon | high | mitigate | Floor discounts to whole baht; assert netSatang % 100 === 0 before QR (Pitfall 1) |
| T-04-10 | EoP | non-admin creating/deactivating coupons | high | mitigate | requireRole owner|admin on all coupon-admin routes (V4) |
| T-04-11 | Tampering | points credited to guest ephemeral row | medium | mitigate | earn/redeem gated on isMember / line_user_id (Pitfall 5) |
</threat_model>

<verification>
- coupon.test.ts, coupon-race.test.ts, loyalty.test.ts, checkout-discount.test.ts all green.
- order-endpoint-race.test.ts unbroken (no second reservation path).
- web-admin builds with CouponComposer + LoyaltySettings.
</verification>

<success_criteria>
Admin creates coupons and tunes loyalty economics; customers apply coupon + redeem points at checkout; discounts reduce the total before the QR while keeping the whole-baht + expected-amount invariants; earn-on-paid is once-per-order; oversell guard untouched.
</success_criteria>

<artifacts_produced>
- Services: coupon.ts (redeemCouponGuarded), loyalty.ts (earnPoints, redeemPointsGuarded, getBalance)
- Edits: orders.ts (couponCode/redeemPoints → discount pre-QR), payments.ts (expectedAmountSatang subtracts discount), order-transition.ts (earn-on-paid)
- Routes: coupons.ts (admin CRUD), loyalty.ts (GET balance)
- Views: CouponComposer.vue, LoyaltySettings.vue
- Tests: coupon.test.ts, coupon-race.test.ts, loyalty.test.ts, checkout-discount.test.ts
</artifacts_produced>

<output>
Create `.planning/phases/04-web-store-marketing-scale/04-03-SUMMARY.md` when done.
</output>
