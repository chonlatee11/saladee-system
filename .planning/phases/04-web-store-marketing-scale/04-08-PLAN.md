---
phase: 04-web-store-marketing-scale
plan: 08
type: execute
wave: 3
depends_on: ["04-01", "04-04"]
files_modified:
  - web-store/package.json
  - web-store/nuxt.config.ts
  - web-store/app.vue
  - web-store/assets/style.css
  - web-store/composables/useApi.ts
  - web-store/pages/index.vue
  - web-store/pages/p/[id].vue
autonomous: false
requirements: [ORD-05]
user_setup:
  - service: nuxt-ssr-host
    why: "The Nuxt SSR store needs a host (Cloudflare Pages edge or the existing VPS behind Caddy) — decided in Plan 09."
    env_vars:
      - name: NUXT_PUBLIC_API_URL
        source: "The live API base URL (same API the LIFF app calls)"
  - service: production-domain
    why: "Full SEO value (sitemap/canonical/OG) needs a real domain replacing sslip.io (deferred prereq, D-06) — the store builds/tests without it."
must_haves:
  truths:
    - "A visitor loads the public web store on desktop and mobile, sees the current round's catalog (server-rendered), and opens a product page with real imagery."
    - "Product pages are server-rendered with meta/OG tags and product JSON-LD; a sitemap is generated (D-06)."
  artifacts:
    - "web-store/ Nuxt SSR app (nuxt.config with @nuxtjs/seo + Tailwind v4 + NUXT_PUBLIC_API_URL)."
    - "useApi.ts Eden Treaty composable against the EXISTING API; index.vue catalog SSR; p/[id].vue product page + JSON-LD."
    - "assets/style.css with the @theme token block copied verbatim from web/ + store-max 1200px desktop layout."
  key_links:
    - "The store reads the SAME GET /catalog (round availability + cut-off + gallery) as LINE — one round model (D-04)."
    - "Tokens/components reuse web/ verbatim — one brand, N apps (UI-SPEC inheritance rule)."
  prohibitions:
    - "MUST NOT introduce shadcn/a component registry — plain Vue SFC + Tailwind v4 CSS-first (UI-SPEC)."
    - "MUST NOT install @nuxtjs/seo before the [SUS] legitimacy checkpoint is approved."
    - "MUST NOT add any new order/payment API — the store consumes existing endpoints only."
---

<objective>
Stand up the public web storefront (ORD-05) as a NEW Nuxt SSR app (D-01) that reuses the existing API, Vue components, and design tokens: a desktop-first-yet-mobile-degrading catalog and SSR product pages with SEO (sitemap/OG/JSON-LD). This plan delivers browse; Plan 09 adds cart→checkout→pay.

Purpose: Success Criterion 1 (browse a public web storefront on the same order core) — the browse half, SEO-ready.
Output: web-store Nuxt app scaffold, Eden client, SSR catalog + product pages, SEO.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-web-store-marketing-scale/04-RESEARCH.md
@.planning/phases/04-web-store-marketing-scale/04-PATTERNS.md
@.planning/phases/04-web-store-marketing-scale/04-UI-SPEC.md
@web/src/api.ts
@web/src/style.css
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify @nuxtjs/seo package legitimacy before install</name>
  <action>Manual verification per how-to-verify below; do not install @nuxtjs/seo until approved (never auto-approve this gate).</action>
  <what-built>Nothing yet — this gate precedes installing @nuxtjs/seo, flagged [SUS] (too-new publish date only) by the Package Legitimacy Audit. nuxt itself is Approved.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@nuxtjs/seo and confirm the package is the harlan-zw / nuxt-seo project with healthy weekly downloads (~72k) and repo github.com/harlan-zw/nuxt-seo.
    2. Confirm the version to install (research saw 5.3.2) is a real published release, not a typosquat.
    3. Approve to proceed with `bun add @nuxtjs/seo@<version>` in web-store/.
  </how-to-verify>
  <resume-signal>Type "approved" to install @nuxtjs/seo, or name an alternative (individual @nuxtjs/sitemap + nuxt-og-image + nuxt-schema-org).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Scaffold web-store Nuxt app + tokens + Eden client</name>
  <files>web-store/package.json, web-store/nuxt.config.ts, web-store/app.vue, web-store/assets/style.css, web-store/composables/useApi.ts</files>
  <read_first>
    - web/src/api.ts (lines 5-11 treaty<App>(API_URL)) — the Eden client to wrap as a Nuxt composable.
    - web/src/style.css (@theme token block) — copy verbatim into the store's assets/style.css.
    - 04-RESEARCH.md "Recommended Project Structure" + "Installation (web-store app only)" — nuxt 4.4.8, @nuxtjs/seo, @elysiajs/eden, tailwindcss 4.3.1 + @tailwindcss/vite.
    - 04-UI-SPEC.md "Design System" + "Spacing Scale" (store-max 1200px) + "Token source of truth".
  </read_first>
  <action>
    Create a new web-store/ package (separate from web/ and web-admin/): bun add nuxt@4.4.8 @nuxtjs/seo@<approved-version> @elysiajs/eden; bun add -d tailwindcss@4.3.1 @tailwindcss/vite. Configure nuxt.config.ts with the @nuxtjs/seo module, the Tailwind v4 vite plugin, runtimeConfig public NUXT_PUBLIC_API_URL, and a nitro preset placeholder (final host chosen in Plan 09). assets/style.css: @import "tailwindcss"; + the @theme block copied verbatim from web/src/style.css + the store-max 1200px layout constant. app.vue mounts the store shell. composables/useApi.ts wraps treaty<App>(config.public.apiUrl) importing the App type from ../../api/src/index (compile-time-safe calls to the EXISTING API — no new endpoints). No shadcn, no Pinia.
  </action>
  <verify>
    <automated>cd web-store && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web-store && bun run build` produces an SSR build with no type errors.
    - useApi.ts resolves the App type from the api package (Eden typed client).
    - The @theme block matches web/src/style.css (one brand); no shadcn dependency present.
  </acceptance_criteria>
  <done>Nuxt web-store scaffold builds with shared tokens + Eden client against the existing API.</done>
</task>

<task type="auto">
  <name>Task 2: SSR catalog + product page + SEO</name>
  <files>web-store/pages/index.vue, web-store/pages/p/[id].vue</files>
  <read_first>
    - web/src/views/CatalogView.vue + VarietyDetailView.vue — the catalog grid + product detail to adapt to SSR.
    - web/src/components/* (VarietyCard, QtyStepper, EmptyState) — reuse verbatim.
    - api/src/routes/catalog.ts (payload incl. gallery from Plan 04) — the SSR data source (round availability, cut-off, gallery).
    - 04-UI-SPEC.md store copy (hero "เลือกผักรอบนี้", catalog empty "ยังไม่เปิดรอบขาย") + desktop layout (hero, multi-column, store-max) + typography Display 28px.
  </read_first>
  <action>
    index.vue: SSR-fetch GET /catalog via useApi in a server context; render a desktop hero (Display 28px, CTA "เลือกผักรอบนี้") + a multi-column catalog grid (store-max 1200px, degrades mobile-first) reusing VarietyCard; show the "ยังไม่เปิดรอบขาย" empty state when no round is open (D-04 same round model). p/[id].vue: SSR product page reusing the detail layout + the product gallery (cover + images from the catalog payload, D-27/28); emit meta/OG tags and product JSON-LD via @nuxtjs/seo (useSeoMeta / defineOgImage / useSchemaOrg) for D-06; ensure the sitemap module picks up product routes. Reuse web/ components; do not fork the token system.
  </action>
  <verify>
    <automated>cd web-store && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - Build emits server-rendered catalog + product HTML (crawlable), including JSON-LD product data and OG/meta tags on p/[id].
    - The sitemap route is generated by @nuxtjs/seo.
    - Catalog shows the open round only and the empty state when no round is open (matches LINE round model).
  </acceptance_criteria>
  <done>SSR catalog + product pages with SEO render on the same round/catalog data as LINE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public browser → web store | Public, unauthenticated browse; must not widen any admin/private surface. |
| new dependency → build | @nuxtjs/seo is [SUS] (too-new); supply-chain risk at install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | npm install (@nuxtjs/seo [SUS]) | high | mitigate | Blocking-human legitimacy checkpoint verifying npmjs.com before install (never auto-approved) |
| T-04-26 | Information Disclosure | store consuming existing public catalog only | low | accept | Store calls only GET /catalog (already public); no new endpoint, no admin surface widened |
| T-04-27 | Tampering | shared-cache leak of b2b pricing | medium | mitigate | Store is B2C public; catalog WR-01 Cache-Control private + Vary Authorization behavior is preserved server-side |
</threat_model>

<verification>
- `cd web-store && bun run build` green (SSR + SEO).
- Product pages emit JSON-LD/OG; sitemap generated.
</verification>

<success_criteria>
A public Nuxt SSR store renders the current round's catalog and product pages (real imagery, SEO tags, sitemap) on the same order core and round model as LINE, reusing the shared token system; @nuxtjs/seo installed only after the legitimacy gate.
</success_criteria>

<artifacts_produced>
- New app: web-store/ (Nuxt SSR)
- Files: nuxt.config.ts, app.vue, assets/style.css (@theme + store-max), composables/useApi.ts, pages/index.vue, pages/p/[id].vue
</artifacts_produced>

<output>
Create `.planning/phases/04-web-store-marketing-scale/04-08-SUMMARY.md` when done.
</output>
