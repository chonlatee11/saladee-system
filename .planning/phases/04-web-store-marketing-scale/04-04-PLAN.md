---
phase: 04-web-store-marketing-scale
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - api/src/routes/product-images.ts
  - api/src/routes/catalog.ts
  - web-admin/src/views/ProductImages.vue
  - api/tests/product-images.test.ts
  - api/tests/catalog.test.ts
autonomous: true
requirements: [ORD-05]
must_haves:
  truths:
    - "An admin uploads product photos (real upload → sharp compress → R2 → stored URL), and multiple images per product form a gallery."
    - "The catalog API returns the image gallery (cover + additional images) so the store, LIFF, and Flex cards can render them."
  artifacts:
    - "product-images.ts admin upload/list/delete route (reuse sharp + R2 pipeline)."
    - "catalog.ts payload includes variety_images / box_images gallery; varieties.imageUrl / boxes.imageUrl stays the cover."
    - "ProductImages.vue admin uploader (multi-image, reuses SlipUploader sharp+R2 pattern)."
  key_links:
    - "Product photos use a PUBLIC R2 path (marketing assets), not the private signed-URL path slips use (D-26 discretion)."
    - "Cover image (varieties.imageUrl) is backward compatible — gallery is additive (D-28)."
  prohibitions:
    - "MUST NOT allow a client-named R2 key — server assigns the key (reuse payments.ts idiom)."
    - "MUST NOT accept an image over the size bound (reuse MAX_SLIP_BYTES 5 MiB) or a non-admin uploader."
    - "MUST NOT allow deleting the last cover image (block if it would leave the product coverless)."
---

<objective>
Real product imagery across the store (D-26/27/28), the concrete owner request behind the storefront. Admin uploads photos through the existing sharp+R2 pipeline; multiple images per product form a gallery; the catalog API returns them so the web store, LIFF catalog, and broadcast Flex cards all render real product photos.

Purpose: Supports ORD-05 (a real storefront needs real imagery) with a genuine upload pipeline, not URL-paste.
Output: product-image upload route, catalog gallery payload, admin uploader view.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-web-store-marketing-scale/04-RESEARCH.md
@.planning/phases/04-web-store-marketing-scale/04-PATTERNS.md
@.planning/phases/04-web-store-marketing-scale/04-UI-SPEC.md
@api/src/routes/payments.ts
@api/src/routes/catalog.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: product-images upload/list/delete route + catalog gallery</name>
  <files>api/src/routes/product-images.ts, api/src/routes/catalog.ts, api/tests/product-images.test.ts, api/tests/catalog.test.ts</files>
  <read_first>
    - api/src/routes/payments.ts (lines 57-97 sharpCompress + presign PUT; 190-193 MAX_SLIP_BYTES size bound) — copy the compress + upload pipeline; product images differ only in PUBLIC path.
    - api/src/routes/catalog.ts (lines 223/329/405 payload assembly points) — add the gallery array to the variety/box payload.
    - api/src/routes/crop.ts (requireRole owner|admin) — admin RBAC.
    - 04-PATTERNS.md "product-images.ts" section — sharp+R2 anchors + public-path difference.
  </read_first>
  <behavior>
    - Admin POST uploads an image ≤ 5 MiB → sharp-compressed → stored at a server-assigned public R2 key → variety_images row created; returns the stored URL.
    - An image > 5 MiB is rejected before sharp.
    - A non-admin token → 403.
    - DELETE removes a gallery image but refuses if it is the last remaining cover.
    - GET /catalog returns each product's gallery (cover + additional images).
  </behavior>
  <action>
    Replace the product-images.ts stub with requireRole owner|admin routes: POST /product-images (multipart, varietyId or boxId + file) — bound size to MAX_SLIP_BYTES BEFORE sharp, sharpCompress, presign PUT to a PUBLIC R2 path with a server-assigned key, INSERT variety_images/box_images(url, sort); GET list by product; DELETE by image id (block if it would leave the product with no cover — cover stays varieties.imageUrl / boxes.imageUrl, D-28). Never accept a client-named key.
    Edit catalog.ts to include a gallery array (cover first, then variety_images/box_images ordered by sort) in the variety and box payloads, keeping the existing imageUrl field as cover for backward compatibility (D-27/28). Keep the existing WR-01 Cache-Control private + Vary Authorization behavior intact.
    Add product-images.test.ts (upload size bound + admin gate + delete-last-cover block) and extend catalog.test.ts to assert the gallery array appears.
  </action>
  <verify>
    <automated>cd api && bun test tests/product-images.test.ts tests/catalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - product-images.test.ts: >5 MiB rejected; non-admin 403; delete-last-cover blocked; upload persists a variety_images row with a server-assigned key.
    - catalog.test.ts: gallery array present; imageUrl cover unchanged for a product with no extra images.
    - Existing catalog cache headers (WR-01) unbroken.
  </acceptance_criteria>
  <done>Admin product-image upload + gallery in catalog payload, tested green.</done>
</task>

<task type="auto">
  <name>Task 2: ProductImages admin uploader view</name>
  <files>web-admin/src/views/ProductImages.vue</files>
  <read_first>
    - web/src/components/SlipUploader.vue — the sharp+R2 upload UX to mirror.
    - web-admin/src/components/DataTable.vue + web-admin/src/api.ts (Eden client) — table + typed calls.
    - 04-UI-SPEC.md "New Component Inventory" (ProductImageUploader) + copy for image remove confirm ("ลบรูปนี้ออกจากสินค้า?").
  </read_first>
  <action>
    Replace the ProductImages.vue stub: a product picker + multi-image uploader (drag/select → POST /product-images), a gallery grid showing cover + additional images with a remove action that opens the shared destructive-confirm dialog ("ลบรูปนี้ออกจากสินค้า?" / confirm "ลบรูป"), blocking removal of the last cover. Reuse the SlipUploader upload idiom and the shared token/spinner components. Accent only on the primary upload CTA.
  </action>
  <verify>
    <automated>cd web-admin && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - web-admin builds; ProductImages view uploads and lists a product's gallery.
    - Remove uses the shared confirm dialog and blocks last-cover removal (UI mirrors the server rule).
  </acceptance_criteria>
  <done>ProductImages admin uploader live and building.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin upload → R2 | Binary upload can abuse CPU/storage; must be admin-only, size-bound, server-keyed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | DoS | product-image upload abuse | high | mitigate | Admin-only requireRole + 5 MiB bound + sharp compress + server-assigned R2 key (reuse payments.ts) |
| T-04-13 | Tampering | client-named R2 key / path traversal | medium | mitigate | Server assigns the key; client cannot name the object |
| T-04-14 | Information Disclosure | public product path vs private slips | low | accept | Product photos are public marketing assets (D-26 discretion); slips stay on the private signed-URL path (unchanged) |
</threat_model>

<verification>
- product-images.test.ts + catalog.test.ts green.
- web-admin builds with ProductImages uploader.
</verification>

<success_criteria>
Admin uploads real product photos through the sharp+R2 pipeline; multiple images per product form a gallery returned by the catalog API for the store/LIFF/Flex; cover image backward compatible; uploads are admin-only, size-bound, and server-keyed.
</success_criteria>

<artifacts_produced>
- Route: product-images.ts (upload/list/delete)
- Edit: catalog.ts (gallery in payload)
- View: ProductImages.vue
- Tests: product-images.test.ts, catalog.test.ts (extended)
</artifacts_produced>

<output>
Create `.planning/phases/04-web-store-marketing-scale/04-04-SUMMARY.md` when done.
</output>
