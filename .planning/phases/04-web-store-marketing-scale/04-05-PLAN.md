---
phase: 04-web-store-marketing-scale
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - api/src/services/carrier/index.ts
  - api/src/services/carrier/manual.adapter.ts
  - api/src/services/carrier/types.ts
  - api/src/routes/tracking.ts
  - web-admin/src/views/CarrierTrackingForm.vue
  - api/tests/tracking.test.ts
autonomous: true
requirements: [DEL-05]
must_haves:
  truths:
    - "Staff record a carrier + tracking number on an order and update its delivery status through a defined enum."
    - "Each delivery-status transition notifies the customer via LINE (reusing the Phase-2 status-notification seam), skipping guests with no line_user_id."
  artifacts:
    - "carrier/{index,manual.adapter,types}.ts — env-selected adapter seam (mirror slip-verify)."
    - "tracking.ts admin route: set carrier + tracking number, PATCH delivery_status."
    - "CarrierTrackingForm.vue (carrier pick tile + tracking field + status badge)."
  key_links:
    - "makeCarrierAdapter(env.CARRIER_PROVIDER) returns ManualCarrierAdapter now; a commented grab/lalamove case is the later-phase seam with no route rework."
    - "Status push reuses notify.ts pushOrderUpdate (guest guard already present)."
  prohibitions:
    - "MUST NOT call a live carrier API this phase — manual entry only behind the adapter seam (D-21)."
    - "MUST NOT allow a non-admin to set tracking or change delivery status (requireRole owner|admin)."
    - "MUST NOT invent a status outside the delivery_status enum (pending→handed_to_carrier→in_transit→delivered / failed)."
---

<objective>
Multi-carrier delivery tracking (DEL-05): staff enter carrier + tracking number and advance a defined delivery-status enum; each transition pushes a LINE status update. Built generically behind a carrier-adapter seam (mirroring the Phase-2 slip-provider adapter) so a real Grab/Lalamove API plugs in later without rework — manual entry now.

Purpose: Deliver Success Criterion 4 (carrier tracking) at MVP scope with a future-proof seam.
Output: carrier adapter seam, tracking admin route, tracking admin view.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-web-store-marketing-scale/04-RESEARCH.md
@.planning/phases/04-web-store-marketing-scale/04-PATTERNS.md
@.planning/phases/04-web-store-marketing-scale/04-UI-SPEC.md
@api/src/services/slip-verify
@api/src/services/notify.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carrier adapter seam + tracking route + status push</name>
  <files>api/src/services/carrier/index.ts, api/src/services/carrier/manual.adapter.ts, api/src/services/carrier/types.ts, api/src/routes/tracking.ts, api/tests/tracking.test.ts</files>
  <read_first>
    - api/src/services/slip-verify/index.ts (whole file, 11-23 env switch) + slipok.adapter.ts + types.ts — copy the env-selected switch + interface + concrete-impl shape verbatim.
    - api/src/services/notify.ts (lines 218-227 pushOrderUpdate; guest guard `if (!order.lineUserId) return false` at 223) — reuse for status push, do not build a new client.
    - api/src/routes/orders.ts (PATCH status → applyTransition idiom) + api/src/routes/reports.ts (line 86 requireRole) — admin route + RBAC shape.
    - 04-PATTERNS.md "carrier/..." + "tracking.ts" sections; 04-UI-SPEC.md "Delivery Status Enum" (binding palette + Thai labels).
  </read_first>
  <behavior>
    - makeCarrierAdapter("manual") returns ManualCarrierAdapter; an unknown provider throws.
    - PATCH sets carrier + tracking_number and advances delivery_status; each transition calls the notify seam.
    - A member order (has line_user_id) receives a LINE push on transition; a guest order (no line_user_id) is skipped without error.
    - A non-admin token → 403; an invalid enum value → 422.
  </behavior>
  <action>
    Create carrier/types.ts (CarrierAdapter interface: recordTracking / normalizeStatus), carrier/manual.adapter.ts (ManualCarrierAdapter — staff-entered values pass through), carrier/index.ts (makeCarrierAdapter(provider = env.CARRIER_PROVIDER) switch: case "manual" → ManualCarrierAdapter; a commented case for grab/lalamove; default throw; export the env-selected singleton). Call sites import only the CarrierAdapter interface + singleton, never a concrete vendor.
    Replace the tracking.ts stub with requireRole owner|admin routes: PATCH /tracking/:orderId sets carrier + tracking_number + delivery_status + tracking_updated_at (validate delivery_status against the enum via TypeBox), then reuse notify.ts pushOrderUpdate to send a Flex status update (guest guard already skips no-line_user_id); GET tracking by order. Update trackingUpdatedAt on each change.
    Add tracking.test.ts: adapter switch (manual + unknown throws), status transition persists + fires the notifier (assert via the notify seam mock, mirroring notify.test.ts), guest order skipped, non-admin 403, invalid enum 422.
  </action>
  <verify>
    <automated>cd api && bun test tests/tracking.test.ts tests/notify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tracking.test.ts green: adapter seam, enum-validated PATCH, push fires for member / skipped for guest, RBAC + validation gates.
    - notify.test.ts unbroken (status-push seam reused, not modified in a breaking way).
    - Enum values match the UI-SPEC binding (pending, handed_to_carrier, in_transit, delivered, failed).
  </acceptance_criteria>
  <done>Carrier adapter seam + admin tracking route + LINE status push, tested green.</done>
</task>

<task type="auto">
  <name>Task 2: CarrierTrackingForm admin view</name>
  <files>web-admin/src/views/CarrierTrackingForm.vue</files>
  <read_first>
    - web-admin/src/views/StandingOrders.vue or Packing.vue — an existing admin form + DataTable view to mirror.
    - 04-UI-SPEC.md "Delivery Status Enum" (badge palette) + "New Component Inventory" (CarrierTrackingForm) + copy ("บันทึกเลขพัสดุ", empty "ยังไม่มีเลขพัสดุ").
  </read_first>
  <action>
    Replace the CarrierTrackingForm.vue stub: a list of orders needing tracking + a form to pick carrier (Grab / Lalamove / general) via the selected-tile ring pattern (ring-2 ring-accent), enter the tracking number, and set delivery status. Render the current status as a DeliveryStatusBadge using the binding palette mapping. Save CTA "บันทึกเลขพัสดุ"; empty state "ยังไม่มีเลขพัสดุ". Accent only on the save CTA + the selected carrier tile.
  </action>
  <verify>
    <automated>cd web-admin && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - web-admin builds; the tracking form sets carrier + number + status and reflects the badge palette.
    - Carrier pick uses the single approved selected-tile pattern (no second variant).
  </acceptance_criteria>
  <done>CarrierTrackingForm view live and building.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff → tracking route | Only staff may set tracking / change status; status pushes reach the customer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | EoP | non-admin setting tracking/status | high | mitigate | requireRole owner|admin on all tracking routes (V4) |
| T-04-16 | Tampering | invalid/forged delivery status | medium | mitigate | TypeBox enum validation; only the delivery_status enum accepted (422 otherwise) |
| T-04-17 | Information Disclosure | status push to wrong recipient | medium | mitigate | Reuse notify.ts pushOrderUpdate keyed on the order's own line_user_id (guest skipped) |
</threat_model>

<verification>
- tracking.test.ts + notify.test.ts green.
- web-admin builds with CarrierTrackingForm.
</verification>

<success_criteria>
Staff record carrier + tracking number and advance a defined delivery-status enum; each transition notifies the customer via LINE; the carrier adapter seam supports a later real API without route rework; all tracking routes are admin-only and enum-validated.
</success_criteria>

<artifacts_produced>
- Services: carrier/index.ts (makeCarrierAdapter), carrier/manual.adapter.ts, carrier/types.ts
- Route: tracking.ts (set carrier/number, PATCH status)
- View: CarrierTrackingForm.vue
- Tests: tracking.test.ts
</artifacts_produced>

<output>
Create `.planning/phases/04-web-store-marketing-scale/04-05-SUMMARY.md` when done.
</output>
