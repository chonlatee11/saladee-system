---
phase: 04-web-store-marketing-scale
plan: 07
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - api/src/services/crop-recommend.ts
  - api/src/routes/crop.ts
  - web-admin/src/views/PlantingMix.vue
  - api/tests/crop-recommend.test.ts
autonomous: true
requirements: [CROP-07]
must_haves:
  truths:
    - "An admin opening the crop-planning page sees a recommendation of per-variety plant counts derived from trailing demand, and can apply it to prefill the planting-mix template."
    - "Demand counts realized sales PLUS unmet demand (back-in-stock requests) so stockouts do not undercount demand."
  artifacts:
    - "crop-recommend.ts: pure plantsToMeetDemand kernel (inverse of forecast) + trailing-demand aggregate."
    - "GET /crop/planting-recommendation (on-demand, requireRole owner|admin|grower)."
    - "PlantingRecommendationCard inside PlantingMix.vue that prefills the mix with full admin override."
  key_links:
    - "plantsToMeetDemand(demandPlants, survivalPct) = ceil(demandPlants * 100 / survivalPct) — inverse of forecast.ts forecastPlants."
    - "Aggregate uses Phase-3 per-variety survival % + trailing avg over last N rounds; N configurable."
  prohibitions:
    - "MUST NOT build a new yield formula — reuse the inverse of forecast.ts (the survival-haircut authority)."
    - "MUST NOT string-concat the demand aggregate SQL — parameterized sql fragments only (reports.ts idiom)."
    - "MUST NOT overwrite the mix without admin action — the card prefills, admin overrides and confirms (D-25)."
---

<objective>
Demand-driven planting recommendation (CROP-07): recommend per-Monday plant counts from trailing demand history, converted back to plant counts via the Phase-3 per-variety params. Realized sales plus unmet demand (back-in-stock requests) feed the signal; the recommendation surfaces as a card in the crop-planning page that prefills the existing planting-mix template with full admin override.

Purpose: Deliver Success Criterion 4 (per-Monday planting recommendation) as a prefill, not a read-only report, closing the demand→supply loop on the proven forecast kernel.
Output: pure inverse-forecast kernel + trailing-demand aggregate, on-demand endpoint, recommendation card.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-web-store-marketing-scale/04-RESEARCH.md
@.planning/phases/04-web-store-marketing-scale/04-PATTERNS.md
@.planning/phases/04-web-store-marketing-scale/04-UI-SPEC.md
@api/src/services/forecast.ts
@api/src/routes/crop.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: crop-recommend.ts kernel + aggregate + endpoint</name>
  <files>api/src/services/crop-recommend.ts, api/src/routes/crop.ts, api/tests/crop-recommend.test.ts</files>
  <read_first>
    - api/src/services/forecast.ts (lines 38-40 forecastPlants pure kernel = floor(planted*pct/100)) — the inverse to implement; keep it pure/DB-free.
    - api/src/services/reports.ts (lines 36 SOLD_STATUSES; 64-83 whereFrag parameterized aggregate) — realized-sales aggregate shape.
    - api/src/db/schema.ts (back_in_stock_requests ~285-296; varieties survival/gPlant params) — unmet-demand source + per-variety params.
    - api/src/routes/crop.ts (lines 17-19 planting_mix template; 75 requireRole owner|admin|grower) — endpoint RBAC + where the prefill target lives.
    - 04-PATTERNS.md "crop-recommend.ts" section (Code Example) + CONTEXT D-23/24/25; RESEARCH A2/A6 (on-demand; unmet-demand heuristic configurable).
  </read_first>
  <behavior>
    - plantsToMeetDemand(180, 90) = 200 (inverse of forecastPlants(200,90)=180); pure, no DB.
    - The demand aggregate = trailing avg over last N rounds of realized sales per variety (SOLD_STATUSES) PLUS back-in-stock requests per variety (converted per the configurable heuristic).
    - GET /crop/planting-recommendation returns per-variety recommended plant counts; requires an admin/grower token.
    - With fewer than N rounds of history, the endpoint returns a no-data signal (drives the UI "ข้อมูลดีมานด์ยังไม่พอ" state).
  </behavior>
  <action>
    Create crop-recommend.ts: pure plantsToMeetDemand(demandPlants, survivalPct) = Math.ceil(demandPlants * 100 / survivalPct) (inverse of forecast.ts); computeDemandRecommendation(db, { nRounds }) building a parameterized aggregate — realized sales per variety over the last N rounds (SOLD_STATUSES, reports.ts idiom) plus back_in_stock_requests per variety converted to a plant estimate via a configurable heuristic (A6 — default one pack-equivalent per request, tunable) — averaged, then run through plantsToMeetDemand using each variety's survival %. N defaults from a sensible constant (make configurable if cheap, D-23). Never string-concat SQL.
    Edit crop.ts to add GET /crop/planting-recommendation (requireRole owner|admin|grower) returning the per-variety recommendation + a no-data flag when history < N rounds.
    Add crop-recommend.test.ts: unit-test the pure kernel (inverse round-trips forecastPlants), and an integration test of the aggregate (seed sales + a back-in-stock request, assert the recommended count includes both and applies the survival haircut inverse).
  </action>
  <verify>
    <automated>cd api && bun test tests/crop-recommend.test.ts tests/forecast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - crop-recommend.test.ts: plantsToMeetDemand inverts forecastPlants; aggregate includes realized sales + back-in-stock demand; survival haircut applied.
    - GET /crop/planting-recommendation is admin/grower-gated and returns a no-data flag under the N-round threshold.
    - forecast.test.ts unbroken (kernel reused, not modified).
  </acceptance_criteria>
  <done>Inverse-forecast kernel + trailing-demand aggregate + on-demand endpoint, tested green.</done>
</task>

<task type="auto">
  <name>Task 2: PlantingRecommendationCard prefill in PlantingMix.vue</name>
  <files>web-admin/src/views/PlantingMix.vue</files>
  <read_first>
    - web-admin/src/views/PlantingMix.vue (the existing mix template form) — where the card mounts and how the mix quantities are edited.
    - 04-UI-SPEC.md CROP-07 copy (heading "คำแนะนำปริมาณปลูก (จากดีมานด์ย้อนหลัง)", apply CTA "ใช้ค่านี้เติมในแผนปลูก", no-data states) + "New Component Inventory" (PlantingRecommendationCard).
  </read_first>
  <action>
    Add a PlantingRecommendationCard to PlantingMix.vue that calls GET /crop/planting-recommendation and shows per-variety recommended plant counts (reuse the chart palette if charted). An "ใช้ค่านี้เติมในแผนปลูก" CTA prefills the mix quantities in the existing template — editable, never auto-committed (D-25 full admin override). Show the "ข้อมูลดีมานด์ยังไม่พอ" no-data state when the endpoint signals insufficient history. Accent only on the apply CTA.
  </action>
  <verify>
    <automated>cd web-admin && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - web-admin builds; the card renders the recommendation and its apply CTA prefills the mix (values remain editable before save).
    - The no-data state renders when history is insufficient.
  </acceptance_criteria>
  <done>Recommendation card prefills the planting mix with admin override; web-admin builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin/grower → recommendation | Read-only compute over existing tables; must be role-gated and injection-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-23 | Tampering | SQL injection in the demand aggregate | high | mitigate | Drizzle parameterized sql fragments only (reports.ts whereFrag), never string concat |
| T-04-24 | EoP | non-staff reading demand analytics | medium | mitigate | requireRole owner|admin|grower on the endpoint (V4) |
| T-04-25 | Tampering | recommendation silently overwriting the mix | low | mitigate | Card prefills only; admin edits + confirms before save (D-25) |
</threat_model>

<verification>
- crop-recommend.test.ts + forecast.test.ts green.
- web-admin builds with the recommendation card.
</verification>

<success_criteria>
An admin sees a demand-driven per-variety plant-count recommendation (realized sales + unmet demand, survival-haircut inverse) and applies it to prefill the planting mix with full override; the compute is on-demand, role-gated, and injection-safe.
</success_criteria>

<artifacts_produced>
- Service: crop-recommend.ts (plantsToMeetDemand, computeDemandRecommendation)
- Edit: crop.ts (GET /crop/planting-recommendation)
- View: PlantingMix.vue (PlantingRecommendationCard)
- Tests: crop-recommend.test.ts
</artifacts_produced>

<output>
Create `.planning/phases/04-web-store-marketing-scale/04-07-SUMMARY.md` when done.
</output>
