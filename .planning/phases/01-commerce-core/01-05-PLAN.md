---
phase: 01-commerce-core
plan: 05
type: execute
wave: 5
depends_on: [01-01, 01-02, 01-04]
files_modified:
  - api/src/routes/boxes.ts
  - api/src/services/reservation.ts
  - api/src/routes/orders.ts
  - api/src/routes/catalog.ts
  - api/src/index.ts
  - api/tests/box-reservation.test.ts
  - api/tests/box-order.test.ts
autonomous: true
requirements: [INV-07, PAY-04]
must_haves:
  truths:
    - "A mixed salad box is orderable only up to its scarcest component's quantity (availability = min over components)"
    - "Ordering a box decrements every component in ONE all-or-nothing transaction; if any component is short, NO component is decremented"
    - "A box order captures its BOM + resolved box price (sum of components, or the fixed override) as a frozen snapshot"
    - "An admin (owner/admin) can define a box as a fixed BOM of component varieties with per-box plant quantities"
  artifacts:
    - path: "api/src/services/reservation.ts"
      provides: "reserveBox() — per-component guarded reserve in one deterministically-ordered transaction"
      exports: ["reserveBox"]
    - path: "api/src/routes/boxes.ts"
      provides: "Staff CRUD for boxes + box_components BOM (D-17/D-18)"
      exports: ["boxesRoutes"]
  key_links:
    - from: "api/src/services/reservation.ts"
      to: "box_components sorted by variety_id"
      via: "db.transaction { for each component reserve(); any 0 rows → rollback }"
      pattern: "db.transaction"
    - from: "api/src/routes/orders.ts"
      to: "reserveBox"
      via: "POST /orders box line path"
      pattern: "reserveBox"
---

## Phase Goal

**As a** customer, **I want to** order a mixed salad box, **so that** I get a curated bundle that is only offered when every ingredient is actually in stock.

<objective>
Deliver the mixed salad box (criterion 3): a fixed-BOM bundle that is orderable only up to its scarcest component and decrements every component in one all-or-nothing transaction, with a frozen BOM + price snapshot. This extends the proven reservation core to the multi-row case and closes the last phase requirement (INV-07).

Purpose: INV-07 (box limited by scarcest component, atomic multi-component decrement) + PAY-04 (box snapshot). Admin defines the BOM (D-17); box price = sum of components or a fixed override (D-18).
Output: `services/reservation.ts` gains `reserveBox()`; `routes/boxes.ts` (staff CRUD BOM); `POST /orders` accepts box lines; catalog shows box availability = min across components. Proven by an all-or-nothing box race test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-commerce-core/01-CONTEXT.md
@.planning/phases/01-commerce-core/01-RESEARCH.md
@.planning/phases/01-commerce-core/01-PATTERNS.md
@.planning/phases/01-commerce-core/01-01-SUMMARY.md
@.planning/phases/01-commerce-core/01-02-SUMMARY.md
@.planning/phases/01-commerce-core/01-04-SUMMARY.md

<interfaces>
<!-- Consumed from 01-01/01-02/01-04. reserveBox() is NEW here; keep reserve()/release() unchanged. -->
reservation.ts: reserve(tx, roundId, varietyId, plants): Promise<boolean>  // do not modify signature
pricing.ts:     deriveUnitPriceSatang(pricePerKgSatang, gramsPerUnit): number
</interfaces>
@api/src/routes/orders.ts
@api/src/routes/catalog.ts
@api/src/routes/files.ts
@api/src/plugins/auth.plugin.ts
@api/src/index.ts
@api/tests/reservation.test.ts
@api/tests/seed.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reserveBox() all-or-nothing multi-component decrement + box CRUD; auth-gated</name>
  <files>api/src/services/reservation.ts, api/src/routes/boxes.ts, api/src/index.ts</files>
  <read_first>
    - 01-RESEARCH.md Pattern 2 (lines 235-257) "All-or-nothing multi-component decrement" (sort components by variety_id for a global lock order; any 0-row reserve → tx.rollback) + Anti-Pattern "decrementing outside a transaction"
    - 01-PATTERNS.md §"reservation.ts (NO ANALOG)" (box = per-component reserve in one db.transaction) + §"boxes.ts (staff CRUD)" + §"Authentication / RBAC gate"
    - 01-CONTEXT.md D-17 (fixed BOM, availability = min, all-or-nothing), D-18 (price = sum of components or fixed override)
    - api/src/services/reservation.ts (existing reserve()/release() — extend, do not break) + 01-01-SUMMARY.md (boxes/box_components columns)
  </read_first>
  <behavior>
    - reserveBox(tx, roundId, components[], boxQty): components sorted by varietyId (global lock order → no deadlock); per component reserve(tx, roundId, varietyId, plantsPerBox*boxQty); if ANY returns false, throw to roll back the whole transaction → zero net decrement across all components.
    - Given components A(quota 4/plant-per-box 2) and B(quota 1/plant-per-box 1): box availability = min(floor(4/2), floor(1/1)) = 1. Ordering 1 box succeeds; ordering 2 boxes fails and leaves A.reserved and B.reserved both unchanged.
    - POST /boxes (owner/admin) creates a box + its box_components BOM; optional fixed_price_satang override.
  </behavior>
  <action>
    Extend `services/reservation.ts` with `reserveBox(tx, roundId, components, boxQty)`: sort `components` by varietyId, loop calling the existing `reserve(tx, roundId, c.varietyId, c.plantsPerBox * boxQty)`; on the first false, `tx.rollback()` (throws → whole db.transaction rolls back). Do NOT alter reserve()/release(). Add a pure helper `boxAvailability(components, stockByVariety)` = min(floor((quota−reserved)/plantsPerBox)) for catalog/display.
    Create `routes/boxes.ts` exporting `boxesRoutes` (dbPlugin + authPlugin): staff-guarded POST/PUT/DELETE boxes { name, description, imageUrl, fixedPriceSatang?: t.Integer({minimum:0}), components: array of { varietyId: uuid, plantsPerBox: t.Integer({minimum:1}) } (minItems 1) }; OPEN GET list/detail (public). Apply requireRole("owner","admin") on writes only. APPEND `.use(boxesRoutes)` to index.ts.
  </action>
  <verify>
    <automated>cd api && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Build/source check: `cd api && bunx tsc --noEmit` exits 0 (reserveBox + boxesRoutes compile; the behavioral proof is Task 2).
    - Source: `grep -c "reserveBox" api/src/services/reservation.ts` ≥ 1; `grep -c "requireRole" api/src/routes/boxes.ts` ≥ 1; `grep -c "boxesRoutes" api/src/index.ts` ≥ 1.
    - Source: reserve()/release() signatures unchanged (grep confirms both still exported).
    - Behavior verified by Task 2 tests (all-or-nothing).
  </acceptance_criteria>
  <done>reserveBox() (all-or-nothing, lock-ordered) + box CRUD BOM mounted; existing reserve/release intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Box ordering via POST /orders + catalog box availability + all-or-nothing proof</name>
  <files>api/src/routes/orders.ts, api/src/routes/catalog.ts, api/tests/box-reservation.test.ts, api/tests/box-order.test.ts</files>
  <read_first>
    - 01-RESEARCH.md Pattern 2 + "box availability = min(floor((quota−reserved)/plantsPerBox))" (line 257) + Pitfall 1/2 (real PG, pool max ≥ N)
    - 01-02-SUMMARY.md (POST /orders body + snapshot shape) + 01-04-SUMMARY.md (catalog response shape — where box availability plugs in)
    - api/src/routes/orders.ts (existing single-line path — add a box-line branch) + api/src/routes/catalog.ts (existing reads — add boxes)
    - api/tests/reservation.test.ts + api/tests/seed.ts (reuse the race + seed harness)
  </read_first>
  <behavior>
    - POST /orders with a box line { boxId, roundId, qty } resolves the BOM, computes box price (sum of resolved component pack/kg prices, or fixed_price_satang override — D-18), opens db.transaction calling reserveBox → on success inserts orders + an order_lines row with line_kind 'box', box_bom_json snapshot + resolved unit_price_satang (D-16/PAY-04); on shortfall → 409, nothing decremented.
    - All-or-nothing: with component B as the scarce one, an over-quota box order returns 409 and BOTH components' reserved_plants are unchanged (partial-failure decrements nothing).
    - Catalog: GET /catalog includes boxes with availability = min(floor((quota−reserved)/plantsPerBox)); a box whose scarce component is 0 shows soldOut "หมดรอบนี้".
    - Race: N parallel box orders for the last available box → exactly one 201, N-1 409; the scarce component's reserved never exceeds its quota.
  </behavior>
  <action>
    Extend `routes/orders.ts` POST /orders to accept a box line variant (line_kind 'box'): load box + box_components, compute the box price per D-18 (sum of component resolved prices via deriveUnitPriceSatang, else fixed_price_satang), call reserveBox inside the existing transaction, insert the box order_line with box_bom_json + snapshot fields. Extend `routes/catalog.ts` to include boxes with boxAvailability + sold-out label.
    Write `tests/box-reservation.test.ts` (real PG): seed a box with a scarce component; assert availability = min across components; assert an over-quota box order rolls back with ZERO net decrement on every component (all-or-nothing); assert an at-limit order succeeds and decrements every component. Write `tests/box-order.test.ts`: race N parallel box orders for the last box → exactly one 201, N-1 409, scarce component reserved ≤ quota; assert the winning order_line carries the box_bom_json snapshot.
  </action>
  <verify>
    <automated>docker compose -f api/tests/docker-compose.pg.yml up -d && cd api && bun test tests/box-reservation.test.ts tests/box-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f api/tests/docker-compose.pg.yml up -d && cd api && bun test tests/box-reservation.test.ts tests/box-order.test.ts` exits 0.
    - Behavior: after a failed (over-quota) box order, every component's reserved_plants is unchanged (all-or-nothing).
    - Behavior: box availability = min(floor((quota−reserved)/plantsPerBox)); scarce-component-0 box shows soldOut "หมดรอบนี้".
    - Behavior: the box order_line stores box_bom_json + resolved unit_price_satang snapshot.
  </acceptance_criteria>
  <done>Box ordering decrements all components atomically or none; catalog shows box availability; all-or-nothing + race proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /orders box line | untrusted boxId/qty; price/BOM resolved server-side |
| staff → box CRUD | owner/admin only |
| concurrent box orders → multiple round_stock rows | multi-row oversell + deadlock boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Tampering | box over-decrements a scarce component (multi-row oversell) | mitigate | reserveBox() = per-component guarded reserve inside one db.transaction; any 0-row → rollback (all-or-nothing); race test proves one winner. |
| T-01-19 | Availability (DoS) | deadlock between concurrent box orders locking components in different orders | mitigate | Components sorted by variety_id → global lock order (RESEARCH Pattern 2). |
| T-01-20 | Tampering | client-supplied box price | mitigate | Box price resolved server-side (sum of component prices or stored fixed override, D-18); never trust client amount; snapshot is server-derived. |
| T-01-21 | Elevation | non-staff defines/edits a box BOM | mitigate | requireRole("owner","admin") on box writes; GET reads public. |
| T-01-SC | Tampering | supply chain | accept | No new packages this phase. |
</threat_model>

<verification>
- `docker compose -f api/tests/docker-compose.pg.yml up -d`
- `cd api && bun test tests/box-reservation.test.ts tests/box-order.test.ts` exit 0.
- `cd api && bun test` FULL suite green (phase gate: all reservation/box/snapshot/cancel/cutoff/auth tests + the concurrency proofs racing real PG17).
</verification>

<success_criteria>
- A mixed box is orderable only up to its scarcest component and decrements every component all-or-nothing in one transaction (criterion 3 / INV-07).
- Box order captures BOM + resolved price snapshot (PAY-04 / D-18).
- Full Phase 1 suite green — the oversell-safe engine is proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-commerce-core/01-05-SUMMARY.md` when done. Record the reserveBox() contract, the box order-line snapshot shape, and confirm the full Phase 1 test suite passes (the phase acceptance gate).
</output>
